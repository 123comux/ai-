# 微信开发者工具操作清单（2026-08-12）

> 对应《本周迭代计划_2026-08-11.md》第一部分（微信登录联调）+ 第二部分（组队/分享解锁端上实测）。
> 代码已全部完成并提交，本清单只做**开发者工具里的验证操作**。

---

## 0. 环境确认（已完成）

| 项 | 状态 |
|----|------|
| 后端 API `http://localhost:8000` | ✅ 已启动，`/health` 返回 ok |
| 前端 weapp watch 编译 | ✅ 已启动，`dist/` 为最新代码 |
| AppID `wxdb921a9b6073a909` | ✅ root + dist 的 project.config.json 已统一 |
| `backend/.env` 真实登录配置 | ✅ `DEV_MODE=false` + AppID/Secret 已配，未提交 git |

> 若后端口径变化需重启：`cd backend && py -m uvicorn main:app --host 0.0.0.0 --port 8000`

---

## 1. 打开项目

1. 打开**微信开发者工具** → 「导入项目」
2. 目录选择 **`D:\aishixi\dist`**，AppID 填 `wxdb921a9b6073a909`（或选「测试号」后手动改）
3. 右上角「详情」→ 「基本信息」，确认 AppID 显示 `wxdb921a9b6073a909`
4. 右上角「详情」→ 「本地设置」，勾选 **「不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书」**
5. 点「编译」，等待首页出来

> 首次导入若提示版本/工具不兼容，按提示升级基础库（≥ 3.0 即可）。

---

## 2. 微信真实登录验证

1. 开发者工具顶部「清缓存」→ 「全部清除」，再点「编译」
2. 自动触发 `wx.login()`，观察：
   - 「我的」页显示**真实微信昵称 + 头像**（不是"体验用户_xxxx"）
   - 后端日志出现 `POST /api/auth/wechat-login` 与 code2session 请求
3. 确认数据库真实 openid：
   - 后端目录执行：`py -c "import sqlite3;c=sqlite3.connect('database.sqlite3');print([r['openid'] for r in c.execute('select openid from users order by id desc limit 3')])"`
   - 预期 openid 以 `wx_` 开头，且为真实微信 openid（非 `dev_`）

### 2.1 双账号数据隔离验证
1. 用一个微信号登录（模拟器 = 微信 A）
2. 点开发者工具「预览」→ 手机微信扫码（手机 = 微信 B），B 正常登录
3. 在 A 里做 1 次打卡 + 发 1 帖 + 创建 1 个小组
4. 在 B 里查看：打卡记录、帖子列表、我的小组 → 都应**看不到 A 的数据**
5. 后端验证（数据按 user_id 隔离）：
   - `select user_id, count(*) from checkins group by user_id;`
   - `select user_id, count(*) from community_posts group by user_id;`
   - `select owner_id from teams;`

---

## 3. 组队学习实测（社区 → 组队 tab）

1. 进入「社区」页 → 第 4 个 tab「组队」
2. **创建小组**：点「创建小组」→ 填组名 → 创建 → 弹窗显示**邀请码**（如 `1A2B3C4D`）
3. **第二账号加入**：切换模拟器到另一微信（或真机）→ 组队 tab → 「加入小组」→ 输入邀请码 → 「加入成功」
4. **组内排行**：点小组卡片展开，看到成员列表 + 各自「学习时长（分钟）」
5. **邀请分享**：点卡片上「邀请好友」→ 弹出转发，转发出去的卡片带邀请码
6. **分享进组**：另一账号点开该转发卡片 → 自动进入并加入该组
7. 后台查看：浏览器开 `http://localhost:8000/admin`，账号 `admin / admin123` → 「学习小组」「小组成员」表可见对应记录

---

## 4. 课程分享解锁实测

1. 进入任意课程详情页（首页 → 课程 → 点开一节）
2. 头部看到「分享解锁进阶实操」卡片 + 「分享解锁」按钮
3. 点「分享解锁」→ 转发给任意人（或发到文件传输助手）
4. 转发成功后自动调 `share-unlock` → 卡片变为 **「🔓 已解锁进阶实操」**，下方展开本课实战案例/提示词要点
5. **幂等验证**：返回再进入该课程 → 仍显示已解锁（不重复解锁）
6. **兜底验证**：若模拟器转发回调不触发（卡片没变），点「我已分享，手动解锁」同样生效
7. 后台「分享解锁」表可见该记录（user_id / share_type=course / share_target=课程id）

---

## 5. 常见问题排查

| 现象 | 处理 |
|------|------|
| 登录报 `invalid code` | 属正常（code 一次性）；前端会自动重试。若持续失败检查网络 |
| 登录报 `invalid appid secret` | AppSecret 被重置 → 微信公众平台重新生成，更新 `backend/.env` 并重启后端 |
| 旧 token 导致 401 | 开发者工具「清缓存 → 全部清除」后重新编译 |
| 请求 `timeout` | 确认后端仍在运行（`curl http://localhost:8000/health`）；后台窗口别关 |
| 「我的」页还是体验用户 | 清缓存重新编译，或确认 .env 里 DEV_MODE=false 且后端已重启 |
| 编译报错 | 后端 task 已跑 watch，改代码会自动重编；报错后修完自动恢复 |

---

## 6. 验收清单

- [ ] AppID 显示 `wxdb921a9b6073a909`
- [ ] 「我的」页为真实微信昵称/头像
- [ ] openid 以 `wx_` 开头
- [ ] A/B 两账号数据完全隔离
- [ ] 能创建小组、输码加入、组内看到学习时长排行
- [ ] 课程页分享后显示「已解锁」，重复进入不重复记录
- [ ] 后台 `/admin` 可见 share_unlocks / teams 记录
- [ ] `backend/.env` 未提交到 git
