# AI 实习培训小程序 — 项目报告

## 一、项目概述

**AI 实习培训小程序**是一款面向高校学生的 AI 技能学习与实习准备平台，帮助用户通过系统化的课程学习、项目实践和能力测评，为进入 AI 相关岗位做好准备。

项目采用**前后端分离**架构：

- **前端**：Taro 4.1.9 + React 18 + TypeScript + Zustand，一套代码同时编译为微信小程序（`dist/`）和 H5 网页（`dist-h5/`），使用 CSS Modules 样式方案。
- **后端**：Python FastAPI + SQLite，提供内容管理、AI 能力、用户数据等 REST API，支持 CORS 跨域。
- **AI 能力**：集成智谱 GLM-4-flash 大模型，提供 AI 导师问答、岗位匹配分析、课程推荐等能力。

项目当前共 **58 个 git 提交**，代码库结构清晰，前后端职责分明。

---

## 二、已实现功能清单

### 2.1 前端页面（18 个页面）

| 页面 | 功能 |
|------|------|
| **首页** (home) | 用户信息、Banner 轮播、学习统计、AI 工具入口、能力测评入口、学习方向、学习路径、AI 推荐课程、推荐课程/项目 |
| **学习页** (learn) | 课程分类浏览、全部课程列表、AI 课程推荐 |
| **项目页** (project) | 项目分类、项目列表、难度筛选 |
| **我的页** (mine) | 用户信息、学习统计、能力报告（五维雷达图）、功能菜单（5 项） |
| **能力测评** (assessment) | AI 能力测评问卷、提交后生成能力报告 |
| **课程详情** (courseDetail) | 课程信息、学习进度、课程目录（章节/小节展开）、视频播放入口、收藏按钮、标记完成 |
| **章节详情** (chapterDetail) | 章节知识点、案例、内容展示 |
| **学习路径** (learningPath) | 方向切换、节点进度、解锁状态、课程/项目跳转 |
| **项目详情** (projectDetail) | 项目信息、分步实践指南、推进步骤、收藏按钮 |
| **岗位对标** (jobMatching) | 输入岗位描述 → AI 结合能力报告分析匹配度、技能掌握、差距技能、推荐课程/项目 |
| **AI 导师** (tutor) | 大模型问答对话，结合课程知识库 RAG |
| **视频播放** (video) | 视频学习、观看进度记录、完成标记 |
| **我的作品集** (portfolio) | 展示有进度的项目，含技术栈、完成状态 |
| **学习目标** (goals) | 目标的增删改查、标记完成、底部弹层添加 |
| **我的收藏** (favorites) | 收藏的课程/项目列表、取消收藏、跳转详情 |
| **设置** (settings) | 编辑个人信息（昵称/年级/专业/方向）、清除缓存、退出登录 |
| **webview** | H5 内嵌页面 |

### 2.2 后端 API（12 组路由）

| 路由 | 功能 |
|------|------|
| `/api/courses` | 课程列表、详情、分类、章节数据 |
| `/api/projects` | 项目列表、详情、推进步骤、进度持久化 |
| `/api/videos` | 视频列表、按课程过滤、观看完成记录 |
| `/api/learning-paths` | 学习路径列表、节点进度、标记完成 |
| `/api/assessment` | 测评题目、提交测评、AI 融合分析 |
| `/api/user` | 能力报告、学习记录、学习统计、岗位匹配分析 |
| `/api/mine` | 作品集、学习目标 CRUD、收藏 CRUD |
| `/api/tutor` | AI 导师问答（智谱 GLM + 课程 RAG） |
| `/api/recommend` | AI 课程推荐（关键词 + 智谱） |
| `/api/knowledge` | 知识库检索 |
| `/api/content` | Banner、学习方向、菜单项 |
| `/api/admin` | 后台管理端认证与内容管理 |

### 2.3 核心亮点功能

1. **能力测评 + 五维雷达图**：完成测评后生成能力报告，展示编程基础、数学基础、机器学习、深度学习、大模型应用、项目经验六个维度的雷达图（H5 用 SVG，小程序用 Canvas，跨端适配）。
2. **AI 岗位对标**：输入目标岗位描述，智谱大模型提取岗位技能，结合用户能力报告判定技能掌握情况，计算真实匹配度，给出差距技能与推荐学习内容。
3. **AI 导师问答**：大模型 + 课程知识库检索增强（RAG），回答贴合课程内容。**双模型高可用设计**：优先调用智谱 GLM 大模型，不可用时自动回退到本地微调的 Qwen 模型兜底，模型接口可插拔，便于后续更换/升级。
4. **学习路径**：按方向组织课程/项目节点，带解锁状态和进度追踪。
5. **完整学习闭环**：看视频 → 记进度 → 完成课程 → 推进项目 → 沉淀作品集 → 生成学习记录与统计。
6. **后台管理系统**：内置 CMS 管理端（`/api/admin`），支持课程、内容等管理（当前为管理员基础认证）。
7. **企业端预留**：岗位对标功能已对接真实岗位描述分析，为后续接入企业岗位库、实习匹配与内推奠定基础。

---

## 三、项目遇到的问题及解决方案

### 3.1 小程序端五维图（雷达图）渲染异常

**问题**：五维图在小程序端占位不显示、尺寸冲突（Canvas 绘制坐标与 CSS 尺寸不匹配导致图形被裁切、偏小）。

**解决**：
- 改用 `createCanvasContext` 经典 API（canvas-id）替代 2d node 获取，避免组件挂载时序问题。
- 统一 Canvas 尺寸为 500rpx，绘制坐标换算为实际像素尺寸（`pxSize = 500 × windowWidth / 750`），与 H5 端半径比例（36%）对齐。
- 为 `getSystemInfoSync` 和绘制逻辑加 try-catch 兜底，异常不阻断页面渲染。

### 3.2 小程序端 header 渐变背景失效

**问题**：微信小程序对 `linear-gradient` 渐变渲染异常，导致蓝色渐变 header 不显示、白色文字看不清。

**解决**：header 背景改为 `$color-primary !important` 纯色兜底，并隐藏渐变层（`display:none`），保证任何环境下文字清晰。

### 3.3 课程卡片超出屏幕

**问题**：课程列表用 `grid-template-columns: 1fr 1fr`，Taro 跨端渲染时 `1fr` 列被卡片内容的 rpx 换算宽度撑开，两列总宽远超容器，卡片溢出屏幕。

**解决**：改用 `flex + flex-wrap` 布局，卡片宽度 `calc(50% - gap)`，两列精确占满一行，跨端稳定。

### 3.4 课程卡片误导性进度条

**问题**：推荐/全部课程列表的卡片上显示了进度条和百分比，误导用户以为学过了。

**解决**：从 `CourseCard` 组件移除进度条逻辑（学习路径卡的进度展示保留），清除相关样式。

### 3.5 岗位对标硬编码与结果失真

**问题**（多次迭代）：
1. 页面一进入就加载 mock 数据，显示硬编码的匹配结果。
2. AI 输出 `mastered` 为字符串 `"未知"`，被 `bool()` 转成 `True`，导致所有技能都显示"已掌握"。
3. 能力报告只有 30 分（初级），却判定大部分技能已掌握、匹配度虚高（78-94%），结果失真。

**解决**：
- 去掉初始 mock 加载，改为输入岗位描述后的空状态引导。
- 技能掌握判定改为后端基于能力报告维度得分（技能关键词映射维度），不再依赖 AI 猜。
- 掌握阈值从 30 提高到 60 分，matchScore 按技能对应维度实际得分计算。验证：30 分能力报告匹配度 43-48%（技能待提升），75 分能力报告匹配度 90%（技能已掌握），能正确区分能力水平。

### 3.6 小程序 `define is not defined` 与页面白屏

**问题**：微信开发者工具编译时出现 `ReferenceError: define is not defined`，导致小程序全部页面白屏。

**解决**：通过 Node 模拟小程序运行时验证产物逻辑层正常、确认构建产物健康（无 AMD `define` 残留、已转 ES5、wxml 完整），定位为开发者工具编译缓存问题，清缓存 + 重新编译解决。

### 3.7 小程序 watch 进程卡死

**问题**：Taro 的 weapp watch 进程在 Windows 环境下偶尔不响应源码变化（僵尸进程），导致 `dist/` 产物不更新。

**解决**：识别并杀掉僵死进程，用 preview_start 重新拉起 watch，源码改动后手动触发完整重建确保产物最新。

### 3.8 学习路径页头部背景未生效

**问题**：页面顶部用了 scss 中未定义的 `.section` 类，`.header`（蓝色背景）类从未被引用，导致头部无蓝色背景、文字看不清。

**解决**：改为引用 `.header` 类，蓝色背景正确渲染。

### 3.9 AI 能力高可用：双模型兜底设计

**问题**：AI 导师、岗位分析等依赖智谱大模型 API，一旦网络异常或 API 不可用，AI 功能会直接中断。

**解决**：设计**双模型可插拔架构**——AI 服务统一封装在 `services/` 层，优先调用智谱 GLM（快、效果好、自动带课程 RAG），失败时自动回退到本地微调的 Qwen LoRA 模型（`tutor_service.py`，离线可用）。模型接口解耦，后续更换或升级模型只需替换服务层实现，无需改动业务逻辑。

---

## 四、目前还存在的问题

1. **微信登录未实现**：当前用户数据是前端硬编码（"AI 学习者"等），无法真实关联微信用户。已规划但暂缓，需配置小程序 AppSecret 才能接入真实 code2session 登录。
2. **用户数据非隔离**：能力报告、学习记录等用户数据来自后端 JSON 文件（全局单例），多用户场景下数据会互相覆盖，暂不适合真实多用户使用。
3. **生产环境配置缺失**：前端 API 地址硬编码 `http://localhost:8000`，小程序需 HTTPS + 域名白名单，尚未配置生产部署环境。
4. **学习目标无日期编辑交互**：目标日期目前是手动输入文本，未使用日期选择器（picker mode="date"）。
5. **AI 分析响应时间**：智谱大模型分析岗位描述需数秒，loading 态体验有待优化（如加提示文案）。
6. **收藏无批量管理**：收藏页仅支持单项取消，无多选批量操作。
7. **部分页面边界场景**：课程/项目数据为空时的兜底展示、网络异常的全局错误提示策略可进一步完善。
8. **课程内容量偏少**：当前约 96 门课程、10 个项目。这是因为项目还处于 **Demo 阶段**——课程由数据管道从公开数据集（FineWeb-Edu、QVAC Genesis 等）清洗生成，重点在于打通完整学习链路、验证功能闭环，而非追求内容规模。随着进入正式运营阶段，会持续扩充课程与项目内容，并逐步引入人工精编的高质量课程。
9. **AI 能力有待升级**：当前使用智谱 GLM-4-flash 作为主模型，已能满足 Demo 场景需求，但推理深度和生成质量相比顶尖大模型还有差距。得益于可插拔的模型服务层设计（`services/zhipu_service.py` / `tutor_service.py`），后续可直接替换为更强的模型（如 GLM-4、Qwen 更大参数量版本、或开源大模型本地部署），无需改动业务代码。

---

## 五、项目演示说明

### 5.1 启动后端

```bash
cd backend
pip install -r requirements.txt
# 配置 backend/.env（ZHIPU_API_KEY 智谱 API Key）
uvicorn main:app --reload   # 默认 http://localhost:8000
```

### 5.2 启动前端

```bash
# H5 开发（端口 10087）
npm run dev:h5

# 微信小程序 watch（产物输出到 dist/）
npm run dev:weapp
```

### 5.3 演示路径

1. **微信小程序**：打开微信开发者工具，导入项目 `D:/aishixi`（miniprogramRoot 为 `dist/`），AppID 已配置，直接编译预览。
2. **H5 网页**：浏览器访问 `http://localhost:10087`。

### 5.4 主要功能演示流程

- **能力测评**：首页 → 能力分析 → 完成问卷 → 生成五维能力报告 → 我的页查看雷达图。
- **岗位对标**：我的 → 岗位能力对标 → 输入岗位描述（如"AI大模型应用开发实习生，要求Python、RAG、LangChain、Agent开发"）→ AI 分析 → 查看匹配度、技能掌握、待提升技能、推荐内容。
- **学习闭环**：首页选择学习方向 → 进入学习路径 → 打开课程 → 看视频 → 标记完成 → 推进项目 → 我的页查看统计与作品集。
- **AI 导师**：首页 AI 工具 → AI 导师 → 输入问题，结合课程知识库问答。

---

## 六、后续迭代计划

### 短期（当前阶段）
1. **接入微信登录**：配置小程序 AppSecret，后端实现 code2session 换 openid，建立 users 表，实现真实用户登录与数据隔离。
2. **用户数据按用户隔离**：能力报告、学习记录、学习目标、收藏等数据关联到具体用户。
3. **生产部署**：前端 API 地址按环境区分，小程序配置 HTTPS 域名，后端部署到服务器。
4. **AI 模型升级**：利用可插拔模型接口，将主模型从 GLM-4-flash 升级到更强的版本，优化分析深度与回答质量。

### 中期
5. **后台管理系统完善**：当前已有管理员基础认证（`/api/admin`），中期扩展为完整的运营后台——可视化课程/项目/内容管理、用户数据看板、AI 模型配置、测评题目维护，让运营人员自助管理平台内容。
6. **学习目标交互优化**：日期选择器、进度跟踪、目标提醒。
7. **收藏增强**：批量管理、分类筛选、收藏分享。
8. **AI 能力增强**：岗位对标增加对比分析、AI 导师多轮对话记忆、课程内容自动生成摘要。
9. **数据看板**：学习时长趋势图、能力维度变化曲线、技能雷达图历史对比。

### 长期
10. **社区功能**：用户作品集分享、学习心得发布、同学互评。
11. **个性化学习推荐**：基于学习行为数据的智能推荐（学什么、练什么、补什么）。
12. **企业端平台**：接入真实企业岗位库与 JD，实现岗位匹配、实习投递、企业内推的完整链路，打通"学生能力 → 企业需求"的匹配闭环。

---

## 七、总结

**AI 实习培训小程序**目前已实现一个功能完整的学习平台闭环：从**能力测评**（生成六维能力画像）→ **AI 岗位对标**（结合能力报告分析岗位匹配度）→ **学习路径**（按方向组织课程/项目）→ **课程与视频学习**（进度追踪）→ **项目实践**（分步推进）→ **作品集与统计**（成果沉淀），全程由 **AI 大模型**提供导师问答、岗位分析、课程推荐等智能化支持。AI 能力采用**智谱大模型为主、本地 Qwen 模型兜底**的双模型可插拔架构，保证高可用且便于后续升级。

项目技术选型合理：Taro 跨端（一套代码同时覆盖小程序和 H5）、FastAPI 轻量高效、大模型即插即用。在开发过程中解决了跨端渲染、Canvas 尺寸、AI 结果可靠性、布局适配等一系列实际问题，代码质量和功能完整性都达到了可演示、可迭代的水平。

当前处于 **Demo 阶段**：课程内容量相对偏少（约 96 门，由公开数据集管道生成）、主模型能力还有提升空间、真实用户体系（微信登录）和多用户数据隔离尚未实现。这些都已纳入清晰的迭代路线。整体而言，项目完成了从 0 到 1 的平台搭建，具备良好的扩展基础，后续将沿**用户体系 → 后台管理 → 企业端**三个方向持续演进，最终成为连接学生能力培养与企业用人需求的完整 AI 学习平台。
