# Vercel 部署忽略清单（只影响上传到 Vercel 的文件，不影响 git）
# 目的：把函数包压到最小（Vercel 函数解压上限 250MB），并且绝不外传本地密钥。

# 依赖与构建产物：Vercel 会用 package.json / requirements.txt 自己装、自己构建
node_modules
dist
dist-h5

# 后端用不到的重资产
backend/data/raw
backend/data/cms.db
backend/database.sqlite3
backend/tests
backend/training
**/__pycache__
**/.pytest_cache
*.log

# 本地训练出来的模型权重（3GB！函数上限 250MB，且线上没有 torch 用不到）。
# 注意只排除权重目录，backend/models/*.py（schemas.py 等）必须保留 —— 业务代码里
# `from models.schemas import ...` 依赖这个 Python 包。
backend/models/assessment
backend/models/tutor
backend/models/recommender

# 本地密钥（线上用 Vercel 环境变量注入，绝不能进部署包）
backend/.env
.env
.env.local

# 文档、临时产物
docs
.pai
*.zip
