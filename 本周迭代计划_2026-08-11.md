# 本周迭代计划（2026年8月11日 - 8月17日）

> 项目：智学 AI · 零基础 AI 教学服务平台（押金式培训版）
> 目标：本周聚焦两项 —— ①微信真实登录联调验证 ②分享解锁 + 组队学习前端补齐
> 背景：后端接口已全部就绪，本周主要完成联调验证与小程序端功能接线。

---

## 一、微信登录联调验证（周一 ~ 周三）

### 1.1 目标
确认 `wx.login → code2session → openid → token` 全链路真实可用，并验证多用户数据隔离生效。

### 1.2 前置条件（已完成 ✅）
- 根目录与 `dist/` 的 `project.config.json` AppID 已统一为 `wxdb921a9b6073a909`
- 后端 `backend/.env` 已配置：
  - `DEV_MODE=false`
  - `WECHAT_APPID=wxdb921a9b6073a909`
  - `WECHAT_SECRET=<已配置>`
- 已验证：用假 code 登录返回微信官方错误 `invalid code`，确认后端已真实调用 code2session（不再造 `dev_xxx` 用户）

### 1.3 联调步骤

**第 1 步：开发者工具重新打开项目**
- 点击「重新打开此项目」，确保读取到新的 AppID
- 右上角「详情」确认显示 `wxdb921a9b6073a909`
- 保持勾选「不校验合法域名」（本地 API 为 `http://localhost:8000`）

**第 2 步：验证登录全链路**
- 编译后自动触发 `wx.login()` 获取真实 code
- 观察后端日志出现 code2session 请求，`users` 表新用户 openid 以 `wx_` 开头（真实 openid）
- 「我的」页昵称/头像应为真实微信身份，而非"体验用户_xxxx"

**第 3 步：验证多用户数据隔离**
- 用两个不同微信号分别登录（模拟器切换用户 / 模拟器+真机各一）
- 验证：A 的测评报告、学习记录、打卡、帖子在 B 端互不可见
- 对应表按 `user_id` 隔离：`user_ability_reports`、`user_video_progress`、`user_path_progress`、`checkins`、`community_posts` 等

### 1.4 涉及文件
- 无需改代码。查证对象：`backend/routers/auth.py`（`_code2session` / `_resolve_user`）、`backend/config.py`、`backend/auth_utils.py`

### 1.5 验收标准
- [ ] 新用户 openid 为真实 `wx_` 开头
- [ ] 两个微信号登录后数据完全隔离
- [ ] `backend/.env` 未提交到 git（已由 .gitignore 排除）

### 1.6 风险与处理
| 风险 | 处理 |
|------|------|
| AppSecret 被重置 → `invalid appid secret` | 微信公众平台重新生成，更新 `.env` 并重启后端 |
| 模拟器缓存旧 token | 开发者工具「清缓存 → 全部清除」后重新编译 |
| code 一次性使用报 `code been used` | 属正常现象，前端失败会自动重试登录，无需处理 |

---

## 二、分享解锁 + 组队学习前端补齐（周三 ~ 周四）

> 后端接口已就绪，本周只做前端接线。

### 2.1 前端 API 封装（`src/services/api.ts` 新增 6 个函数）

| 函数 | 对应接口 | 说明 |
|------|----------|------|
| `createTeam(name, maxMembers)` | `POST /api/community/teams` | 创建小组，返回邀请码 |
| `joinTeam(code)` | `POST /api/community/teams/join` | 通过邀请码加入 |
| `fetchMyTeams()` | `GET /api/community/teams/mine` | 我的小组列表 |
| `fetchTeamDetail(id)` | `GET /api/community/teams/{id}` | 组详情 + 成员学习时长排行 |
| `shareUnlock(type, target)` | `POST /api/community/share-unlock` | 分享后解锁进阶内容 |
| `fetchShareUnlocks()` | `GET /api/community/share-unlock/status` | 查询已解锁内容 |

> 这些接口需要鉴权，`api.ts` 中已有 `authHeaders()` 会自动附带 token。

### 2.2 组队学习（社区页 `src/pages/community/index.tsx`）

**现状**：社区页有 打卡 / 排行榜 / 社区 三个 tab。

**改动**：新增第 4 个 tab「组队」。

1. `TABS` 增加 `{ key: 'team', label: '组队' }`
2. 组队 tab 界面：
   - 我的小组卡片列表（`fetchMyTeams`）：组名、邀请码、成员数/上限、创建时间
   - 「创建小组」按钮 → 弹窗填组名与人数上限 → 创建后展示邀请码
   - 「加入小组」按钮 → 弹窗输入邀请码 → `joinTeam`
   - 点小组卡片 → 内联展开成员列表 + 各自学习时长排行（`fetchTeamDetail`）
3. 分享邀请：小组卡片「邀请好友」→ `useShareAppMessage` 分享带邀请码的组队邀请

### 2.3 课程分享解锁（课程详情页 `src/pages/courseDetail/index.tsx`）

**现状**：课程详情页无分享入口。

**改动**：
1. 课程头部加「🔗 分享解锁」按钮
2. 组件函数体**内部**调用 `useShareAppMessage`（务必避免社区页之前的 hook 越界问题），转发路径带 `courseId`
3. 转发成功后调 `shareUnlock('course', courseId)` 记录解锁
4. 进入页面时调 `fetchShareUnlocks()`：
   - 已解锁 → 显示「已解锁进阶内容」标识
   - 未解锁 → 显示引导「分享给好友，解锁本课进阶实操」
5. 解锁内容：展示该课程的进阶实操提示词/案例（取自课程 chapters 的 sections）

### 2.4 涉及文件
- `src/services/api.ts` — 新增 6 个函数
- `src/pages/community/index.tsx` + `index.module.scss` — 组队 tab
- `src/pages/courseDetail/index.tsx` + `index.module.scss` — 分享解锁
- 后端无需改动（接口已就绪）

### 2.5 验收标准
- [ ] 能创建小组、获取邀请码、第二账号输码加入、组内看到成员学习时长排行
- [ ] 课程页分享后显示「已解锁」，重复进入不重复记录
- [ ] `share_unlocks` / `teams` 表按 `user_id` 隔离，后台 `/admin` 可见对应记录

### 2.6 风险与处理
| 风险 | 处理 |
|------|------|
| 转发成功回调在模拟器不触发 | 提供「我已分享」手动确认按钮兜底，同时调 share-unlock |
| H5 环境无转发 API | 用 `Taro.canIUse('button.open-type.share')` 判断，H5 降级为普通按钮 |
| 组件外调 hook（重蹈社区页覆辙） | 所有 `useShareAppMessage` 必须写在组件函数体内 |

---

## 三、执行顺序与节奏

```
A. API 封装（api.ts）      → 半天
B. 组队 tab（社区页）      → 1 天
C. 分享解锁（课程详情页）  → 1 天
D. 回归验证 + 提交 git      → 半天
```

每步编译验证（weapp watch 已在跑），完成一步提交一步。

---

## 四、遗留事项（不在本周，仅提醒）

- 微信小程序真实上线硬性前提需尽快启动（文档 6.6）：
  - 小程序企业主体认证（300 元/年，审核数天）
  - 域名 ICP 备案（周期 1-3 周）
  - 云服务器 + HTTPS 证书
- 这两项为线下审批流程，不依赖代码，建议尽早启动以保证 11.9 前可上线。
