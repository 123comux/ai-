# AI 实习培训微信小程序 — 立项汇报策划书（完整版）

---

## 执行摘要（Executive Summary）

**项目定位：** 面向高校学生与职场新人的 AI 实战培训小程序，以"AI 能力测评 + 个性化学习路径 + 项目实战 + 岗位能力对标"为核心闭环，打造"学得会、做得出、找得到工作"的 AI 人才培养平台。

**战略价值：** 契合公司探索"AI + 职业教育"新增长曲线的战略方向，能够复用现有技术开发能力，以较低成本切入 AI 培训赛道，通过免费内容获客、增值服务变现，形成可持续商业模式。

**核心模式：** 前端通过免费 AI 测评、基础课程、学习社区吸引用户；后端通过项目实战营、AI 作业批改、简历/作品集优化、求职陪跑等增值服务变现；长期可接入企业/HR 渠道，形成"培训—认证—就业"闭环。

**资源投入：** MVP 阶段建议投入 45–50 万元，周期 2–3 个月，配置 5–7 人轻量团队。

**预期回报：** 6 个月内完成 MVP 验证，12 个月内积累 2 万+ 用户并实现单月盈亏平衡，24 个月内服务 5 万+ 用户、推出 10+ 实战项目。

**立项建议：** 建议立项，启动 MVP 开发。

---

## 1. 项目概述

### 1.1 产品名称与定位

- **产品名称：** 智学 AI（示例）
- **产品定位：** 专注于 AI 领域的个性化实战培训平台
- **目标用户：** 大三/大四学生、研究生、转行求职的职场新人
- **核心价值主张：** 让 AI 学习有方向、有实战、有结果
- **Slogan：** "AI 实战，从 0 到 Offer"

### 1.2 与公司战略的契合点

1. **市场空间大：** AI 人才缺口持续扩大，高校培养与企业需求存在明显断层；
2. **付费意愿强：** 学生为能提升就业竞争力的技能培训付费意愿高；
3. **可复用现有资源：** 技术开发团队可快速搭建小程序，企业/HR 渠道可作为后续就业服务端；
4. **数据资产积累：** 用户学习行为数据可形成个性化推荐壁垒。

### 1.3 项目目标

| 阶段 | 时间 | 目标 |
|---|---|---|
| 短期 | 0–6 个月 | 完成 MVP 上线，验证用户学习需求和增值服务付费意愿 |
| 中期 | 6–12 个月 | 跑通"测评—学习—实战—就业"闭环，实现单月盈亏平衡 |
| 长期 | 12–24 个月 | 拓展至多个 AI 方向，建立企业合作通道，服务 5 万+ 用户 |

---

## 2. 市场与竞品分析

### 2.1 市场现状与趋势

**AI 培训市场：**
- 2024 年中国 AI 人才缺口预计超过 500 万；
- 高校 AI 课程偏理论，学生缺乏实战项目和岗位对接能力；
- AIGC、大模型应用、AI 产品经理等方向成为新热点。

**在线职业教育趋势：**
- 从"知识传授"向"能力交付"转变；
- 用户更关注"学完能做出什么项目、能否找到工作"；
- AI 技术降低了个性化教学成本，为新型培训模式提供可能。

### 2.2 竞品分析

| 竞品 | 产品形态 | 优势 | 劣势 |
|---|---|---|---|
| **慕课网/极客时间** | 录播课程平台 | 课程丰富、品牌成熟 | 缺乏个性化、实战监督和就业闭环 |
| **开课吧/网易云课堂** | 综合培训平台 | 课程体系完整 | 价格高、服务重、近年经营压力大 |
| **牛客网** | 技术题库+招聘 | 校招社区活跃、题库丰富 | 培训内容薄弱，偏刷题 |
| **ChatGPT/Kimi** | 通用 AI 工具 | 随时答疑、能力强 | 无结构化学习路径、无学习追踪、无项目指导 |

### 2.3 市场机会与差异化优势



**机会点：**
1. 现有课程平台"只教不练"，学生学完不会用；
2. 通用 AI 工具"只答不导"，学生不知道问什么、学到什么程度；
3. 垂直于 AI 领域的"学习+实战+就业"一体化平台稀缺。

**我们的差异化优势：**
1. **个性化学习路径：** 不是一套课给所有人，而是根据测评结果定制；
2. **项目实战驱动：** 每个方向都有可写进简历的项目产出；
3. **岗位能力对标：** 学习目标和真实岗位 JD 挂钩，学完知道能投什么岗位；
4. **AI 赋能学习全流程：** 测评、答疑、批改、推荐都由 AI 辅助，但内容由平台结构化设计。

### 2.4 市场份额与用户规模预期

- **6 个月目标：** 覆盖 5000+ 用户，推出 3–5 个实战项目；
- **12 个月目标：** 覆盖 2 万+ 用户，增值服务付费率 3–5%；
- **24 个月目标：** 覆盖 5 万+ 用户，建立企业合作通道。

---

## 3. 用户需求与场景分析

### 3.1 核心用户画像

| 维度 | 描述 |
|---|---|
| **年龄** | 20–28 岁 |
| **学历** | 本科、研究生、转行职场人 |
| **城市** | 一二线城市为主 |
| **专业/背景** | 计算机、数学、电子、文科转行等 |
| **付费意愿** | 为能提升就业竞争力的内容付费，单次预算 100–1000 元 |
| **核心诉求** | 系统学 AI、做出项目、找到工作 |

### 3.2 典型使用场景

**场景一：能力测评**
> 小李想转 AI 产品经理，但不知道自己差多少。他打开小程序做 AI 能力测评，系统告诉他：产品思维 OK，但 Prompt Engineering 和 AI 项目经验不足，推荐学习路径。

**场景二：个性化学习**
> 小王选择"大模型应用开发"方向，系统根据他的 Python 基础和可用时间，生成 4 周学习计划，每天推送课程和练习。

**场景三：项目实战**
> 小张学习到第 3 周，解锁"AI 客服机器人"项目，按照平台拆解的步骤完成项目，AI 自动检查代码并给出改进建议。

**场景四：岗位对标**
> 小陈上传目标岗位 JD，AI 分析后告诉他：还差数据分析能力和一个推荐系统项目。于是他针对性补齐。

### 3.3 需求优先级排序（MoSCoW）

| 优先级 | 需求 | 说明 |
|---|---|---|
| **Must have** | AI 能力测评 | 核心入口，决定个性化推荐 |
| **Must have** | 学习路径规划 | 核心差异化功能 |
| **Must have** | 基础课程体系 | 免费内容基础 |
| **Must have** | 项目实战模块 | 核心付费转化点 |
| **Should have** | AI 答疑/作业批改 | 提升学习效率 |
| **Should have** | 岗位能力对标 | 增强就业导向 |
| **Could have** | 学习社区/打卡 | 提升留存 |
| **Won't have** | 完整直播系统 | MVP 不做重投入 |

---

## 4. 产品方案设计

### 4.1 小程序核心功能模块

#### 4.1.1 AI 能力测评

- 多维度测评：编程基础、数学基础、AI 认知、项目经验、目标岗位等；
- 生成能力雷达图和水平报告；
- 推荐最适合的学习方向和起点。

#### 4.1.2 个性化学习路径

- 根据测评结果、目标方向、可用时间生成学习计划；
- 动态调整：根据学习进度和练习表现优化路径；
- 支持多方向：算法工程师、AI 产品经理、AIGC 应用、数据分析等。

#### 4.1.3 基础课程学习

- 短视频/图文课程，每节 5–15 分钟；
- 课后知识点练习；
- 学习进度追踪与提醒。

#### 4.1.4 项目实战营

- 按方向设计实战项目，如：
  - AI 客服机器人
  - 推荐系统 Demo
  - AIGC 内容生成工具
  - AI 数据分析看板
- 项目步骤拆解、代码模板、AI 检查与反馈。

#### 4.1.5 AI 答疑与作业批改

- 结合当前学习内容的 AI 答疑；
- 代码/项目作业自动检查；
- 给出改进建议，不是直接给答案。

#### 4.1.6 岗位能力对标

- 用户上传目标岗位 JD；
- AI 分析岗位能力要求；
- 对比用户当前能力，生成差距报告和补齐建议。

#### 4.1.7 学习社区与打卡

- 学习小组、每日打卡；
- 项目作品展示；
- 问答讨论区。

### 4.2 MVP 版本功能清单

| 功能 | Must/Should/Could | 说明 |
|---|---|---|
| 微信登录与用户信息 | Must | 基础功能 |
| AI 能力测评 | Must | 核心入口 |
| 学习路径生成 | Must | 核心差异化 |
| 基础课程学习 | Must | 免费内容 |
| 课后练习 | Must | 巩固学习 |
| 项目实战模块 | Must | 核心转化 |
| AI 答疑 | Should | 提升体验 |
| 作业 AI 批改 | Should | 可二期完善 |
| 岗位能力对标 | Should | 增强就业导向 |
| 学习社区 | Could | 可二期做 |
| 直播系统 | Won't | MVP 不做 |

### 4.3 核心用户流程

```
用户注册/登录
    ↓
填写背景信息（专业、年级、目标）
    ↓
AI 能力测评
    ↓
生成能力报告 + 推荐学习方向
    ↓
选择学习方向 → 生成个性化学习路径
    ↓
学习基础课程 → 完成课后练习
    ↓
解锁实战项目 → 按步骤完成项目
    ↓
AI 检查项目 → 生成作品集
    ↓
岗位能力对标 → 针对性补齐
    ↓
使用求职服务（简历优化、内推）
```

### 4.4 小程序页面架构

```
首页
├── AI 能力测评入口
├── 推荐学习方向
├── 今日学习任务
├── 热门实战项目
├── 学习社区精选

学习页
├── 我的学习路径
├── 课程列表
├── 练习题目
├── 学习进度

项目页
├── 项目列表
├── 项目详情
├── 项目步骤
├── 代码提交/检查
├── 项目作品

求职页
├── 岗位能力对标
├── 简历优化
├── 面试题库
├── 求职指南

我的
├── 能力报告
├── 作品集
├── 学习记录
├── 订单/会员
├── 消息通知
```

### 4.5 企业端（B 端 Web 后台）

企业端是整个"培训—就业"闭环的关键环节，承载 B 端变现能力。MVP 阶段做极简版，二期完善。

#### 4.5.1 企业端功能架构

```
企业端 Web 后台
├── 首页仪表盘
│   ├── 岗位数据概览（曝光量、投递量、转化率）
│   ├── 今日新增简历
│   └── 消息通知
│
├── 岗位管理
│   ├── 发布岗位（填写 JD、薪资、地点、要求）
│   ├── 岗位列表（编辑/暂停/删除）
│   └── 岗位数据分析
│
├── 人才管理
│   ├── 投递简历列表
│   ├── 简历详情（含作品集）
│   ├── 简历筛选（按方向、技能、学历）
│   ├── 简历收藏
│   └── 面试邀请
│
├── 人才搜索
│   ├── 按技能标签搜索
│   ├── 按项目经验搜索
│   └── 查看学生能力画像
│
├── 账户与付费
│   ├── 企业信息管理
│   ├── 套餐购买（岗位发布包/会员）
│   ├── 消费记录
│   └── 发票管理
│
└── 数据报表
    ├── 招聘效果分析
    ├── 候选人画像分析
    └── 导出报表
```

#### 4.5.2 企业端核心流程

```
企业注册/认证
    │
    ▼
完善企业信息（名称、Logo、简介）
    │
    ▼
选择套餐（免费试用 / 付费套餐）
    │
    ▼
发布岗位
  ├── 填写岗位信息
  ├── 选择技能标签
  └── 设置薪资范围
    │
    ▼
接收投递简历
  ├── 查看简历 + 作品集 + 能力报告
  ├── 筛选/收藏
  └── 发送面试邀请
    │
    ▼
查看数据报表 → 优化岗位 → 持续招聘
```

#### 4.5.3 企业端 MVP 功能清单

| 功能 | MVP 优先级 | 说明 |
|---|---|---|
| 企业注册与认证 | Must | 营业执照认证，确保真实性 |
| 岗位发布与管理 | Must | 核心功能 |
| 简历查看与筛选 | Must | 核心功能 |
| 面试邀请 | Should | 站内通知 + 微信模板消息 |
| 人才搜索 | Should | 主动搜索符合条件的学生 |
| 数据看板 | Should | 简单版即可 |
| 套餐购买 | Should | MVP 可先免费，验证需求 |
| 企业定制培训 | Won't | 二期 |
| 人才能力认证 | Won't | 二期 |

#### 4.5.4 企业端收费模式

| 收费项 | 定价 | 说明 |
|---|---|---|
| 免费试用 | 0 元 | 首次注册可发布 3 个岗位，查看 50 份简历 |
| 岗位发布包 | 999 元/月 | 不限量发布，不限量查看简历 |
| 简历推荐 | 50–200 元/份 | 按成功入职收费 |
| 企业年度会员 | 9999 元/年 | 无限发布 + 优先推荐 + 数据报告 + 定制服务 |
| 企业定制培训 | 面议 | 按企业需求定制课程和项目 |

---

### 4.6 运营后台（平台管理 Web 后台）

运营后台是平台方自己使用的管理工具，用于内容管理、用户管理、数据监控和业务运营。**MVP 阶段必须做精简版**，否则课程无法上线、订单无法处理。

#### 4.6.1 运营后台功能架构

```
运营后台 Web
├── 首页仪表盘
│   ├── 核心指标大屏（DAU、新增、付费、收入）
│   ├── 实时数据（在线人数、AI 调用量）
│   └── 异常告警
│
├── 用户管理
│   ├── 用户列表（搜索、筛选、导出）
│   ├── 用户详情（画像、学习记录、订单记录）
│   ├── 用户标签管理
│   └── 封禁/解封
│
├── 内容管理
│   ├── 课程管理（增删改查、上下架）
│   ├── 章节管理（视频/图文上传、排序）
│   ├── 练习题管理（题库维护、答案解析）
│   ├── 项目模板管理（步骤配置、测试用例）
│   └── 内容审核（用户发布内容审核）
│
├── 测评管理
│   ├── 题库管理（题目增删改查）
│   ├── 评分规则配置
│   ├── 测评结果查看
│   └── 知识图谱维护
│
├── 订单管理
│   ├── 订单列表（搜索、筛选）
│   ├── 退款处理
│   ├── 发票管理
│   └── 财务对账
│
├── 企业审核
│   ├── 企业认证审核
│   ├── 岗位信息审核
│   └── 企业黑名单
│
├── 消息推送
│   ├── 模板消息管理
│   ├── 推送任务创建
│   ├── 推送记录
│   └── 推送效果统计
│
├── 社区管理
│   ├── 帖子审核
│   ├── 举报处理
│   └── 精选/推荐
│
├── 数据报表
│   ├── 用户增长报表
│   ├── 收入报表
│   ├── 内容消费报表
│   ├── AI 调用量报表
│   └── 自定义报表
│
└── 系统设置
    ├── 角色权限管理
    ├── 操作日志
    ├── 系统参数配置
    └── 模型开关（切换 LLM 提供商）
```

#### 4.6.2 运营后台 MVP 功能清单

| 功能 | MVP 优先级 | 说明 |
|---|---|---|
| 仪表盘 | Must | 核心数据一眼看清 |
| 用户管理 | Must | 至少能查看和搜索 |
| 课程管理 | Must | 没有它内容无法上线 |
| 项目模板管理 | Must | 没有它项目无法上线 |
| 订单管理 | Must | 退款、对账必须处理 |
| 企业审核 | Should | 有种子企业时就需要 |
| 练习题管理 | Should | 可先用 Excel 导入 |
| 测评管理 | Should | 可二期完善 |
| 消息推送 | Could | 可先用微信后台 |
| 社区管理 | Could | MVP 先不做社区 |
| 财务对账 | Could | 可先手动对账 |

#### 4.6.3 运营后台角色权限

| 角色 | 权限范围 |
|---|---|
| **超级管理员** | 全部权限，包括系统设置 |
| **内容运营** | 课程管理、项目管理、练习题管理、测评管理 |
| **用户运营** | 用户管理、消息推送、社区管理 |
| **商务运营** | 企业审核、订单管理 |
| **数据分析** | 数据报表（只读） |

---

### 4.7 导师端（二期规划）

当项目营规模扩大，需要引入真人导师进行人工答疑和作业精细批改时，启用导师端。MVP 阶段不做，此处仅做规划。

#### 4.7.1 导师端功能架构

```
导师端（小程序 / Web）
├── 首页
│   ├── 待批改作业数
│   ├── 待回答问题数
│   └── 收益概览
│
├── 学员管理
│   ├── 学员列表（按项目营分组）
│   ├── 学员学习进度
│   └── 学员能力画像
│
├── 作业批改
│   ├── 待批改列表
│   ├── 代码查看 + AI 初评结果
│   ├── 人工评语 + 打分
│   └── 批改历史
│
├── 答疑互动
│   ├── 待回答列表
│   ├── 对话界面
│   └── 回答模板
│
├── 项目营管理
│   ├── 我的项目营
│   ├── 学员名单
│   ├── 开班/结营管理
│   └── 学习报告
│
└── 收益管理
    ├── 批改收益
    ├── 答疑收益
    └── 提现管理
```

#### 4.7.2 导师端收费模式（平台与导师分成）

| 服务 | 用户付费 | 平台分成 | 导师分成 |
|---|---|---|---|
| 作业精细批改 | 49–99 元/次 | 30% | 70% |
| 1v1 答疑 | 29–99 元/次 | 30% | 70% |
| 项目营带班 | 299–499 元/期 | 40% | 60% |

---

### 4.8 四端全景总览

| 端口 | 使用对象 | 形态 | 核心功能 | MVP 做不做 |
|---|---|---|---|---|
| **学生端（C 端）** | 学生/求职者 | 微信小程序 | 测评、学习、项目实战、求职 | ✅ 必须做 |
| **企业端（B 端）** | 企业 HR/用人部门 | Web 后台 | 发布岗位、查看人才、数据报表 | ✅ 极简版 |
| **运营后台** | 平台运营人员 | Web 后台 | 内容管理、用户管理、数据看板、订单 | ✅ 精简版 |
| **导师端** | 兼职导师/助教 | Web/小程序 | 批改作业、答疑、管理项目营 | ❌ MVP 不做 |

---

## 5. 内容体系规划

### 5.1 课程体系

按方向划分学习路径：

| 方向 | 适合人群 | 基础课程 | 实战项目 |
|---|---|---|---|
| **AI 算法工程师** | 有编程/数学基础 | Python、机器学习、深度学习、大模型基础 | 图像分类、推荐系统、大模型微调 |
| **AI 产品经理** | 有产品基础 | AI 产品方法论、Prompt Engineering、AI 应用案例 | AI 功能设计、PRD 撰写、用户调研 |
| **AIGC 应用人才** | 创意/运营背景 | AIGC 工具使用、提示词工程、内容创作 | AI 海报生成、AI 短视频、AI 文案助手 |
| **数据分析工程师** | 想转数据方向 | SQL、Python 数据分析、可视化、统计学 | 销售数据分析、用户行为分析看板 |
| **AI 应用开发** | 有前后端基础 | 大模型 API、向量数据库、RAG、Agent | AI 客服机器人、知识库问答系统 |

### 5.2 实战项目设计

MVP 阶段精选 3–5 个项目，突出"小而完整、可展示"：

1. **AI 客服机器人**
   - 技术点：大模型 API、Prompt Engineering、知识库
   - 产出：一个可运行的对话机器人

2. **推荐系统 Demo**
   - 技术点：协同过滤、Python、数据分析
   - 产出：电影/商品推荐系统

3. **AI 数据分析看板**
   - 技术点：Pandas、可视化、Streamlit
   - 产出：交互式数据看板

4. **AIGC 内容生成器**
   - 技术点：提示词工程、API 调用
   - 产出：文案/海报生成工具

5. **AI 产品经理项目**
   - 技术点：需求分析、PRD、原型设计
   - 产出：一份完整 AI 产品方案

### 5.3 内容生产机制

- **MVP 阶段：** 由运营团队整理公开优质资源，编写项目模板和练习题目；
- **课程制作：** 采用短视频+图文形式，降低制作成本；
- **项目更新：** 每月新增 1 个实战项目；
- **AI 内容生成辅助：** 利用 AI 生成课程大纲、练习题、项目说明，人工审核。

### 5.4 师资/专家资源合作

- 初期不签约全职讲师，采用兼职/顾问模式；
- 邀请 AI 从业者录制短视频经验分享；
- 与高校 AI 社团、实验室建立内容合作关系。

---

## 6. 商业模式与盈利预测

### 6.1 收入来源

| 收入类型 | 来源端 | 说明 | MVP 阶段优先级 |
|---|---|---|---|---|
| **实战项目营** | C 端 | 单个项目实战营收费，含项目指导、批改、作品集 | 高 |
| **会员订阅** | C 端 | 月度/年度会员，解锁全部课程、项目、AI 批改 | 高 |
| **AI 增值服务** | C 端 | 简历优化、岗位能力对标、模拟面试、1v1 答疑 | 中 |
| **企业岗位发布** | B 端 | 企业发布岗位按条或包月收费 | 中 |
| **企业简历推荐** | B 端 | 按成功推荐简历或入职收费 | 中 |
| **企业会员** | B 端 | 企业年度会员，含无限发布+优先推荐+数据报告 | 低（二期） |
| **企业定制培训** | B 端 | 为企业定制 AI 培训课程和项目 | 低（二期） |
| **证书认证** | C 端 | 完成学习路径后获得平台认证证书 | 低 |

### 6.2 定价策略

| 服务 | 面向 | 定价 | 说明 |
|---|---|---|---|---|
| 基础课程 | C 端 | 免费 | 获客和建立信任 |
| 单个项目营 | C 端 | 199–499 元 | 核心付费产品 |
| 月度会员 | C 端 | 49–99 元/月 | 解锁全部内容和 AI 服务 |
| 年度会员 | C 端 | 399–699 元/年 | 提高用户 LTV |
| 简历/作品集优化 | C 端 | 99–299 元/次 | 求职增值服务 |
| 1v1 职业咨询 | C 端 | 199–499 元/次 | 高客单价服务 |
| 企业岗位发布包 | B 端 | 999 元/月 | 不限量发布岗位 |
| 企业简历推荐 | B 端 | 50–200 元/份 | 按入职效果收费 |
| 企业年度会员 | B 端 | 9999 元/年 | 无限发布+优先推荐+数据报告 |

### 6.3 收入预测模型

| 时间 | 注册用户 | 付费用户 | 月收入（估算） |
|---|---|---|---|
| 第 6 个月 | 5000 人 | 150–250 人 | 1–2 万元 |
| 第 12 个月 | 2 万人 | 800–1200 人 | 5–8 万元 |
| 第 24 个月 | 5 万人 | 2500–4000 人 | 15–25 万元 |

### 6.4 盈亏平衡分析

- **固定成本（月度）：** 团队工资 + 服务器 + 第三方服务 ≈ 6–8 万元/月
- **盈亏平衡点：** 月收入达到 8 万元左右，预计在第 10–14 个月实现
- **关键假设：** 付费转化率 3–5%，单用户年付费 300–500 元

---

## 7. 运营与增长策略

### 7.1 冷启动方案

**第一步：种子内容**
- 先上线 1–2 个完整学习方向的基础课程；
- 推出 1 个免费实战项目，降低用户体验门槛。

**第二步：种子用户获取**
- 在 5–10 所高校 AI 社团、就业群推广；
- 在知乎、小红书、B 站发布 AI 学习干货和项目案例；
- 通过"免费 AI 能力测评"作为钩子吸引用户。

**第三步：口碑传播**
- 鼓励用户分享学习报告和项目作品；
- 建立学习打卡社群，营造学习氛围。

### 7.2 裂变与传播机制

1. **测评报告分享：** 用户完成测评后生成能力雷达图海报；
2. **项目作品展示：** 完成项目后生成作品集页面，支持分享；
3. **邀请奖励：** 邀请好友注册，双方获得项目营优惠券；
4. **高校大使计划：** 招募校园大使，按拉新数量奖励。

### 7.3 用户留存与转化策略

- **新用户 7 日流程：** 注册 → 测评 → 生成路径 → 开始学习 → 完成第一节 → 解锁练习；
- **消息触达：** 通过微信服务消息推送学习提醒、项目解锁通知；
- **社群运营：** 按方向建立学习群，定期答疑和打卡；
- **转化设计：** 免费课程中自然引导至实战项目营。

### 7.4 关键运营指标

| 环节 | 指标 | 目标（MVP 阶段） |
|---|---|---|
| 获取 | 新增用户成本（CAC） | < 5 元 |
| 激活 | 完成测评率 | > 50% |
| 留存 | 7 日留存率 | > 25% |
| 转化 | 项目营购买率 | > 5% |
| 收入 | 月度付费用户数 | 持续增长 |
| 推荐 | NPS | > 30 |

---

## 8. 技术实现方案

### 8.1 整体架构概览

#### 8.1.1 架构图

```
┌─────────────────────────────────────────────────────────────────┐
│                         客户端层（四端）                          │
│  ┌──────────────┐ ┌──────────────┐ ┌──────────────┐            │
│  │ 微信小程序    │ │ 企业端 Web   │ │ 运营后台 Web  │            │
│  │  (学生端)    │ │  (B 端后台)  │ │  (管理后台)  │            │
│  └──────┬───────┘ └──────┬───────┘ └──────┬───────┘            │
│         │                │                │                     │
│         └────────────────┼────────────────┘                     │
│                          │ HTTPS + WebSocket                    │
└──────────────────────────┼──────────────────────────────────────┘
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                         API 网关层                                │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 鉴权拦截  │ │ 限流控制  │ │ 日志采集  │ │ 路由分发  │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        业务服务层                                  │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐        │
│  │用户服务 │ │课程服务 │ │项目服务 │ │测评服务 │ │订单服务 │        │
│  └────────┘ └────────┘ └────────┘ └────────┘ └────────┘        │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐                  │
│  │社区服务 │ │推荐服务 │ │通知服务 │ │企业服务 │                  │
│  └────────┘ └────────┘ └────────┘ └────────┘                  │
│  ┌──────────┐                                                    │
│  │运营管理服务│ ← 内容管理、企业审核、数据报表、系统配置             │
│  └──────────┘                                                    │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        AI 服务层                                   │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │ 能力测评  │ │ 学习路径  │ │ AI 答疑   │ │ 作业批改  │           │
│  │  引擎    │ │  生成器   │ │  引擎    │ │  引擎    │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
│  ┌──────────┐ ┌──────────┐                                      │
│  │ 岗位对标  │ │ 内容生成  │                                      │
│  │  引擎    │ │  助手    │                                      │
│  └──────────┘ └──────────┘                                      │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        LLM 网关层                                 │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │DeepSeek  │ │GPT-4o    │ │Claude    │ │Qwen      │           │
│  │Coder V2  │ │mini      │ │3.5       │ │Coder     │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
└──────────────────────────┬──────────────────────────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                        数据层                                     │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐           │
│  │PostgreSQL│ │  Redis   │ │ 腾讯COS  │ │Elasticsearch│         │
│  │  主库    │ │  缓存    │ │ 文件存储  │ │  搜索     │           │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘           │
└─────────────────────────────────────────────────────────────────┘
```

#### 8.1.2 分层设计原则

| 层级 | 职责 | 关键原则 |
|---|---|---|
| **客户端** | UI 渲染、用户交互、本地缓存 | 不包含业务逻辑，仅做数据展示与用户输入 |
| **API 网关** | 统一入口、鉴权、限流、日志 | 无状态，所有请求经过网关 |
| **业务服务** | 核心业务逻辑 | 服务间解耦，通过 API 或消息队列通信 |
| **AI 服务** | AI 能力封装 | 独立部署，可横向扩展 |
| **LLM 网关** | 多模型统一调度 | 屏蔽底层模型差异，支持动态切换 |
| **数据层** | 持久化存储与缓存 | 读写分离，核心数据多副本 |

### 8.2 前端架构（微信小程序）

#### 8.2.1 技术选型

| 技术 | 版本 | 选型理由 |
|---|---|---|
| **Taro** | 3.6+ | 一套代码编译多端，React 语法，生态成熟 |
| **React** | 18.x | 组件化开发，状态管理灵活 |
| **TypeScript** | 5.x | 类型安全，减少运行时错误 |
| **Zustand** | 4.x | 轻量级状态管理（比 Redux 轻，比 Context 强） |
| **Taro UI / NutUI** | 最新 | 小程序 UI 组件库，减少样式开发 |

#### 8.2.2 目录结构

```
src/
├── app.tsx                    # 小程序入口
├── app.config.ts              # 全局配置
├── app.scss                   # 全局样式
│
├── pages/                     # 页面
│   ├── index/                 # 首页
│   │   ├── index.tsx
│   │   ├── index.config.ts
│   │   └── index.scss
│   ├── assessment/            # AI 能力测评
│   │   ├── index.tsx          # 测评入口
│   │   ├── quiz.tsx           # 答题页
│   │   └── result.tsx         # 测评结果
│   ├── learning/              # 学习模块
│   │   ├── path.tsx           # 学习路径
│   │   ├── course.tsx         # 课程详情
│   │   ├── player.tsx         # 视频/图文播放
│   │   └── exercise.tsx       # 课后练习
│   ├── project/               # 项目实战
│   │   ├── list.tsx           # 项目列表
│   │   ├── detail.tsx         # 项目详情
│   │   ├── step.tsx           # 项目步骤
│   │   └── submit.tsx         # 代码提交
│   ├── career/                # 求职模块
│   │   ├── match.tsx          # 岗位能力对标
│   │   └── resume.tsx         # 简历优化
│   └── mine/                  # 我的
│       ├── index.tsx
│       ├── report.tsx         # 能力报告
│       └── portfolio.tsx      # 作品集
│
├── components/                # 公共组件
│   ├── CodeEditor/            # 代码编辑器（WebView 内嵌 Monaco）
│   ├── RadarChart/            # 能力雷达图
│   ├── ProgressBar/           # 学习进度条
│   ├── AIChat/                # AI 答疑对话组件
│   └── ProjectCard/           # 项目卡片
│
├── stores/                    # 状态管理（Zustand）
│   ├── userStore.ts           # 用户信息
│   ├── learningStore.ts       # 学习状态
│   └── assessmentStore.ts     # 测评状态
│
├── services/                  # API 请求封装
│   ├── api.ts                 # 基础请求封装（含 token 刷新）
│   ├── user.ts                # 用户相关 API
│   ├── assessment.ts          # 测评相关 API
│   ├── course.ts              # 课程相关 API
│   ├── project.ts             # 项目相关 API
│   └── ai.ts                  # AI 服务相关 API
│
├── hooks/                     # 自定义 Hooks
│   ├── useRequest.ts          # 请求封装（loading/error/data）
│   ├── useAuth.ts             # 登录鉴权
│   └── useWebSocket.ts        # WebSocket 连接（AI 答疑流式）
│
├── utils/                     # 工具函数
│   ├── storage.ts             # 本地缓存封装
│   ├── format.ts              # 格式化工具
│   └── constants.ts           # 常量定义
│
└── types/                     # TypeScript 类型定义
    ├── user.ts
    ├── course.ts
    └── project.ts
```

#### 8.2.3 核心交互设计

**代码编辑器方案：**
- 使用 `<web-view>` 组件嵌入 Monaco Editor（轻量版）
- 支持 Python、JavaScript 语法高亮
- 代码通过 `postMessage` 与小程序通信

**AI 答疑组件：**
- 使用 WebSocket 实现 SSE 流式输出
- 支持 Markdown 渲染（代码块、公式、表格）
- 对话历史本地缓存

**视频播放：**
- 使用微信原生 `<video>` 组件
- 记录播放进度，支持断点续播
- 关键节点插入练习题

#### 8.2.4 性能优化措施

| 优化项 | 方案 |
|---|---|
| 首屏加载 | 分包加载，首页独立包 < 500KB |
| 图片优化 | WebP 格式，CDN 加速，懒加载 |
| 长列表 | 虚拟列表（复用 DOM 节点） |
| 接口缓存 | SWR 策略（先返回缓存，再更新） |
| 预加载 | 页面跳转前预请求数据 |
| 骨架屏 | 首屏关键模块使用骨架屏占位 |

### 8.3 后端架构

#### 8.3.1 技术选型

| 技术 | 版本 | 选型理由 |
|---|---|---|
| **Python FastAPI** | 0.100+ | 异步高性能，AI 生态好，类型提示完善 |
| **SQLAlchemy 2.0** | 2.0+ | ORM，异步支持，迁移工具完善 |
| **Alembic** | 最新 | 数据库版本迁移管理 |
| **Celery** | 5.x | 异步任务队列（AI 调用、通知推送） |
| **Redis** | 7.x | 缓存 + 消息队列 + 会话管理 |
| **Pydantic** | 2.x | 数据校验，与 FastAPI 深度集成 |

#### 8.3.2 目录结构

```
backend/
├── app/
│   ├── main.py                  # FastAPI 应用入口
│   ├── config.py                # 配置管理（环境变量）
│   ├── dependencies.py          # 依赖注入（DB、Redis、Auth）
│   │
│   ├── api/                     # 路由层（Controller）
│   │   ├── v1/
│   │   │   ├── __init__.py
│   │   │   ├── router.py        # 路由汇总
│   │   │   ├── auth.py          # 登录/注册
│   │   │   ├── user.py          # 用户信息
│   │   │   ├── assessment.py    # 测评
│   │   │   ├── course.py        # 课程
│   │   │   ├── project.py       # 项目
│   │   │   ├── community.py     # 社区
│   │   │   ├── order.py         # 订单
│   │   │   ├── enterprise.py    # 企业端
│   │   │   ├── admin.py         # 运营后台
│   │   │   └── ai.py            # AI 服务
│   │   └── deps.py              # 路由层依赖
│   │
│   ├── schemas/                 # Pydantic 请求/响应模型
│   │   ├── user.py
│   │   ├── course.py
│   │   └── ...
│   │
│   ├── models/                  # 数据库模型（ORM）
│   │   ├── base.py              # 基础模型
│   │   ├── user.py
│   │   ├── course.py
│   │   ├── project.py
│   │   ├── assessment.py
│   │   ├── enterprise.py        # 企业/岗位模型
│   │   ├── application.py       # 投递记录模型
│   │   └── order.py
│   │
│   ├── services/                # 业务逻辑层
│   │   ├── user_service.py
│   │   ├── assessment_service.py
│   │   ├── learning_service.py
│   │   ├── project_service.py
│   │   ├── recommendation_service.py
│   │   ├── enterprise_service.py   # 企业端业务逻辑
│   │   ├── admin_service.py        # 运营后台业务逻辑
│   │   └── order_service.py
│   │
│   ├── ai/                      # AI 服务层
│   │   ├── __init__.py
│   │   ├── llm_gateway.py       # 多模型网关
│   │   ├── assessment_engine.py # 测评引擎
│   │   ├── learning_path.py     # 学习路径生成
│   │   ├── qa_engine.py         # 答疑引擎
│   │   ├── code_review.py       # 作业批改
│   │   └── job_matcher.py       # 岗位对标
│   │
│   ├── tasks/                   # Celery 异步任务
│   │   ├── celery_app.py        # Celery 配置
│   │   ├── notification.py      # 通知推送
│   │   ├── ai_tasks.py          # AI 异步调用
│   │   └── analytics.py         # 数据分析
│   │
│   ├── core/                    # 核心设施
│   │   ├── security.py          # JWT + 加密
│   │   ├── database.py          # 数据库连接
│   │   ├── redis.py             # Redis 连接
│   │   └── logging.py           # 日志配置
│   │
│   └── utils/                   # 工具函数
│       ├── pagination.py
│       ├── validators.py
│       └── helpers.py
│
├── alembic/                     # 数据库迁移
│   ├── versions/
│   └── env.py
│
├── tests/                       # 测试
│   ├── conftest.py
│   ├── test_api/
│   └── test_services/
│
├── requirements.txt
├── Dockerfile
└── docker-compose.yml
```

#### 8.3.3 API 设计规范

**RESTful 风格，统一响应格式：**

```json
{
  "code": 0,
  "message": "success",
  "data": { ... },
  "meta": {
    "page": 1,
    "page_size": 20,
    "total": 100
  }
}
```

**核心 API 端点：**

| 模块 | 端点 | 方法 | 说明 |
|---|---|---|---|
| 认证 | `/api/v1/auth/login` | POST | 微信登录 |
| 用户 | `/api/v1/users/me` | GET | 获取当前用户信息 |
| 用户 | `/api/v1/users/me` | PUT | 更新用户信息 |
| 测评 | `/api/v1/assessments` | POST | 开始测评 |
| 测评 | `/api/v1/assessments/{id}/submit` | POST | 提交测评答案 |
| 测评 | `/api/v1/assessments/{id}/result` | GET | 获取测评结果 |
| 课程 | `/api/v1/courses` | GET | 课程列表 |
| 课程 | `/api/v1/courses/{id}` | GET | 课程详情 |
| 课程 | `/api/v1/learning-paths` | POST | 生成学习路径 |
| 课程 | `/api/v1/courses/{id}/progress` | PUT | 更新学习进度 |
| 项目 | `/api/v1/projects` | GET | 项目列表 |
| 项目 | `/api/v1/projects/{id}/submit` | POST | 提交项目代码 |
| AI | `/api/v1/ai/chat` | POST | AI 答疑 |
| AI | `/api/v1/ai/chat/stream` | WS | AI 答疑（流式） |
| AI | `/api/v1/ai/code-review` | POST | 代码批改 |
| AI | `/api/v1/ai/job-match` | POST | 岗位对标 |
| 订单 | `/api/v1/orders` | POST | 创建订单 |
| 订单 | `/api/v1/orders/{id}/pay` | POST | 支付回调 |
| 企业 | `/api/v1/enterprises` | POST | 企业注册 |
| 企业 | `/api/v1/enterprises/me` | GET | 获取企业信息 |
| 企业 | `/api/v1/enterprises/jobs` | GET | 企业岗位列表 |
| 企业 | `/api/v1/enterprises/jobs` | POST | 发布岗位 |
| 企业 | `/api/v1/enterprises/jobs/{id}` | PUT | 编辑岗位 |
| 企业 | `/api/v1/enterprises/resumes` | GET | 收到的简历列表 |
| 企业 | `/api/v1/enterprises/resumes/{id}` | GET | 简历详情 |
| 企业 | `/api/v1/enterprises/interviews` | POST | 发送面试邀请 |
| 运营 | `/api/v1/admin/courses` | POST | 创建课程 |
| 运营 | `/api/v1/admin/courses/{id}` | PUT | 编辑课程 |
| 运营 | `/api/v1/admin/projects` | POST | 创建项目模板 |
| 运营 | `/api/v1/admin/users` | GET | 用户列表 |
| 运营 | `/api/v1/admin/orders` | GET | 订单列表 |
| 运营 | `/api/v1/admin/orders/{id}/refund` | POST | 退款处理 |
| 运营 | `/api/v1/admin/enterprises/audit` | POST | 企业审核 |
| 运营 | `/api/v1/admin/stats/dashboard` | GET | 数据仪表盘 |

### 8.4 AI 服务层详细设计

#### 8.4.1 LLM 网关设计

LLM 网关的核心职责是**屏蔽底层模型差异，提供统一的调用接口，支持动态路由和降级**。

```python
# 网关核心接口设计（伪代码）
class LLMGateway:
    """多模型统一调度网关"""

    def __init__(self):
        self.providers = {
            "deepseek-coder": DeepSeekProvider(),
            "gpt-4o-mini": OpenAIProvider(),
            "claude-3.5": AnthropicProvider(),
            "qwen-coder": QwenProvider(),
        }

    async def chat(
        self,
        messages: List[Message],
        scene: str,           # 调用场景：assessment / qa / code_review / job_match
        stream: bool = False,
        fallback: bool = True # 是否允许降级
    ) -> ChatResponse:
        # 1. 根据场景选择最优模型
        provider = self.route(scene)

        # 2. 注入场景专属 System Prompt
        messages = self.inject_system_prompt(messages, scene)

        # 3. 调用模型，失败时降级
        try:
            return await provider.chat(messages, stream)
        except Exception:
            if fallback:
                fallback_provider = self.get_fallback(scene)
                return await fallback_provider.chat(messages, stream)
            raise
```

**场景路由策略：**

| 调用场景 | 首选模型 | 降级模型 | 路由原因 |
|---|---|---|---|
| 能力测评 | GPT-4o mini | Qwen Coder | 需要结构化输出，mini 性价比高 |
| 学习路径生成 | Claude 3.5 | GPT-4o mini | 长上下文，需理解复杂逻辑 |
| AI 答疑 | Claude 3.5 | DeepSeek Coder | 长上下文，解释清晰 |
| 代码批改 | DeepSeek Coder | GPT-4o mini | 代码能力强，成本低 |
| 岗位对标 | GPT-4o mini | Qwen Coder | 结构化输出，解析 JD |
| 题目生成 | DeepSeek Coder | GPT-4o mini | 代码生成强，成本低 |

#### 8.4.2 能力测评引擎

**流程：**

```
用户开始测评
    ↓
1. 基础信息收集（专业、年级、目标方向）
    ↓
2. 自适应出题（根据前面回答调整难度）
    ├── 编程基础题（Python 语法、数据结构）
    ├── 数学基础题（线代、概率统计）
    ├── AI 认知题（ML/DL 基础概念）
    └── 场景题（根据目标方向动态生成）
    ↓
3. 答案收集 → LLM 批量评分
    ↓
4. 生成能力雷达图 + 水平报告 + 学习建议
    ↓
5. 推荐学习方向 + 起点
```

**核心数据模型：**

```python
class AssessmentResult(BaseModel):
    """测评结果"""
    dimensions: Dict[str, float]  # 维度得分 {"python": 75, "math": 60, "ml": 40}
    level: str                     # 综合等级：beginner / intermediate / advanced
    strengths: List[str]           # 优势
    weaknesses: List[str]          # 薄弱点
    recommended_paths: List[Path]  # 推荐学习路径
    estimated_time: int            # 预估学习时长（小时）
```

#### 8.4.3 学习路径生成器

**核心逻辑：**

1. 输入：测评结果 + 目标方向 + 每周可用时间
2. 从课程知识图谱中匹配节点
3. 按依赖关系排序，生成周计划
4. 动态调整：根据学习进度和练习正确率，调整后续路径

**知识图谱结构（简化）：**

```
Python 基础 → 数据分析 → 机器学习 → 深度学习 → 大模型应用
    ↓            ↓          ↓           ↓           ↓
  [必学]      [必学]     [选学]      [选学]      [高阶]

AI 产品思维 → Prompt Eng → AI 工具链 → AI 产品设计 → 项目实战
    ↓            ↓           ↓           ↓           ↓
  [必学]      [必学]     [必学]      [必学]      [核心]
```

#### 8.4.4 AI 答疑引擎

**设计思路：** 不是通用 AI，而是**绑定当前学习上下文的智能答疑**。

```python
class QAEngine:
    """AI 答疑引擎"""

    def build_context(
        self,
        user_id: str,
        course_id: str,
        current_chapter: str,
        learning_history: List[Progress]
    ) -> str:
        """
        构建答疑上下文，包含：
        1. 当前课程内容和章节
        2. 用户已学知识点
        3. 用户薄弱点（来自测评）
        4. 用户之前的提问历史
        """
        return f"""
        你是一个 AI 编程导师，正在辅导一位学生学习 {course_name}。
        当前章节：{current_chapter}
        学生已掌握：{mastered_topics}
        学生薄弱点：{weak_topics}
        回答要求：
        - 解释要结合课程内容，不要超出学生当前水平过多
        - 不要直接给答案，先引导思考
        - 可以给出代码示例，但要解释每一行
        """
```

#### 8.4.5 代码批改引擎

**设计思路：** 不只判断对错，而是**指出问题 + 给出改进方向 + 参考运行结果**。

```python
class CodeReviewEngine:
    """代码批改引擎"""

    async def review(
        self,
        code: str,
        project_id: str,
        step_id: str,
        language: str = "python"
    ) -> CodeReviewResult:
        """
        批改流程：
        1. 静态检查：语法、风格、常见错误
        2. 逻辑检查：是否满足项目步骤要求
        3. 运行检查（可选）：沙箱执行代码
        4. 改进建议：代码优化、最佳实践
        """
        prompt = f"""
        作为代码审查者，请检查以下 {language} 代码。

        项目要求：{step_requirements}
        评分标准：{rubric}

        请按以下格式输出：
        1. 评分：[0-100分]
        2. 功能正确性：[通过/不通过] - 具体问题
        3. 代码质量：[好/中/差] - 具体问题
        4. 优化建议：[3-5条具体建议]
        5. 知识点总结：[本次练习涉及的知识点]
        """
        response = await self.gateway.chat(prompt, scene="code_review")
        return self.parse_review(response)

class CodeReviewResult(BaseModel):
    score: int
    passed: bool
    issues: List[CodeIssue]
    suggestions: List[str]
    knowledge_points: List[str]
```

#### 8.4.6 岗位对标引擎

**设计思路：** 解析岗位 JD → 提取能力要求 → 与用户能力对比 → 生成差距报告。

```python
class JobMatcher:
    """岗位能力对标引擎"""

    async def match(
        self,
        user_id: str,
        job_description: str
    ) -> MatchResult:
        """
        对标流程：
        1. LLM 解析 JD，提取结构化能力要求
        2. 从数据库读取用户能力画像
        3. 逐项对比，计算匹配度
        4. 生成差距报告 + 补齐建议
        """
        # Step 1: 解析 JD
        requirements = await self.parse_jd(job_description)

        # Step 2: 获取用户能力
        user_skills = await self.get_user_skills(user_id)

        # Step 3: 对比
        gaps = []
        for req in requirements:
            user_level = user_skills.get(req.skill, 0)
            if user_level < req.required_level:
                gaps.append(SkillGap(
                    skill=req.skill,
                    current=user_level,
                    required=req.required_level,
                    courses=await self.find_courses(req.skill),
                    projects=await self.find_projects(req.skill)
                ))

        return MatchResult(
            match_score=calculate_match(requirements, user_skills),
            gaps=gaps,
            recommend_courses=get_recommendations(gaps),
            recommended_projects=get_project_recommendations(gaps)
        )
```

### 8.5 数据库设计

#### 8.5.1 核心表结构

**用户表：**

```sql
CREATE TABLE users (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    openid          VARCHAR(64) UNIQUE NOT NULL,   -- 微信 openid
    unionid         VARCHAR(64),                    -- 微信 unionid
    nickname        VARCHAR(64),
    avatar_url      VARCHAR(512),
    phone           VARCHAR(20),
    email           VARCHAR(128),
    university      VARCHAR(128),
    major           VARCHAR(128),
    grade           VARCHAR(20),                    -- 大一/大二/大三/大四/研一/...
    target_direction VARCHAR(64),                   -- 目标方向
    weekly_hours    INT DEFAULT 10,                 -- 每周可用学习时间
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

-- 索引
CREATE INDEX idx_users_openid ON users(openid);
CREATE INDEX idx_users_created_at ON users(created_at);
```

**测评记录表：**

```sql
CREATE TABLE assessments (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID REFERENCES users(id),
    direction       VARCHAR(64),                    -- 测评方向
    status          VARCHAR(20) DEFAULT 'in_progress', -- in_progress/completed
    python_score    INT,
    math_score      INT,
    ml_score        INT,
    product_score   INT,
    overall_level   VARCHAR(20),                    -- beginner/intermediate/advanced
    strengths       JSONB,                          -- ["Python基础", "逻辑思维"]
    weaknesses      JSONB,                          -- ["机器学习", "数据分析"]
    report_text     TEXT,                           -- LLM 生成的完整报告
    created_at      TIMESTAMP DEFAULT NOW(),
    completed_at    TIMESTAMP
);

CREATE INDEX idx_assessments_user ON assessments(user_id, created_at);
```

**课程表：**

```sql
CREATE TABLE courses (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title           VARCHAR(256) NOT NULL,
    description     TEXT,
    direction       VARCHAR(64),                    -- 所属方向
    category        VARCHAR(64),                    -- 课程分类
    difficulty      VARCHAR(20),                    -- beginner/intermediate/advanced
    cover_url       VARCHAR(512),
    is_free         BOOLEAN DEFAULT TRUE,
    sort_order      INT DEFAULT 0,
    status          VARCHAR(20) DEFAULT 'draft',    -- draft/published/archived
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_courses_direction ON courses(direction, sort_order);
```

**章节表：**

```sql
CREATE TABLE chapters (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    course_id       UUID REFERENCES courses(id) ON DELETE CASCADE,
    title           VARCHAR(256) NOT NULL,
    content_type    VARCHAR(20),                    -- video/article/code
    content_url     VARCHAR(512),
    content_text    TEXT,                           -- 图文内容
    duration        INT,                            -- 视频时长（秒）
    sort_order      INT DEFAULT 0,
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_chapters_course ON chapters(course_id, sort_order);
```

**练习题表：**

```sql
CREATE TABLE exercises (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    chapter_id      UUID REFERENCES chapters(id) ON DELETE CASCADE,
    question_type   VARCHAR(20),                    -- single_choice/multi_choice/code/fill
    question_text   TEXT NOT NULL,
    options         JSONB,                          -- 选择题选项
    correct_answer  TEXT,                           -- 正确答案
    explanation     TEXT,                           -- 解析
    sort_order      INT DEFAULT 0
);
```

**学习进度表：**

```sql
CREATE TABLE learning_progress (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID REFERENCES users(id),
    course_id       UUID REFERENCES courses(id),
    chapter_id      UUID REFERENCES chapters(id),
    exercise_id     UUID REFERENCES exercises(id),
    progress_type   VARCHAR(20),                    -- video_watch/exercise_submit
    progress_data   JSONB,                          -- {"watch_time": 120, "completed": true}
    score           INT,                            -- 练习得分
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_progress_user ON learning_progress(user_id, course_id);
CREATE INDEX idx_progress_created ON learning_progress(user_id, created_at);
```

**学习路径表：**

```sql
CREATE TABLE learning_paths (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID REFERENCES users(id),
    assessment_id   UUID REFERENCES assessments(id),
    direction       VARCHAR(64),
    path_data       JSONB NOT NULL,                 -- 路径结构
    status          VARCHAR(20) DEFAULT 'active',   -- active/completed/paused
    started_at      TIMESTAMP DEFAULT NOW(),
    completed_at    TIMESTAMP
);

CREATE INDEX idx_paths_user ON learning_paths(user_id, status);
```

**项目表：**

```sql
CREATE TABLE projects (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    title           VARCHAR(256) NOT NULL,
    description     TEXT,
    direction       VARCHAR(64),
    difficulty      VARCHAR(20),
    cover_url       VARCHAR(512),
    is_free         BOOLEAN DEFAULT FALSE,
    price           INT DEFAULT 0,                  -- 单位：分
    steps           JSONB NOT NULL,                 -- 项目步骤
    template_code   TEXT,                           -- 初始模板代码
    test_cases      JSONB,                          -- 测试用例
    sort_order      INT DEFAULT 0,
    status          VARCHAR(20) DEFAULT 'draft',
    created_at      TIMESTAMP DEFAULT NOW()
);
```

**项目提交表：**

```sql
CREATE TABLE project_submissions (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID REFERENCES users(id),
    project_id      UUID REFERENCES projects(id),
    step_id         VARCHAR(64),
    code            TEXT NOT NULL,
    files           JSONB,                          -- 多文件提交
    review_result   JSONB,                          -- AI 批改结果
    score           INT,
    status          VARCHAR(20) DEFAULT 'pending',  -- pending/reviewed/passed
    submitted_at    TIMESTAMP DEFAULT NOW(),
    reviewed_at     TIMESTAMP
);

CREATE INDEX idx_submissions_user ON project_submissions(user_id, project_id);
```

**订单表：**

```sql
CREATE TABLE orders (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID REFERENCES users(id),
    order_type      VARCHAR(32),                    -- project_camp/membership/resume
    product_id      UUID,                           -- 关联产品 ID
    product_name    VARCHAR(256),
    amount          INT NOT NULL,                   -- 单位：分
    status          VARCHAR(20) DEFAULT 'pending',  -- pending/paid/refunded/cancelled
    wx_transaction_id VARCHAR(64),                  -- 微信支付交易号
    paid_at         TIMESTAMP,
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_orders_user ON orders(user_id, created_at);
```

**企业表：**

```sql
CREATE TABLE enterprises (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    name            VARCHAR(256) NOT NULL,           -- 企业名称
    logo_url        VARCHAR(512),
    industry        VARCHAR(64),                     -- 行业
    scale           VARCHAR(32),                     -- 规模
    description     TEXT,                            -- 企业简介
    contact_name    VARCHAR(64),                     -- 联系人
    contact_phone   VARCHAR(20),
    license_url     VARCHAR(512),                    -- 营业执照
    status          VARCHAR(20) DEFAULT 'pending',   -- pending/approved/rejected
    account_balance INT DEFAULT 0,                   -- 账户余额（分）
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_enterprises_status ON enterprises(status);
```

**岗位表：**

```sql
CREATE TABLE jobs (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    enterprise_id   UUID REFERENCES enterprises(id),
    title           VARCHAR(256) NOT NULL,
    description     TEXT,                            -- 岗位描述
    requirements    TEXT,                            -- 岗位要求
    direction       VARCHAR(64),                     -- 技术方向
    skills          JSONB,                           -- ["Python", "ML", "DeepSeek"]
    city            VARCHAR(64),
    salary_min      INT,
    salary_max      INT,
    job_type        VARCHAR(32),                     -- intern/fulltime
    headcount       INT DEFAULT 1,                   -- 招聘人数
    view_count      INT DEFAULT 0,                   -- 浏览量
    apply_count     INT DEFAULT 0,                   -- 投递量
    status          VARCHAR(20) DEFAULT 'published', -- published/paused/closed
    created_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_jobs_enterprise ON jobs(enterprise_id, status);
CREATE INDEX idx_jobs_direction ON jobs(direction, status);
```

**投递记录表：**

```sql
CREATE TABLE job_applications (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    job_id          UUID REFERENCES jobs(id),
    user_id         UUID REFERENCES users(id),
    status          VARCHAR(20) DEFAULT 'pending',   -- pending/viewed/interview/offer/hired/rejected
    resume_data     JSONB,                           -- 投递时的简历快照
    interview_time  TIMESTAMP,
    remark          TEXT,                            -- 企业备注
    applied_at      TIMESTAMP DEFAULT NOW(),
    updated_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_applications_job ON job_applications(job_id);
CREATE INDEX idx_applications_user ON job_applications(user_id);
```

**企业订单表：**

```sql
CREATE TABLE enterprise_orders (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    enterprise_id   UUID REFERENCES enterprises(id),
    order_type      VARCHAR(32),                    -- job_package/membership/resume_recommend
    product_name    VARCHAR(256),
    amount          INT NOT NULL,                   -- 单位：分
    quantity        INT DEFAULT 1,
    status          VARCHAR(20) DEFAULT 'pending',  -- pending/paid/refunded
    wx_transaction_id VARCHAR(64),
    paid_at         TIMESTAMP,
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_ent_orders_enterprise ON enterprise_orders(enterprise_id, created_at);
```

**运营操作日志表：**

```sql
CREATE TABLE admin_logs (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    admin_id        UUID REFERENCES users(id),
    action          VARCHAR(64),                    -- create_course/edit_user/refund_order
    target_type     VARCHAR(64),                    -- course/user/order/enterprise
    target_id       UUID,
    detail          JSONB,                          -- 操作详情
    ip_address      VARCHAR(45),
    created_at      TIMESTAMP DEFAULT NOW()
);

CREATE INDEX idx_admin_logs_admin ON admin_logs(admin_id, created_at);
CREATE INDEX idx_admin_logs_target ON admin_logs(target_type, target_id);
```

#### 8.5.2 缓存策略

| 数据 | 缓存时间 | 更新策略 |
|---|---|---|
| 用户信息 | 30 分钟 | 用户操作时主动失效 |
| 课程列表 | 1 小时 | 内容变更时失效 |
| 课程详情 | 2 小时 | 内容变更时失效 |
| 测评结果 | 永久 | 重新测评时覆盖 |
| AI 对话历史 | 24 小时 | LRU 淘汰 |
| 热门项目 | 1 小时 | 定时刷新 |
| 学习路径 | 用户 session 期间 | 进度变更时失效 |

### 8.6 部署与运维

#### 8.6.1 部署架构

```
┌──────────────────────────────────────────────────┐
│                    Nginx 反向代理                    │
│              (SSL 终止 + 静态资源 + 负载均衡)          │
└────────┬──────────┬──────────┬───────────────────┘
         │          │          │
         ▼          ▼          ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│ FastAPI  │ │ FastAPI  │ │ Celery   │
│ Worker 1 │ │ Worker 2 │ │ Worker   │
└────┬─────┘ └────┬─────┘ └────┬─────┘
     │            │            │
     └────────────┼────────────┘
                  │
     ┌────────────┼────────────┐
     ▼            ▼            ▼
┌──────────┐ ┌──────────┐ ┌──────────┐
│PostgreSQL│ │  Redis   │ │ 腾讯COS  │
│  (主)    │ │  (缓存)   │ │ (文件)   │
└──────────┘ └──────────┘ └──────────┘
```

#### 8.6.2 Docker Compose 配置

```yaml
version: '3.8'

services:
  # 后端 API 服务
  api:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:pass@db:5432/ailearn
      - REDIS_URL=redis://redis:6379/0
      - LLM_API_KEY=${LLM_API_KEY}
    depends_on:
      - db
      - redis
    restart: always
    deploy:
      replicas: 2

  # Celery 异步任务
  celery:
    build: ./backend
    command: celery -A app.tasks.celery_app worker -l info
    environment:
      - DATABASE_URL=postgresql+asyncpg://user:pass@db:5432/ailearn
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - db
      - redis
    restart: always

  # PostgreSQL 数据库
  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: user
      POSTGRES_PASSWORD: pass
      POSTGRES_DB: ailearn
    volumes:
      - pgdata:/var/lib/postgresql/data
    restart: always

  # Redis 缓存
  redis:
    image: redis:7-alpine
    volumes:
      - redisdata:/data
    restart: always

  # Nginx 反向代理
  nginx:
    image: nginx:alpine
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
      - ./ssl:/etc/nginx/ssl
    depends_on:
      - api
    restart: always

volumes:
  pgdata:
  redisdata:
```

#### 8.6.3 云服务选型建议

| 服务 | 方案 | 月成本估算 |
|---|---|---|
| 云服务器 | 腾讯云轻量 2C4G × 2 | 200–400 元 |
| 数据库 | 云数据库 PostgreSQL（1C2G） | 300–500 元 |
| Redis | 云 Redis（1G） | 200–300 元 |
| 对象存储 | 腾讯云 COS | 50–100 元 |
| CDN | 腾讯云 CDN | 50–100 元 |
| SSL 证书 | 免费 Let's Encrypt | 0 元 |
| **合计** | | **约 800–1400 元/月** |

### 8.7 安全设计

#### 8.7.1 认证与鉴权

- **微信登录：** 小程序端调用 `wx.login()` 获取 code → 后端换 openid → 生成 JWT
- **JWT Token：** access_token（2 小时）+ refresh_token（7 天）
- **Token 刷新：** 前端拦截器自动刷新，用户无感
- **API 鉴权：** 所有业务 API 需携带 Authorization Header

#### 8.7.2 数据安全

| 措施 | 说明 |
|---|---|
| 传输加密 | 全站 HTTPS，API 强制 TLS 1.2+ |
| 敏感数据加密 | 手机号、邮箱 AES-256 加密存储 |
| 代码隔离 | 用户提交代码在沙箱环境执行，限制网络和文件系统 |
| SQL 注入防护 | ORM 参数化查询，禁止拼接 SQL |
| XSS 防护 | 输入输出严格过滤，CSP 头配置 |

#### 8.7.3 接口安全

| 措施 | 说明 |
|---|---|
| 请求签名 | 关键接口（支付、订单）增加请求签名校验 |
| 频率限制 | 同一用户/IP 限制请求频率（如 100次/分钟） |
| 幂等性 | 支付、订单创建接口保证幂等，防止重复提交 |
| 参数校验 | Pydantic 严格校验所有输入参数 |

### 8.8 监控与日志

#### 8.8.1 日志方案

```json
{
    "timestamp": "2026-08-05T10:30:00Z",
    "level": "INFO",
    "service": "api",
    "request_id": "uuid",
    "user_id": "uuid",
    "path": "/api/v1/ai/chat",
    "method": "POST",
    "status_code": 200,
    "duration_ms": 1234,
    "llm_provider": "deepseek-coder",
    "llm_tokens": 500
}
```

- 使用 structlog 结构化日志
- 日志输出到 stdout，由容器日志收集器统一处理
- AI 调用专项日志：记录每次 LLM 调用的模型、耗时、Token 数

#### 8.8.2 监控指标

| 类型 | 指标 | 告警阈值 |
|---|---|---|
| 系统 | CPU / 内存 / 磁盘 | > 80% |
| API | 响应时间 P99 | > 3s |
| API | 错误率 | > 1% |
| AI | LLM 调用成功率 | < 95% |
| AI | LLM 平均响应时间 | > 10s |
| 业务 | 日活用户 | 异常下降 > 30% |
| 业务 | 付费转化率 | 异常下降 > 50% |

#### 8.8.3 推荐工具

| 工具 | 用途 |
|---|---|
| Prometheus + Grafana | 系统监控 + 可视化 |
| Sentry | 错误追踪 |
| 微信后台 | 小程序数据分析 |
| 自定义埋点 | 业务指标看板 |

### 8.9 开发规范

#### 8.9.1 分支管理

```
main           ← 生产环境（只允许 merge，不允许直接 push）
  └── develop  ← 开发环境
       ├── feature/assessment-v1  ← 功能分支
       ├── feature/course-player
       └── hotfix/xxx             ← 紧急修复
```

#### 8.9.2 代码规范

- **Python：** 遵循 PEP 8，使用 Black + Ruff 格式化
- **TypeScript：** 使用 ESLint + Prettier
- **提交信息：** 遵循 Conventional Commits（`feat:` / `fix:` / `docs:` / `refactor:`）
- **Code Review：** 所有合并到 develop 需要至少 1 人 Review

#### 8.9.3 测试要求

| 层级 | 覆盖率要求 | 工具 |
|---|---|---|
| 单元测试 | > 70% | pytest |
| API 测试 | 核心接口 100% | pytest + httpx |
| 前端组件测试 | 核心组件 | Jest + Taro 测试工具 |

---

## 9. 项目计划与里程碑

### 9.1 项目阶段划分

| 阶段 | 时间 | 关键任务 | 产出 |
|---|---|---|---|
| **调研期** | 第 1–2 周 | 用户调研、竞品分析、内容规划 | 调研报告、需求文档 |
| **设计期** | 第 3–4 周 | 产品原型、UI 设计、技术方案 | 原型图、设计稿、技术方案 |
| **开发期** | 第 5–10 周 | 小程序开发、AI 接入、内容录入 | 可测试版本 |
| **测试期** | 第 11–12 周 | 功能测试、用户体验测试 | 测试报告 |
| **上线期** | 第 13 周 | 灰度发布、种子用户推广 | 正式上线 |

### 9.2 里程碑与验收标准

| 里程碑 | 时间 | 验收标准 |
|---|---|---|
| M1 需求确认 | 第 2 周 | 需求文档评审通过 |
| M2 设计完成 | 第 4 周 | 原型/UI 评审通过 |
| M3 开发完成 | 第 10 周 | 核心功能可跑通 |
| M4 测试通过 | 第 12 周 | 无 P0/P1 级 Bug |
| M5 正式上线 | 第 13 周 | 上线并通过内部验收 |

### 9.3 上线与推广节奏

- **第 1 个月：** 上线 1 个学习方向 + 1 个实战项目，获取 1000 名种子用户；
- **第 2 个月：** 优化体验，新增 1–2 个学习方向；
- **第 3 个月：** 推出首个付费项目营，验证商业模式。

---

## 10. 团队与资源需求

### 10.1 项目团队配置

| 角色 | 人数 | 职责 | 投入方式 |
|---|---|---|---|
| 产品经理 | 1 人 | 需求分析、产品设计、项目管理 | 全职 |
| UI 设计师 | 1 人 | 小程序设计 | 全职或兼职 |
| 前端开发 | 1 人 | 小程序前端开发 | 全职 |
| 后端开发 | 1 人 | API 开发、数据库设计 | 全职 |
| AI/算法工程师 | 0.5 人 | AI 测评、答疑、批改接入 | 兼职 |
| 内容运营 | 1 人 | 课程内容、项目设计、社区运营 | 全职 |
| 增长运营 | 0.5 人 | 用户增长、推广、转化 | 兼职 |

### 10.2 预算明细

| 项目 | 金额（万元） | 说明 |
|---|---|---|
| 人员成本 | 25–30 | 3 个月核心团队人力 |
| 设计与开发外包 | 5–8 | 若有部分工作外包 |
| 服务器与云服务 | 2–3 | 云服务器、存储、CDN（含 1 年） |
| AI API 调用 | 2–3 | 大模型 API 费用（含 1 年预算） |
| 内容制作 | 3–5 | 课程录制、项目模板制作 |
| 推广与运营 | 3–5 | 高校推广、活动、社群 |
| 第三方服务 | 2–3 | 支付、短信、视频服务等 |
| 预留资金 | 3–5 | 应对不确定性 |
| **合计** | **45–50** | |

### 10.3 需要公司支持的事项

1. **决策授权：** 确认项目立项，授权 50 万预算；
2. **团队支持：** 调配 1 名产品经理 + 1 名开发加入项目组；
3. **渠道支持：** 若公司有公众号/社群/高校资源，给予初期推广支持；
4. **时间窗口：** 给予 3 个月验证期，期间不过度考核短期收入；
5. **AI 资源：** 提供大模型 API 账号或相关技术支持。

---

## 11. 风险分析与应对措施

### 11.1 市场风险

| 风险 | 应对措施 |
|---|---|
| 竞品快速跟进 | 快速建立内容壁垒和用户口碑 |
| 用户需求不及预期 | MVP 阶段小范围验证，及时调整方向 |
| 付费转化低 | 优化项目营设计和转化路径 |

### 11.2 技术风险

| 风险 | 应对措施 |
|---|---|
| 开发延期 | 严格控制 MVP 范围，采用敏捷开发 |
| AI 效果不稳定 | 多模型网关 + 降级策略，逐步调优 Prompt |
| 系统稳定性不足 | 选择成熟云服务商，Docker 容器化部署，做好监控 |

### 11.3 内容风险

| 风险 | 应对措施 |
|---|---|
| 内容质量参差不齐 | 建立内容审核机制 |
| 版权问题 | 优先使用原创内容或已授权内容 |
| 课程效果难衡量 | 设置学习反馈和项目验收标准 |

### 11.4 运营风险

| 风险 | 应对措施 |
|---|---|
| 用户增长慢 | 聚焦高校社团和微信群，做透小圈子 |
| 社群活跃度低 | 设计打卡、积分、排行榜等机制 |
| 项目营交付重 | 控制每期人数，逐步标准化服务流程 |

### 11.5 法律合规风险

| 风险 | 应对措施 |
|---|---|
| 教育资质问题 | MVP 阶段以"职业技能培训信息服务"定位，规避学历教育资质 |
| 数据隐私 | 遵守《个人信息保护法》，学习数据加密存储 |
| 付费课程合规 | 明确服务协议，避免夸大就业承诺 |

---

## 12. 投资回报与决策建议

### 12.1 预期投入与回报

| 指标 | 数值 |
|---|---|
| MVP 投入 | 45–50 万元 |
| 验证周期 | 3–6 个月 |
| 盈亏平衡预期 | 10–14 个月 |
| 24 个月收入预期 | 200–400 万元 |
| 24 个月用户预期 | 5 万+ 用户，10+ 实战项目 |

### 12.2 成功关键假设

1. 高校学生和职场新人对 AI 技能培训有真实需求；
2. 用户愿意为"项目实战 + 就业导向"的增值服务付费；
3. 团队能在 3 个月内完成 MVP 并上线；
4. 公司能够提供初期的内容和推广支持。

### 12.3 推荐决策

**建议立项，启动 MVP 开发。**

理由：
1. AI 培训市场空间真实且持续增长；
2. "测评—学习—实战—就业"闭环有清晰差异化；
3. 50 万预算可控，风险可承受；
4. 能够快速验证商业模式，为后续融资或扩张提供数据支撑。

### 12.4 下一步行动计划

若立项通过，未来 2 周内完成：

| 序号 | 事项 | 负责人 | 完成时间 |
|---|---|---|---|
| 1 | 组建项目核心团队 | 产品负责人 | 3 天内 |
| 2 | 完成 30 名目标用户访谈 | 产品经理 | 1 周内 |
| 3 | 完成竞品深度分析 | 产品经理 | 1 周内 |
| 4 | 输出详细产品需求文档 | 产品经理 | 2 周内 |
| 5 | 确认技术方案与开发排期 | 技术负责人 | 2 周内 |
| 6 | 准备立项汇报 PPT | 产品经理 | 2 周内 |

---

## 附录：核心指标看板（建议）

| 指标类型 | 指标 | 频率 |
|---|---|---|
| 用户增长 | 日新增、注册转化率 | 每日 |
| 用户活跃 | DAU、7 日留存 | 每周 |
| 学习转化 | 测评完成率、路径生成率、课程完成率 | 每日 |
| 收入 | 项目营购买数、会员数、ARPU | 每月 |
| 产品体验 | NPS、核心功能完成率 | 每月 |
| 系统健康 | API 响应时间、错误率、LLM 调用成功率 | 实时 |

---

**文档版本：** V2.0（含完整技术架构）  
**编写人：** [你的名字]  
**日期：** 2026 年 8 月 5 日