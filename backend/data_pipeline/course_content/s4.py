"""阶段四 · 多 Agent 系统 — 小节内容扩充。"""

SECTION_ENRICH: dict[str, dict] = {
    # ================= s4-multi-agent 多 Agent 架构与协作 =================
    "s4-ma-c1-s1": {
        "content": "多 Agent 协作的第一组模式是『指挥式』：监督者与层级。Supervisor（监督者调度）：一个主管 Agent 作为中心，接收用户任务、拆解成子任务、分配给多个执行 Agent，再汇总结果。主管掌握全局，执行 Agent 专注执行——结构简单清晰，适合『任务可分、各司其职』的场景，也是实际工程中最常用的多 Agent 形态。Hierarchical（层级式）：多层 Agent 树状结构——顶层 Agent 拆任务给中层，中层再拆给底层，逐级分解。比 Supervisor 更复杂，适合任务层级深、粒度差别大的场景（如公司级的复杂项目）。\n\n两者的共同点是『有中心、有分工』：中心负责协调，执行者负责干活。设计的要点：中心的『任务分解与分配』能力决定整个系统的上限——分解不清，执行 Agent 拿到的是糊涂任务；分配不当，会造成某些 Agent 过载、某些闲置。多数场景从 Supervisor（一层）起步，只有任务确实深到一层管不过来，才升级到层级式。",
        "case": "一个软件开发团队用 Supervisor 模式：主管 Agent 收到『做一个小工具』的需求，拆解为『需求分析』『技术方案』『写代码』三个子任务，分别派给需求 Agent、架构 Agent、代码 Agent；各执行 Agent 完成后，主管汇总成完整交付。相比一个 Agent 从头做到尾，分工让每个 Agent 专注自己的角色，产出更专业。当项目大而复杂时，再升级为『技术总监→各模块负责人→具体实现』的层级式。",
    },
    "s4-ma-c1-s2": {
        "content": "多 Agent 协作的另外三组模式是『平级式』与『集中讨论式』。Pipeline（流水线）：多个 Agent 按固定顺序处理，前一个的输出是后一个的输入——像工厂流水线，适合流程固定、阶段清晰的场景（数据清洗→分析→成文）。Network（网状）：Agent 自由通信、平等协作，谁需要谁就联系谁——最灵活也最难控制，适合高度动态的场景，但工程上风险大（协调困难、易失控）。Group Chat（群聊）：所有 Agent 在一个共享对话中共同讨论、达成决策——适合需要集体协商的场景（方案评审、头脑风暴）。\n\n选择模式的关键，是匹配任务的『确定性』：流程固定选 Pipeline（高效可控）、需要灵活协调选 Network（但慎用）、需要集体决策选 Group Chat、有明确主从关系选 Supervisor/Hierarchical。工程建议：能从简单模式（Supervisor、Pipeline）满足需求，就别上复杂模式（Network）——复杂模式的控制成本极高。",
        "case": "一个『数据周报』系统用 Pipeline 模式：数据清洗 Agent → 分析 Agent → 周报生成 Agent，三个 Agent 串成一条流水线，前一步产出后一步直接消费，流程稳定、效率高。而一个『营销方案头脑风暴』则用 Group Chat：市场 Agent、文案 Agent、设计 Agent 在共享对话里互相补充、质询、打磨方案，最终共同产出。不同的任务形态，匹配不同的协作模式。",
    },
    "s4-ma-c2-s1": {
        "content": "Agent 之间的通信方式，决定了信息传递的效率与可靠性。四种常用方式：消息传递——Agent 之间发消息（结构化 JSON 最佳），字段明确、可校验，是工程上最推荐的默认方式；共享黑板——多个 Agent 往一个共享空间里写数据、读数据（类似公告板），适合并行写入、汇总的协作，但要注意写冲突与脏读；结论式交接——前一个 Agent 只把『结论/要点』传给下一个，而非原始全量数据，能显著减少信息量、避免上下文爆炸；结构化 JSON——无论用哪种方式，消息内容用 JSON 承载（字段清晰、可解析、可校验），是沟通的『格式规范』。\n\n通信设计的两个原则：信息最少化——只传下一个 Agent 真正需要的，别把整段上下文都丢过去（这是上下文爆炸的根源）；格式结构化——用 JSON 而非自由文本，让接收方能可靠解析。通信方式选得对，多 Agent 才『聊得明白』；选得糙，Agent 之间互相看不懂、信息丢失，系统就乱套。",
        "case": "Supervisor 把『用户退款请求』分给售后 Agent，采用结构化 JSON 消息：`{task:'handle_refund', order_id:'2024-001', user_id:'U88', reason:'商品破损'}`——售后 Agent 拿到的是干净、完整、可解析的任务描述，直接执行。如果改用自由文本『帮用户处理一下这个退款』，缺少订单号等关键信息，售后 Agent 还得回头问，效率大降。结构化 JSON 让 Agent 之间的沟通『精准对接』。",
    },
    "s4-ma-c2-s2": {
        "content": "任务分配（把总任务拆给多个 Agent）是协调层的关键能力，两种主要方式：静态分解——预先定义好分工（谁负责哪类任务），运行时直接按规则派发，简单可控、可预测，适合流程固定的场景；动态分解——由协调 Agent 运行时根据任务内容临时拆分，灵活但不可预测、成本高，适合复杂多变的任务。配合的经典模式是 Map-Reduce：把一个大任务拆成多个可并行的子任务（Map，分头执行），再汇总所有子任务的结果（Reduce，归并输出）——适合『可并行、可汇总』的任务（批量处理、多路检索）。\n\n分配时的工程要点：子任务的粒度要合适（太大不好并行、太小协调成本高）；子任务之间尽量独立（减少互相等待与依赖）；分配结果要可追踪（谁领了哪个任务、进度如何）。任务分配的质量，直接影响多 Agent 的『负载均衡』与整体吞吐——分配得好，Agent 各安其位；分配得乱，系统低效甚至死锁。",
        "case": "一个批量翻译任务用 Map-Reduce：Map 阶段把 100 篇文档拆成 10 批，分给 10 个翻译 Agent 并行处理；Reduce 阶段由汇总 Agent 收集各批结果、统一格式、合并输出。10 个 Agent 并行，整体耗时比单 Agent 串行缩短约 8 倍。而一个『用户意图未知』的客服任务则用动态分解：Supervisor 先判断问题类型，再临时决定分给售前还是售后——任务形态不同，分配策略也相应变化。",
    },
    "s4-ma-c3-s1": {
        "content": "角色设计是多 Agent 系统的地基——角色划得清，协作才顺畅。设计的两条黄金原则：职责不重叠——每个角色只负责一类职责，避免两个 Agent 抢同一个活（如『运营 Agent』和『客服 Agent』都回消息，就会重复处理）；接口清晰——每个角色的输入、输出、触发条件要明确，让其他 Agent 知道『什么时候把任务交给它、它会产出什么』。典型角色划分：产品经理 Agent（要什么）、架构师 Agent（怎么做）、程序员 Agent（实现）、测试 Agent（验证）、运营 Agent（推广）。\n\n角色设计与『通信方式』『任务分配』紧密相关：角色的输入输出格式（通信）要在设计时就定好；角色间的任务流转（分配）要遵循角色的职责边界。一个务实的检查方法：画出角色 × 职责矩阵，看每个职责是否恰好归属一个角色、每个角色是否有清晰边界——有重叠就合并或明确边界，有空洞就补角色。角色清晰，是后续一切协调的前提。",
        "case": "一个内容生产系统设计角色：选题 Agent（负责发现热点）、写稿 Agent（负责成文）、编辑 Agent（负责审核润色）。三个角色职责分明、接口清晰——选题 Agent 输出选题列表，写稿 Agent 消费选题产出稿件，编辑 Agent 审核稿件。对比一个『什么都能干』的内容 Agent，分工后的每个环节质量更高，且某个环节想优化（如换个更好的编辑模型）只动那个角色，互不影响。",
    },
    "s4-ma-c3-s2": {
        "content": "『什么时候该上多 Agent』有四个明确的触发信号，对应四种『单 Agent 撑不住』的具体场景。一是工具太多难选择——工具数量多到单 Agent 频繁选错，按工具类别分给不同 Agent 能显著改善（每个 Agent 只面对自己类别的少量工具）。二是角色冲突——任务需要冲突的立场（同一个 Agent 既要写方案又要审方案，自己审自己难免护短），拆成『生成 Agent + 独立审查 Agent』。三是任务可并行——多个独立子任务串行做太慢，拆给多个 Agent 并行。四是需要独立审查——关键产出需要一个不参与生成的 Agent 独立检查，消除『自己检查自己』的盲区。\n\n判断的要点：四个信号都是『单 Agent 有具体的、可指出的不足』，而不是『觉得多 Agent 更高级』。如果一个信号都不占，上多 Agent 只会收获成本上升与复杂度爆炸。把『单 Agent 哪里不够』想清楚，是触发多 Agent 的正当理由——这也是学习路线反复强调的务实原则。",
        "case": "一个客服 Agent 的工具从 5 个涨到 25 个后，模型频繁选错工具（调用退款的工具去查物流）。这正是『工具太多难选择』的信号——于是按业务拆成『订单 Agent』『售后 Agent』『知识库 Agent』三个执行者，由 Supervisor 先判断问题归属再派发，每个执行者只面对少量工具，选错率大幅下降。而工具少的时候强拆，反而制造了『谁处理』的争议——触发信号没到，就不要拆。",
    },
    "s4-ma-c4-s1": {
        "content": "多 Agent 的代价是真实且沉重的，上之前必须算清这笔账。五大代价：一是成本上升——每个 Agent 每次决策都是一次 LLM 调用，N 个 Agent 就是 N 倍基础成本，多轮协作更是成倍增长；二是延迟变高——串行调用时，Agent 之间要来回传递消息，整体响应时间显著拉长；三是调试困难——错误可能级联放大（一个 Agent 的偏差传给下一个被放大），定位问题要沿着调用链逐一排查；四是上下文爆炸——Agent 之间传递过多信息会撑爆各自的上下文窗口，影响判断质量；五是责任不清晰——Agent 多了，出了错不知道该怪谁，也容易『三个和尚没水喝』（每个 Agent 都以为别人会做）。\n\n理解这五大代价，不是为了劝退，而是为了『算账』——多 Agent 的收益（分工质量、并行效率、独立审查）必须能盖过这些代价，才值得上。工程上，每个多 Agent 项目都应该预估成本、设计延迟预算、建立调试手段（日志链路）、控制信息传递量、明确每个 Agent 的 owner。",
        "case": "一个团队把单 Agent 改造成 4 个 Agent 协作后，单次任务成本翻了 3 倍、响应时间从 3 秒变 15 秒，还出现『需求 Agent 的误解被代码 Agent 放大成完全跑偏的功能』的级联错误。复盘发现：任务根本不需要多 Agent——触发信号一个都不占。这个反面案例的教训：多 Agent 的代价是必然的，收益是可选的——没有触发信号，代价就是净亏损。",
    },
    "s4-ma-c4-s2": {
        "content": "多 Agent 的务实原则只有一句话：能用单 Agent 解决的，不要上多 Agent。展开是三个决策习惯：第一，先问『单 Agent 哪里不够』——把单 Agent 的具体不足列出来（选错工具？角色冲突？太慢？没独立审查？），再决定多 Agent 是不是解法；第二，从最小形态起步——即使确实需要多 Agent，也从『Supervisor + 2 个执行者』这样的小规模开始，验证价值后再扩展，而不是一上来就画一张复杂的多 Agent 架构图；第三，持续验证收益——上线后对比单 Agent 方案，确认成本增加换来了可量化的质量/效率提升，没有就回退。\n\n这套原则的本质是『工程上的克制』：多 Agent 是一个高成本的先进工具，用它的前提是有明确的、可量化的理由。能把『不该用多 Agent』的时候想明白，比能用多 Agent 更体现架构成熟度——因为大多数业务，单 Agent + 好的工具设计已经足够。",
        "case": "一个团队在给客服系统加新功能时，产品同学提议『再加一个专门处理投诉的 Agent』。工程师先做了『单 Agent 哪里不够』的分析：现有单 Agent 处理投诉的成功率已到 90%，且未发现选错工具、角色冲突等具体不足——结论是不需要新增 Agent。他们改用优化投诉相关的工具描述，成功率提到 93%，成本几乎为零。这就是务实原则的实践：不是不能上，而是不上也能解决，就先不上。",
    },
    "s4-ma-c5-s1": {
        "content": "Supervisor 最小 Demo 是阶段四的动手实践：用 LangGraph 实现『一个主管 + 两个执行 Agent』的协作。架构：主管 Agent 接收用户问题 → 判断任务归属 → 分给『检索 Agent』（查知识库）或『计算 Agent』（做计算/处理数据）→ 执行 Agent 返回结果 → 主管汇总给用户。用 LangGraph 实现时：Supervisor 是一个节点（调 LLM 做任务分发决策），两个执行 Agent 各是一个节点，用条件边把任务路由到正确的执行节点。\n\n实现要点：State 里要有『当前任务』『分配给谁』『子结果』等字段；主管的决策用结构化输出（让模型返回 {assignee, task}）便于路由；执行 Agent 的输入输出要规范化（接收什么任务、返回什么结果）。跑通这个 Demo，你就亲手实现了一个多 Agent 的最小闭环——理解了 Supervisor 模式的核心：『中心决策 + 分工执行 + 结果汇总』。",
        "case": "Demo 的实际行为：用户问『公司年假政策是什么』→ 主管判断属于知识类 → 路由到检索 Agent（查制度库）→ 返回政策条款 → 主管汇总回答；用户问『帮我算下 30 天年假还剩几天』→ 主管判断属于计算类 → 路由到计算 Agent → 返回结果。同一个入口，主管按任务类型分流给不同执行者——这就是 Supervisor 协作的直观体现。",
    },
    "s4-ma-c5-s2": {
        "content": "阶段四的定位是『了解为主、不必做完整项目』，所以这个 Demo 的验收标准不是功能完整，而是『能讲清原理』。你要能讲清楚的几件事：为什么用 Supervisor 模式（有明确主从关系、任务可分）；每个角色的职责边界（主管管什么、执行 Agent 管什么、为什么这样划分）；通信怎么设计（任务和结果用什么格式传递）；出错怎么办（执行 Agent 失败主管怎么兜底）。能把这些讲明白，这个 Demo 就完成了它的教学使命。\n\n学习路线的安排是有意为之：多 Agent 的深水区（完整的多 Agent 产品、复杂的编排）留给阶段六项目实战；阶段四的目标是建立『多 Agent 的心智模型』——知道有哪些模式、什么时候该用、代价是什么。带着这个心智进入项目阶段，你才能在真实项目里做出有分寸的多 Agent 设计，而不是盲目堆 Agent。",
        "case": "面试或复盘时讲这个 Demo 的话术：『我用 LangGraph 实现了一个 Supervisor 多 Agent Demo——主管根据问题类型把任务分给检索或计算两个执行 Agent。我理解 Supervisor 的核心是中心决策 + 分工执行，也意识到代价是多次 LLM 调用和主管的决策质量决定整体上限。这个 Demo 让我建立了对多 Agent 协作模式的直观理解。』讲清原理、说透取舍，比堆功能更能体现理解深度。",
    },

    # ================= s4-metagpt-autogen MetaGPT 与 AutoGen 框架 =================
    "s4-fw-c1-s1": {
        "content": "MetaGPT 的核心思想，是『把软件公司的运转模拟进多 Agent 系统』。一个软件项目要经过需求、设计、开发、测试多个环节，MetaGPT 让不同角色 Agent 各司其职、像真实公司一样协作：产品经理 Agent 产出需求文档（PRD）→ 架构师 Agent 产出接口设计 → 程序员 Agent 写代码 → 测试 Agent 检查质量。关键设计是『用标准化的中间产物交接』——每个角色 Agent 的输出是结构化文档（PRD、接口文档、代码），下一个角色基于上一份文档继续，而不是自由聊天。\n\nMetaGPT 的价值：一是分工逼真——每个角色专注自己的专业产出，质量更高；二是流程可追踪——每个环节都有明确的文档产出，过程可审计；三是标准化交接——结构化文档让 Agent 之间的信息传递可靠。理解『以物（文档）为界交接、而非以话（对话）为界沟通』，你就抓住了 MetaGPT 区别于对话式多 Agent 的本质——它是『流水线 + 标准件』，不是『开讨论会』。",
        "case": "让 MetaGPT 开发一个『待办事项 App』：产品 Agent 先产出包含功能清单、用户故事的 PRD；架构 Agent 读 PRD 产出接口定义与技术方案；程序员 Agent 按接口写代码；测试 Agent 跑测试反馈问题。整个过程中，PRD 是产品到架构的『合同』、接口文档是架构到开发的『合同』——任何一环出问题，都能定位到哪份文档、哪个环节。这就是以文档为界、以流程为纲的多 Agent 协作。",
    },
    "s4-fw-c1-s2": {
        "content": "MetaGPT 的架构核心，是『角色（Role）+ 工作流（SOP）+ 结构化输出』三要素。角色：每个 Agent 被赋予一个专业角色（产品经理、架构师、程序员…），角色定义它的职责、专业知识和输出规范。工作流（SOP）：定义角色之间的协作流程——谁先谁后、谁产出什么交给谁，模拟软件公司的标准作业流程。结构化输出：强制每个角色按固定格式产出（PRD 有模板、接口有 schema），保证交接物的一致性与可解析性。\n\n这三个要素共同保证了 MetaGPT 的『确定性』：角色分工消除职责混乱，工作流固定协作顺序，结构化输出保证交接可靠。研究 MetaGPT 源码时，重点看三件事：角色如何定义（职责与输出约束）、工作流如何编排（角色顺序与交接物）、结构化输出如何约束（模板与校验）——看懂这三块，你就理解了多 Agent 框架『如何把协作变成可执行的流程』，这也是设计任何多 Agent 系统的通用方法论。",
        "case": "读 MetaGPT 源码时的观察点：Role 类里定义了 `observe/act/reflect` 等方法——角色『看』到输入、『做』自己的产出、『反思』结果；PRD 输出有结构化模板约束格式；SOP 里定义了产品→架构→开发的顺序。这些设计让『软件公司』可复现——同一个需求，跑出的流程是稳定的。理解这些源码背后的设计意图，比记住 API 重要得多。",
    },
    "s4-fw-c2-s1": {
        "content": "AutoGen（微软开源）代表了多 Agent 协作的另一条路线：对话式协作。与 MetaGPT 的『流水线 + 文档交接』不同，AutoGen 让 Agent 之间通过『对话』协作——两个或更多 Agent 互相发送消息、讨论、协商、共同解决问题。经典形态是『双 Agent』模式：一个充当『助理』（解决问题），一个充当『用户/审查者』（提出质疑、把关质量），两者反复对话，直到达成结论。群聊模式（GroupChat）则是多个 Agent 在一个共享对话里共同讨论。\n\n对话式协作的特点：灵活——Agent 之间的交互不受固定流程限制，能处理开放式问题；自然——很多协作通过来回讨论天然形成（像团队开讨论会）；但相对的，更不可控、更耗 Token。AutoGen 的设计哲学与 MetaGPT 不同：前者是『让 Agent 聊出结果』，后者是『让流程产出结果』。理解两条路线的分野，你在选型时就知道：流程明确选 MetaGPT 式，需要协商讨论选 AutoGen 式。",
        "case": "用 AutoGen 的『助理 + 审查者』双 Agent 打磨一份方案：助理 Agent 生成初稿 → 审查者 Agent 提出『预算没考虑、风险没分析』 → 助理补充修订 → 审查者再检查… 几轮对话后方案趋于完善。这种『生成—质疑—修订』的循环，用固定流水线很难自然表达，而对话式协作天生擅长。这正是 AutoGen 的适用场景——需要迭代协商、而非固定流程。",
    },
    "s4-fw-c2-s2": {
        "content": "AutoGen 的灵活性体现在『自定义与扩展』：你可以定义自己的 Agent（指定角色、系统提示词、可用工具）、设计 Agent 之间的对话流程（谁和谁聊、怎么终止）、定制群聊的协调规则（怎么仲裁、怎么结束）。扩展的维度：角色自定义——把 Agent 定义成任何角色（面试官、律师、审核员），只需配置系统提示词与工具；流程定制——规定对话轮次上限、终止条件（何时算『讨论完了』）；工具接入——给特定 Agent 挂上工具，让它不仅能说还能做。\n\nAutoGen 的代价也源于灵活性：越灵活越难控制。对话可能跑偏、无限循环、或者讨论了半天没结论——所以自定义的『终止条件』是必须的（对话轮次上限、达成一致的条件、超时）。使用建议：先从小规模的对话模式起步（双 Agent），需要更复杂协作再上群聊；每一步自定义都配好终止条件，避免对话失控。",
        "case": "自定义一个『模拟面试 Agent』：面试官 Agent（系统提示词设定为资深后端面试官）与候选人 Agent（模拟求职者）对话，限定最多 10 轮、候选人答不出就结束。这个 Demo 展示了 AutoGen 的自定义能力——角色、对话流程、终止条件全部可控。同时它也是一个教学场景：观察两个 Agent 的对话如何迭代、何时终止，理解对话式协作的节奏与控制。",
    },
    "s4-fw-c3-s1": {
        "content": "多 Agent 框架的选型，核心是匹配『任务的协作形态』。LangGraph：图编排，把 Agent 流程画成可控状态图——适合需要精细控制、复杂分支、人工介入的场景，也是当前最灵活、最受推荐的选择（阶段三已深入）。MetaGPT：软件流程模拟，角色化分工 + 文档交接——适合『软件开发』这类流程标准化、产出物明确的场景。AutoGen：对话式协作，Agent 之间协商讨论——适合需要迭代打磨、集体决策的场景。CrewAI：角色团队，用『crew』组织多个角色 Agent 协作——上手快、适合中小型角色化任务。\n\n选型的心智模型：先想清楚你的任务需要哪种协作形态（流程固定？协商讨论？角色分工？），再选匹配的框架——而不是先选框架再硬套。LangGraph 覆盖面最广（图能表达任意流程），所以很多场景它都是安全的默认选择；MetaGPT/AutoGen/CrewAI 则各有独特擅长的形态。选型没有绝对优劣，只有『是否匹配任务』。",
        "case": "一个标准化研发流程（需求→设计→开发→测试）用 MetaGPT 最省心——它的文档交接模式天然匹配；一个『两个专家来回打磨商业方案』的场景用 AutoGen 双 Agent 最自然；一个需要『按条件分流、人工审批、可控循环』的复杂业务 Agent 用 LangGraph。团队甚至会在不同子系统里用不同框架——选型永远是跟着任务形态走，而不是品牌偏好。",
    },
    "s4-fw-c3-s2": {
        "content": "『不要过度选型』是对多 Agent 框架最实用的提醒：多数业务单 Agent + 工具就够了，多 Agent 框架是特定场景的武器，别为用而用。三个清醒的认知：第一，框架不等于多 Agent——用 LangGraph 搭单 Agent 图、用 AutoGen 跑双 Agent，都是合法的用法，多 Agent 与否取决于任务需要而非框架选择；第二，先单后多——任何多 Agent 方案，都应该先评估『单 Agent + 优化工具描述/提示词』能不能解决，能解决就不用多 Agent；第三，成本意识——多 Agent 框架天然增加调用次数与复杂度，一个『单 Agent 就能 90 分』的任务，没必要用多 Agent 框架做到 92 分却花 3 倍成本。\n\n把框架当工具箱而不是目的地：工具箱里有单 Agent 的锤子、多 Agent 的电钻，你按任务需求拿工具，而不是因为买了电钻就到处钻洞。工程成熟度的标志，恰恰是能克制地选择『最简单能解决问题的方案』。",
        "case": "一个『文本分类 + 简单问答』的需求，团队为了『技术先进』上了多 Agent 框架，三个 Agent 串成流程，成本和延迟翻倍，效果与单 Agent 相当。复盘结论：这个任务单 Agent 完全能 95 分，多 Agent 只是增加了开销。而另一个『客服 + 审批 + 记忆』的复杂需求，单 Agent 明显撑不住，多 Agent（或图编排）才是对的选择。判断标准从来只有一个——任务需不需要。",
    },
    "s4-fw-c4-s1": {
        "content": "读 MetaGPT 源码，重点看它如何用工程手段实现『软件公司』的协作。三个源码观察点：一是角色约束——看 Role 类如何通过 `observe/act/reflect` 让角色『接收输入→按 SOP 产出→自我反思』，以及如何用结构化模板约束每个角色的输出格式（PRD 必须有功能清单、接口文档必须有字段定义）；二是交接物设计——看角色之间传递的是什么（结构化文档而非自由文本），以及这些文档如何被下游角色解析消费；三是流程编排——看 SOP（标准作业流程）如何定义角色顺序与依赖，如何保证『产品经理先于架构师』这类先后约束。\n\n读源码的价值不是背代码，而是提取『可迁移的设计思想』：角色职责如何用结构化输出约束、协作流程如何用文档交接解耦、系统如何通过流程保证确定性。这些思想你可以直接用在自建的多 Agent 系统里——不管用不用 MetaGPT 框架。",
        "case": "从 MetaGPT 源码提取的一个可复用设计：『用结构化文档做交接物』——在每个角色的输出上定义固定 schema（如需求必须含『目标、功能列表、验收标准』），下游角色只消费符合 schema 的输入。把这个思想用到自己的客服多 Agent 系统：售后 Agent 输出统一格式的 `{order_id, issue, decision}`，知识库 Agent 只认这个格式——Agent 之间的沟通变得可靠且可解析。这就是读框架源码的收获：借它的设计思想，而非只抄它的代码。",
    },
    "s4-fw-c4-s2": {
        "content": "读 AutoGen 源码，重点看它如何实现『对话式协作』的可控循环。三个源码观察点：一是对话循环——看两个 Agent 如何在一个循环里互相发消息（`a.send(b)`、`b.reply(a)`），循环如何推进；二是终止条件——看对话如何判定结束（达成条件、轮次上限、超时），这是对话式协作『不失控』的关键；三是群聊仲裁——看 GroupChat 里多个 Agent 的发言顺序与仲裁规则（谁接话、怎么决定下一个发言者）。\n\n读 AutoGen 源码的可迁移收获：『对话必须有明确的终止协议』——因为对话式协作最怕无限聊天，AutoGen 把终止条件（max_turns、达成一致的条件）做成配置，这是所有对话式协作系统的必备工程护栏。另一个收获是消息路由——群聊怎么决定谁发言，对应你自建系统里『消息怎么路由给合适的 Agent』。带着这两个问题读源码，你会看到对话式多 Agent 不只是『让它们聊』，而是『设计好怎么开始、怎么推进、怎么结束』。",
        "case": "从 AutoGen 源码提取的可复用设计：『为对话设定终止协议』——AutoGen 用 max_turns 和『termination condition』控制对话何时结束，防止两个 Agent 无限来回。把这个设计用到自己的『方案打磨 Agent 对』：规定最多 5 轮、或双方都对结论无异议即终止。没有这个协议，打磨 Agent 可能永远改下去——终止条件是对话式协作的刹车。",
    },
    "s4-fw-c5-s1": {
        "content": "搭建一个多 Agent Demo，是验证你理解的最好方式。推荐最小任务：『写需求 → 生成方案 → 评审』三角色小任务——需求 Agent 接收一个需求描述、产出需求文档；方案 Agent 读需求、产出技术方案；评审 Agent 审方案、指出问题（可循环几轮）。用任一框架实现（LangGraph 图编排、AutoGen 对话、MetaGPT 角色都可），核心是跑通『角色分工 + 流程流转 + 结果产出』。\n\n搭建时的检查清单：角色职责是否清晰（每个 Agent 知道自己做什么）；流程是否可追踪（能打印每个 Agent 的输入输出）；交接是否结构化（需求文档、方案文档格式统一）；终止是否明确（评审通过或达到轮次上限即结束）。跑通后，打印整个流程：每个角色收到什么、产出什么、怎么交接——这一步的输出就是你对多 Agent 协作理解的最好证明。",
        "case": "Demo 跑通后的打印输出（伪）：`需求Agent <- 「做一个订单查询工具」 → 产出需求文档 {功能: 查询订单, 场景: 客服}` → `方案Agent <- 需求文档 → 产出技术方案 {架构: FastAPI+SQL, 接口: /api/order}` → `评审Agent <- 技术方案 → 意见: 缺少超时处理` → `方案Agent 修订方案 → 评审通过`。这个清晰的协作链条，就是你理解『分工、交接、评审』的直接体现。",
    },
    "s4-fw-c5-s2": {
        "content": "多 Agent Demo 的另一个必修课是成本观察：跑一个完整任务，记录它的 Token 消耗与耗时，直观感受多 Agent 的代价。观察维度：总调用次数——一个任务下来，几个 Agent 各调了几次 LLM；Token 总量——累计输入输出（尤其多 Agent 之间互相传上下文，消耗增长很快）；耗时——串行调用下整体延迟。用一个简单任务对比『单 Agent 做』与『多 Agent 做』的成本差异，往往能让你对『多 Agent 的代价』有切肤体会。\n\n成本观察的意义：一是建立成本敏感——你在设计多 Agent 时，会本能地想到『这个任务要调几次、花多少钱』；二是为决策提供依据——以后评估『要不要上多 Agent』时，你能用数据说话（多 Agent 成本 ×3，收益是否值得）；三是积累优化意识——观察成本分布，找出可以省的地方（精简消息传递、减少无谓轮次）。把成本观察养成习惯，你就从『会用多 Agent』进阶到『会算多 Agent 的账』。",
        "case": "对比数据示例：同样『生成一个活动方案』，单 Agent 完成：1 次调用、约 2000 Token、2 秒；三角色多 Agent 完成：6 次调用（每人 2 次含评审修订）、约 12000 Token、15 秒。成本是单 Agent 的 6 倍，质量提升有限（这个任务单 Agent 已够好）。看到这组数据，你会本能地判断：这个任务不值得多 Agent。这就是成本观察的训练价值——它让你自动带上『值不值』的判断。",
    },

    # ================= s4-orchestration Agent 编排与工程化陷阱 =================
    "s4-or-c1-s1": {
        "content": "Agent 编排的第一步，是把『任务需求』翻译成『可控流程』——从需求到流程的建模。方法：把任务画成一个状态机——先梳理任务的各个状态（如客服：待理解 → 查单中 → 待处理 → 已处理 / 需转人工）、状态之间的转移条件（什么情况从查单转到待处理、什么情况转到转人工）、以及哪些步骤可并行、哪些有依赖、哪里需要人工介入。画清楚这张状态图，你就有了编排的骨架。\n\n从需求到流程的关键，是识别出『分支与依赖』：分支——任务在哪些点会产生不同走向（检索有无结果、校验通不通过、审批批不批准）；依赖——哪些步骤必须在前一步完成后才能开始（先查单才能处理）。把这些分支和依赖结构化，才能用图（LangGraph）或状态机实现。编排的质量取决于流程建模的质量——流程画得糊涂，Agent 跑起来就糊涂。",
        "case": "为『客服退款』画状态机：`用户诉求 → 理解意图 → 查订单（分支：有订单→校验资格；无订单→请用户核对）→ 校验（分支：符合→提交退款；不符合→说明原因）→ 退款前 interrupt 人工确认（分支：批准→执行退款；拒绝→告知用户）→ 完成`。这张状态图把每个分支、每次人工介入都标得清清楚楚——直接对应 LangGraph 的节点与条件边，也对应了阶段三『小 G』的设计。",
    },
    "s4-or-c1-s2": {
        "content": "编排载体，是『用什么技术把你的流程设计跑起来』。三个可选层级：一是 LangGraph 图——最灵活、最推荐，节点/边/条件路由能精确表达任意流程，还天然支持记忆、人工介入、可观测（阶段三已掌握）；二是简单状态循环——用代码手写状态机（一个循环 + 状态变量 + 分支判断），适合流程简单、不想引入框架的场景；三是脚本顺序执行——对纯线性、无分支的流程，直接按顺序调用即可，最简单。\n\n选载体的原则：流程复杂度决定载体复杂度——流程简单用脚本/状态循环（避免过度设计），流程复杂、有分支/循环/人工介入用 LangGraph（框架的价值恰好在这里）。无论用哪种，『流程可读、可改、可观测』是共同要求——编排不是写死逻辑，而是让流程能看清、能调整。工程上，多数『真实业务 Agent』的流程都有分支与介入需求，所以 LangGraph 是默认选择，但永远记住『先画流程、再选载体』。",
        "case": "两个对比：一个『纯线性』任务（读文件→总结→写文件）用脚本顺序执行即可，上 LangGraph 是过度设计；一个『带分支、审批、循环』的客服流程，用 LangGraph 画图——每个分支是条件边、审批是中断点、重试是循环。同样的业务，选择不同载体，取决于流程里有没有分支与介入——这就是『按流程复杂度选载体』。",
    },
    "s4-or-c2-s1": {
        "content": "条件分支是流程设计的骨架，它让 Agent 能『按结果走不同路线』。分支的本质是判断点：在流程的某个节点，根据上一步的结果决定下一步走哪条路。典型分支：检索有无结果（有→回答，无→重查或告知）、校验是否通过（通过→继续，不通过→提示修正）、审批是否批准（批准→执行，拒绝→终止并说明）。在 LangGraph 里，分支就是条件边——一个路由函数读 State，返回走哪条路。\n\n设计分支的要点：判断条件要明确可判定（『检索结果是否为空』比『结果好不好』更可执行）；每个分支都要有落点（不能走到无路可走的死分支）；分支之间要互斥完整（覆盖所有可能情况，不遗漏）。分支不是越多越好——每个分支都增加流程复杂度和测试面，只加真正必要的分支（影响行为走向的判断点），琐碎的分支会淹没主线。",
        "case": "订单查询流程的分支设计：`查订单（分支：命中→返回订单详情；未命中→校验订单号格式（分支：格式错→提示重输；格式对→告知无此订单））`。两个分支嵌套，每个判断点都明确、每个分支都有落点。如果这里漏了『格式错』的分支，用户输错订单号时会收到莫名其妙的『无此订单』——分支设计不完整，用户体验就差一截。",
    },
    "s4-or-c2-s2": {
        "content": "循环与迭代让流程具备『自我修正』能力，但也是流程失控的高发区。循环的两种典型用途：重试——失败的步骤重跑（限流重试、工具失败重试）；回炉——不合格的产出返回重做（回答质量检查不过，回去重生成）。迭代则指多轮修正直到达标（如反思打磨几轮）。\n\n循环设计的铁律是『每个循环都必须有终止条件』：重试要有次数上限（如 3 次后放弃走兜底）；回炉要有轮次上限（如最多 2 轮）；迭代要有达标判断或最大轮数（达到质量线或轮次上限即停）。没有终止条件的循环，是生产事故的温床——一个 bug 或一次偶发失败，会触发无限循环烧光 Token。把『终止条件』当作循环的必要组成部分，而不是可选配置，是 Agent 工程化的底线之一。",
        "case": "一个回答质量检查的循环：回答生成后检查，不合格回炉重生成。设计时带上终止条件：`最多重生成 2 次，仍不合格则输出当前最佳版本并附『建议人工复核』`。实测中，一次资料缺失导致前两轮生成都不达标，第三轮直接输出最佳版本并提示人工复核，流程干净结束。如果没有这 2 次的上限，这个问题会无限重生成——终止条件就是循环的保险丝。",
    },
    "s4-or-c3-s1": {
        "content": "错误处理的第一步，是给错误分类——不同类别的错误，处置方式不同。三类典型错误：瞬时错误——超时、限流这类『过一会可能就好了』的，处置是重试（配合退避）；逻辑错误——参数非法、数据不匹配这类『请求本身有问题』的，处置是修正（改参数、换输入）或让 Agent 澄清；致命错误——鉴权失败、依赖不可用这类『重试也救不了』的，处置是立即上报、转人工，不做无谓重试。\n\n错误分类的价值，是避免『一刀切』：对瞬时错误做有限重试（性价比最高），对逻辑错误别死磕重试（该修正修正、该澄清澄清），对致命错误立刻止损（别再烧钱重试）。工程实现上，用一个错误分类器（按错误码或类型判断）把异常映射到处置策略，每个策略配好重试次数与退避参数。错误处理分层设计，让 Agent 面对异常时『该重试的重试、该放弃的放弃、该找人的找人』。",
        "case": "一个 Agent 调用外部天气 API 的三种错误处置：超时（瞬时）→ 退避重试 3 次；返回『城市不存在』（逻辑）→ 修正参数重试 1 次，不行就问用户；鉴权失败（致命）→ 立即上报运维、转人工处理，不重试。三种错误三种处置，Agent 既不过度重试浪费资源，也不一遇错误就崩溃——分类处置让异常处理有章法。",
    },
    "s4-or-c3-s2": {
        "content": "兜底策略，是错误处理链的最后一环：当重试、修正、澄清都救不了时，Agent 必须有『优雅退出』的方式，而不是把错误或空白抛给用户。三类兜底：降级——主方案失败时退回次优方案（强模型不可用→用弱模型；Agent 完整流程失败→退回直线问答）；明确告知——如实告诉用户『暂时无法处理』及原因、建议的下一步（如『请稍后再试』『请联系人工客服』）；转人工——把复杂/失败场景交给人类（客服场景里最常见，也是最可靠的兜底）。\n\n兜底设计的原则：兜底路径也要『有内容』——给用户一个明确的状态与下一步，而不是一句干巴巴的『出错了』；兜底要分层——先尝试降级（自己解决），再明确告知（让用户知情），最后转人工（求援）。把兜底当作流程的正常组成部分来设计（就像红绿灯下的应急通道），Agent 才能做到『再怎么失败都不让用户面对空白』。",
        "case": "一个支付 Agent 的兜底链：支付接口限流 → 退避重试 → 仍失败 → 降级为『引导用户稍后重试』并保留订单草稿 → 用户追问时转人工客服处理。整个失败链路，用户始终得到明确的状态与下一步，没有一次面对白屏或报错弹窗。兜底不是可选项，是把 Agent 的『不可靠』关在笼子里的栅栏。",
    },
    "s4-or-c4-s1": {
        "content": "Agent 工程的四大陷阱，是所有多步骤 Agent 都容易踩的坑。一是死循环——Agent 反复调用同一个工具/反复执行同一步，烧 Token 且无法结束（如模型一直调查询工具却不去判断结果）。二是责任扩散——Agent 多了没人负责，每个 Agent 都以为『别人会做』，关键步骤被跳过。三是上下文爆炸——Agent 之间传递过多信息，撑爆窗口、稀释判断（多 Agent 尤甚）。四是级联失败——一个 Agent 的偏差传给下一个被放大，导致整体输出崩坏（需求理解的偏差被代码 Agent 放大成完全跑偏的功能）。\n\n识别这四大陷阱，关键是在设计时就预判：死循环→设计终止条件；责任扩散→明确每个 Agent 的 owner 与验收；上下文爆炸→最小化信息传递（结论式交接）；级联失败→每步设质量检查点、错误隔离。陷阱不是遇到才头疼，而是设计阶段就要列进检查清单的工程风险。",
        "case": "一个真实的级联失败案例：需求 Agent 把『生成订单报表』理解成『生成销售趋势图』，方案 Agent 基于误解设计成图表功能，代码 Agent 实现了图表——最终交付完全偏离用户需求。每一个环节都『照做了』，但偏差被逐级放大。如果方案 Agent 与需求 Agent 之间有一个『确认理解一致』的检查点，这个偏差在第一环就会被拦下。级联失败的对策，就是在关键交接处设质量门。",
    },
    "s4-or-c4-s2": {
        "content": "对抗四大陷阱，需要一套『工程护栏』——它们是生产级 Agent 的底线，缺一不可。第一道：日志记录每一步——每个 Agent 的输入输出、每次工具调用、每次状态变化都记日志，出问题能回溯；第二道：设置预算上限——单会话的调用次数/Token 成本设上限，超限熔断（降级或终止），防止失控账单；第三道：评估每轮质量——关键环节设质量检查（结果是否合理、是否达标），不合格即纠正；第四道：失败熔断——连续失败达到阈值就停止重试，转兜底或人工，避免『越挫越勇』地烧钱。\n\n护栏的设计原则：宁可『过度记录』不可『无据可查』（日志）；宁可『提前熔断』不可『放任失控』（预算）；宁可『每轮检查』不可『最后才看』（质量）；宁可『及时止损』不可『死磕到底』（失败）。把这四道护栏当作 Agent 工程的标配去落地，你的 Agent 才从『能跑』走向『可靠』——这也是阶段五『监控、安全、评估』章节的提前热身。",
        "case": "给客服 Agent 上全套护栏后的一次事故复盘：某天外部接口异常导致大量失败，但由于有『失败熔断』——连续 3 次失败即停止重试转人工，系统没有陷入无限重试的雪崩；日志让工程师快速定位到接口异常；预算上限防止了异常期间的失控成本。护栏在事故中证明了自己的价值：不是防止出错，而是让出错变得可控、可查、可恢复。",
    },
    "s4-or-c5-s1": {
        "content": "加固 Demo，是把你前面学到的护栏落到一个真实 Agent 上的实践。给阶段四的 Supervisor Demo（或阶段三的『小 G』）加四类加固：终止条件——给 Agent 循环加 max_steps，防止无限调用工具；工具调用去重——同一工具同一参数不重复调用（防死循环）；失败重试与兜底——工具失败按错误分类重试，重试后仍失败走兜底回答；预算上限——单会话的调用次数/成本设上限，超限熔断。\n\n加固后的对比测试是这一节的重点：故意制造异常场景（工具一直失败、模型反复调同一个工具、输入包含诱导指令），观察加固前后的行为差异——加固前可能崩溃/死循环/失控，加固后应该被护栏拦下、优雅结束。这个『制造故障—验证护栏』的练习，让你真正理解每道护栏在什么时候起作用，而不是只会配置。",
        "case": "加固对比测试示例：加固前，让 Demo 调用一个『永远失败的工具』，Agent 陷入无限重试，直到手动终止；加固后，同一场景——失败重试 3 次→触发兜底回答『服务暂时不可用，请稍后再试』→干净结束。再看『模型反复调同一工具』：max_steps=5 + 工具去重让 Agent 在第 5 步强制收敛。一道护栏，把灾难场景变成了可控场景。",
    },
    "s4-or-c5-s2": {
        "content": "加固完 Demo，把经验沉淀成一份『工程清单』（checklist），这是阶段五系统化的桥梁。清单应包含：终止条件——所有循环都有次数上限；日志——每个 Agent/工具/状态变化都记录；评估——关键环节有质量检查；成本控制——调用次数与 Token 预算、熔断；错误处理——错误分类、重试策略、兜底路径；安全——权限最小化、敏感操作审批。\n\n这份清单的价值：一是可复用——做任何新 Agent 时逐项对照，确保工程底线不丢；二是可讲解——面试时能展示『我的工程化意识』（不是只会写 Agent，还知道怎么让它可靠）；三是为阶段五做衔接——阶段五会把这清单里的每一项系统化为正式章节（性能、安全、监控、评估）。把『一次加固』变成『一套方法』，你就完成了从『做 Demo』到『做工程』的思维升级——这是阶段四最重要的一份收获。",
        "case": "沉淀的工程清单落地到一个新项目：新客服 Agent 立项时，团队拿着上一项目沉淀的 checklist 逐项确认——循环终止条件已配、日志已接、预算上限已设、退款审批已加、失败兜底已备。原本要踩一轮坑才能想全的工程要点，因为有清单，立项即到位。清单把『个人经验』变成了『团队资产』——这正是工程方法论的终极价值。",
    },
}
