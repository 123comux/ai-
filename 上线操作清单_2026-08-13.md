# 上线操作清单（2026-08-13 → 2026-11-09）

> 用途：**需要你本人操作的线下事项**（购买/审批/后台配置），代码侧已全部就绪。
> 对照《商业评审报告》6.6 硬性前提 + 11.9 上线计划。
> 每条完成请打勾，缺哪项就卡哪项。

---

## 一、立即启动（有审批周期，越早越好）

### 1. 购买域名 + 云服务器
- [ ] 在同一云厂商（腾讯云 / 阿里云）购买：1 个域名 + 1 台云服务器（2核4G 起步即可）
- [ ] 保持同账号（备案要用它的服务器 IP）
- **为什么**：没有服务器和域名，后面所有部署、备案都无从谈起。
- **耗时**：当天可完成。

### 2. ICP 备案提交（最紧急）
- [ ] 在云厂商备案系统提交：主体信息（企业主体已有）+ 网站信息 + 域名
- [ ] 备案期间可继续开发，不阻塞
- **为什么**：备案周期 10-20 自然日，是 11.9 能否上线的最大时间风险。**今天（8/13）就该提交。**
- **耗时**：提交后约 1-3 周通过。

### 3. 小程序企业主体认证
- [ ] 登录 [微信公众平台](https://mp.weixin.qq.com) → 小程序 → 设置 → 微信认证
- [ ] 用企业主体提交（现有 AppID `wxdb921a9b6073a909` 挂到企业主体下）
- **为什么**：个人主体无法使用微信支付、部分接口受限；押金收退款必须企业主体。
- **耗时**：300 元/年，审核数天。

### 4. 微信支付商户号申请
- [ ] 在微信支付商户平台申请商户号（V3），绑定上述小程序
- [ ] 申请 APIv3 密钥 + 下载 API 证书（apiclient_key.pem、序列号）
- [ ] 把值填入 `backend/.env`（代码已就绪，配好后自动启用真实支付）：
  ```
  WXPAY_MCHID=<商户号>
  WXPAY_SERIAL_NO=<证书序列号>
  WXPAY_PRIVATE_KEY=/app/certs/apiclient_key.pem  # 或绝对路径
  WXPAY_APIV3_KEY=<32字节APIv3密钥>
  WXPAY_NOTIFY_URL=https://你的域名/api/pay/notify
  WXPAY_REFUND_NOTIFY_URL=https://你的域名/api/pay/notify
  ```
- **为什么**：押金收付从占位变真实的核心依赖；代码已开发完（JSAPI 下单/回调验签/退款），配好即用。
- **耗时**：企业主体认证通过后 1-7 个工作日。

### 5. 订阅消息模板 ID 申请
- [ ] 小程序后台 → 订阅消息 → 申请模板：
  - 打卡成功提醒 → 填 `backend/.env` 的 `WX_SUB_TEMPLATE_CHECKIN`
  - 作业评审结果通知 → `WX_SUB_TEMPLATE_HOMEWORK`
  - 学习提醒 → `WX_SUB_TEMPLATE_LEARNING`
- [ ] 前端同步：`src/utils/subscribe.ts` 的 `SUB_TEMPLATES` 填同一批 ID
- **为什么**：未填则推送静默跳过（代码已就绪，不影响其他功能）。

### 6. 配置 DeepSeek API Key（可选但建议）
- [ ] 在 [DeepSeek 开放平台](https://platform.deepseek.com) 充值并拿 Key
- [ ] 填 `backend/.env` 的 `DEEPSEEK_API_KEY`
- **为什么**：不填则 AI 全部走智谱 flash（够用）；填了则作业评审走 deepseek-reasoner（更强）、标准档更省钱。

---

## 二、11 月集中（与部署同步）

### 7. HTTPS 证书
- [ ] Let's Encrypt（certbot）或云厂商免费 SSL 证书
- [ ] 证书文件放到 `deploy/certs/fullchain.pem`、`privkey.pem`
- [ ] 记得配自动续期（certbot renew crontab）
- **为什么**：小程序 request 合法域名只接受 HTTPS。

### 8. 域名解析 + 部署
- [ ] 域名 A 记录指向服务器公网 IP
- [ ] 按 `DEPLOY.md` 部署后端（Docker）+ Nginx
- [ ] `deploy/nginx.conf` 两处 `server_name` 改为你的域名
- [ ] 构建前端并注入生产 API 地址：
  ```
  TARO_APP_API_BASE=https://你的域名 npm run build:weapp
  TARO_APP_API_BASE=https://你的域名 npm run build:h5
  ```
- **为什么**：11.9 前必须「可公网访问」。

### 9. 小程序后台配置合法域名
- [ ] 公众平台 → 开发管理 → 开发设置 → 服务器域名
- [ ] request 合法域名：`https://你的域名`
- [ ] uploadFile / downloadFile 合法域名按需添加
- **为什么**：不配置小程序无法请求后端。

---

## 三、提审与发布（11.9 前留 1-2 周）

### 10. 上线自检 + 提审
- [ ] `backend/.env`：`DEV_MODE=false`、`JWT_SECRET` 已换强随机串、商户号已配
- [ ] 真实支付/退款各走一笔小额测试
- [ ] 按 `DEPLOY.md` §3.5 提审清单逐项核对
- [ ] 准备《隐私保护指引》与《押金服务协议》（押金收退款是审核重点）
- [ ] 选择类目：教育/在线培训
- [ ] 提交审核（附测试账号 + 功能说明）
- **为什么**：审核 1-7 天，需预留缓冲。

### 11. 发布
- [ ] 审核通过后发布
- [ ] 首日监控：登录 / 支付到账 / 退款核对 / 后端日志 / 后台看板

---

## 依赖关系速览

```
今天(8/13)起 ── 购买域名+服务器 ──▶ ICP备案(10-20天) ──▶ HTTPS+部署 ──▶ 提审(1-7天) ──▶ 11.9 发布
                    │
                    ├──▶ 企业认证(数天) ──▶ 商户号(1-7天) ──▶ 真实支付
                    │
                    └──▶ 订阅模板ID ──▶ 消息推送
```

**最大风险**：ICP 备案。**建议今天就提交**，否则 11.9 大概率延期。
