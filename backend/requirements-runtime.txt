# 运行时依赖（精简版）——仅 API 服务所需。
# 说明：本地 torch / transformers 模型（services/tutor_service.py、
# services/assessment_ai_service.py、training/*）只是 AI 能力的「兜底」实现，
# 线上主路径走智谱 GLM API，因此镜像里不需要这些重依赖，启动更快、体积更小。
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.0
httpx==0.27.2
python-dotenv==1.0.1
# HTTP 客户端：智谱/DeepSeek/微信支付/订阅消息均走 requests
requests==2.32.3
# 微信支付 V3：RSA-SHA256 签名 + AES-256-GCM 回调解密
cryptography==49.0.0
