"""阶段二 · Agent 基础概念 — 小节内容扩充。"""

SECTION_ENRICH: dict[str, dict] = {
    # ================= s2-agent-core AI Agent 核心概念 =================
    "s2-core-c1-s1": {
        "content": "聊天机器人与 AI Agent 的分水岭，是『说』与『做』。聊天机器人只负责理解你的话并生成回复——它把一切问题都转化为『语言输出』，无法触碰真实世界。AI Agent 则是一个能主动执行任务的智能体：它不仅能理解需求，还能调用工具（查数据库、调 API、读写文件）、规划步骤、把任务真正做完。打个比方：聊天机器人是『顾问』，只会给建议；Agent 是『执行者』，会把事情办妥。\n\n这种跨越意义重大：因为多数真实任务不是『回答一个问题』，而是『完成一串操作』。比如『帮我查这个订单到哪了并催促发货』，聊天机器人只能回答查询结果，Agent 却能查订单、判断状态、调用催单接口、再把结果汇报给你。理解『被动回答 vs 主动执行』，是判断一个产品是否算 Agent 的第一把尺子。",
        "case": "同样是『帮我处理客户退款』：聊天机器人能解释退款政策；而一个智能客服 Agent 能查询订单、校验退款资格、发起退款流程、并处理异常（金额不符则转人工）。前者停留在信息层面，后者真正推进了业务。AI 应用从『问答』到『办事』，就是 Agent 带来的范式跃迁。",
    },
    "s2-core-c1-s2": {
        "content": "一个完整的 Agent，可以拆成四个部件：LLM（大脑）——负责理解、推理与决策，是 Agent 的智能核心；工具（手脚）——LLM 本身不能执行，工具（API、数据库、文件系统）让它能对世界产生实际作用；记忆（经验）——让 Agent 记住对话历史与沉淀的知识，避免每次从零开始；规划（策略）——把大任务拆成小步骤，决定『先做什么、后做什么』。四者协作，才构成『能感知、能决策、能执行』的完整闭环。\n\n理解这个组成，对开发有直接指导意义：你写 Agent 时，实际上是在编排这四件套——选一个 LLM 当大脑、注册一批工具当手脚、接一套记忆、写一条规划循环。阶段二到四的课程，就是逐个深入这四个部件（记忆见第八章、工具见第七章、规划与架构见第六/九章）。",
        "case": "以客服 Agent 为例对号入座：LLM=读懂用户诉求；工具=订单查询、退款接口、工单系统；记忆=记住用户前面提供的订单号和诉求；规划=先验证身份→查订单→判断是否符合退款条件→发起退款或转人工。任何 Agent 产品，都能用这个四件套模型拆解清楚。",
    },
    "s2-core-c2-s1": {
        "content": "Agent 的四大核心能力中，感知与推理决定了它『能不能想明白』。感知（Perception）：把外部信息转成模型能处理的形式——理解用户的一句话、读取系统状态、解析工具返回的数据。感知的核心是『信息转化』，比如把用户口语『帮我看看那个快递』转化为结构化的查询意图。推理（Reasoning）：在感知到的信息上进行分析——拆解任务、判断前提是否成立、生成候选方案。比如推理出『用户说“那个快递”指最近下单的订单』。\n\n感知与推理在实践中高度耦合：感知为推理提供素材，推理结果又决定下一步要感知什么。对开发者而言，这两步通常交给 LLM 完成，但你要理解它们的存在——因为你需要设计『给模型什么信息』（感知侧）和『让模型怎么想』（推理侧，见思维链），这是决定 Agent 质量的两大抓手。",
        "case": "用户对客服 Agent 说『我之前买的耳机坏了』。感知环节：模型识别出意图=售后、实体=耳机、隐含信息=需要查询订单；推理环节：判断需要先查用户订单找到对应耳机订单，再决定后续动作。如果感知丢掉了『之前买的』这个时间线索，Agent 可能查错订单。感知与推理的质量，直接决定 Agent 的下一步动作是否正确。",
    },
    "s2-core-c2-s2": {
        "content": "决策与执行是 Agent 的『手脚』，决定它能否真正把事情办成。决策（Decision Making）：在推理出的多个候选动作里，选择最合适的一个——比如判断该调『订单查询』工具还是『退款申请』工具。执行（Action）：真正调用选中的工具，并把执行结果（成功/失败/返回数据）取回。关键点在于：执行后的结果必须『回填』给模型，让模型基于真实结果继续决策——这构成了 Agent 的工作循环。\n\n决策的质量依赖两个东西：一是候选动作（工具）定义得是否清晰（工具描述不清，模型就会选错，呼应第七章工具描述）；二是决策依据是否充分（模型需要看到足够的上下文才能做对选择）。而执行环节，开发者最该关注的是错误处理——工具可能失败、参数可能不对，Agent 要能识别失败并调整策略，而不是硬着头皮继续。",
        "case": "一个能『查天气』和『订机票』的 Agent，用户问『北京明天冷不冷』。推理后模型决策：应该调用查天气工具（而不是订机票）；执行返回『明天气温 8-15 度』；模型基于结果组织回答『偏冷，建议穿厚外套』。如果工具描述写得含糊（两个工具描述相似），模型就可能误调订机票工具——决策和执行看似简单，实则高度依赖工具设计与结果反馈。",
    },
    "s2-core-c3-s1": {
        "content": "判断一个 LLM 应用是否算 Agent，用三个问题自测：是否调用外部工具（还是只在内部生成文本）？是否多步规划（还是单轮一问一答）？是否自主决策（还是每次都按固定脚本走）？三个都『是』，是货真价实的 Agent；有一个『是』，是带 Agent 元素的增强应用；全『否』，就是普通的 LLM 应用（哪怕它回答得很聪明）。\n\n这个判定标准的意义，是帮你厘清『Agent 化』的增量到底在哪里：它增加的不是模型能力，而是『调用工具 + 多步循环 + 自主决策』这一层工程外壳。理解这一点，你就不会纠结『是不是用了 LangChain 就是 Agent』——框架只是工具，Agent 的本质是这套闭环架构。",
        "case": "一个『用 LLM 生成周报』的应用，只有生成没有工具调用、没有多步规划，它是 LLM 应用而非 Agent。同样做周报，如果它自动读取本周日程数据（调接口）、筛选重要事件（推理）、再生成周报并归档（执行），就是 Agent。同一个业务目标，是不是 Agent 的差别，全在那层『自动执行的闭环』上。",
    },
    "s2-core-c3-s2": {
        "content": "为什么在 LLM 之上还要多包一层 Agent？因为 LLM 单次输出无法完成真实任务。真实任务往往是多步骤、要访问外部系统、需要异常处理的——『查库存→算折扣→下单→通知用户』，这四步单靠一次 LLM 调用无法完成：模型不知道实时库存、无法真正下单、也不会在失败时重试。Agent 层补上的正是这些：循环（一步步执行）、工具（访问真实系统）、记忆（记住中间状态）、异常处理（失败怎么补救）。\n\n换句话：LLM 是『能力引擎』，Agent 是把引擎装进车架、接上方向盘、让它能在真实道路上行驶的整车。没有 Agent 层，LLM 的能力只能停留在『聊天』；有了 Agent 层，能力才能真正『落地办事』。这也解释了为什么 AI 应用工程师的技能栈，重心正从『写提示词』转向『编排 Agent』。",
        "case": "一个物流查询产品最初是纯 LLM 对话：用户问包裹到哪了，模型只能按训练记忆给一个模糊回答。加上 Agent 层后：模型识别出要查运单号→调用物流 API 实时查询→拿到『已到上海中转站』→再组织成回答。同样是答一句话，前者是编的、后者是查的。Agent 层让 LLM 从『凭记忆说话』升级为『基于实时数据办事』。",
    },
    "s2-core-c4-s1": {
        "content": "Agent 的落地场景集中在『任务需要多步 + 需要访问真实系统』的地方。三大高频场景：一是智能客服——查订单、处理售后、引导流程，任务链条清晰、工具明确，是 Agent 最成熟的落地形态；二是办公自动化——自动取数（连数据库）、分析（LLM 推理）、成文（生成报告），把『查数-分析-写报告』整条链路自动化；三是研发助手——查代码库、定位问题、改代码、跑测试，在开发流程内做 Agent 化。\n\n判断一个场景适不适合 Agent，看它是否符合：有明确的目标、需要多个步骤、能访问外部系统、动作可预期。符合越多，Agent 化价值越大。反过来，纯聊天、纯生成、不需要访问外部系统的场景，上 Agent 是过度设计。记住学习路线的原则：先问『单 Agent 哪里不够』，而不是为了用 Agent 而用。",
        "case": "电商售后是典型场景：用户退换货要『验证订单→检查退货资格→生成退货单→通知物流』，每一步都查系统、都可能失败。客服 Agent 把这些串起来后，70% 的标准售后请求无需人工即可处理完毕，人工只接手异常情况。而另一家做『AI 聊天陪伴』的产品，强行套 Agent 架构反而增加了延迟和成本——场景不匹配，Agent 反而是负担。",
    },
    "s2-core-c4-s2": {
        "content": "场景适配判断是一道『该不该上 Agent』的决策题，用四个问题把关：第一，任务是否需要多步执行？——一问一答的任务不需要 Agent，直接 LLM 就够；第二，是否要访问外部系统？——如果不需要查库、调接口，Agent 的工具层就没用武之地；第三，是否允许自主执行？——涉及高风险动作（支付、删除）的场景，要么加人工审批，要么干脆不做成自动 Agent；第四，是否有明确的成功标准？——没有可判定的『做完』，Agent 就不知道何时停止。\n\n四问下来，如果多数是『否』，就退回普通 LLM 应用——这能省成本、降延迟、减风险。场景适配判断是架构师思维：不是把最强的技术套在每个需求上，而是为每个需求选最合适的技术。Agent 是工具箱里的一件工具，不是万能答案。",
        "case": "团队为『自动回复差评』设计 Agent：判断发现——任务是多步的（识别差评→分析原因→生成回复→提交）、要访问电商后台、动作有风险（直接回复可能激怒用户）。因此最终设计为『Agent 自动分析并生成回复草稿，人工一键确认后才发布』，既利用了 Agent 的效率，又保住了风险控制。场景判断的结论，往往不是『用不用 Agent』，而是『Agent 做到哪一步』。",
    },
    "s2-core-c5-s1": {
        "content": "不依赖任何框架，用『LLM + 一个普通函数』就能实现最小的 Agent 循环，这是理解 Agent 本质的最佳途径。核心逻辑约 30 行：第一轮，把用户问题发给 LLM，同时告诉它『你有一个工具 get_weather(city)，需要时返回结构化的调用请求』；模型返回的内容若含工具调用意图，程序解析出要调的函数与参数；程序真正执行该函数拿到结果；把结果作为新的消息回传给模型；模型基于结果给出最终回答。如此循环，直到模型不再请求工具。\n\n这段代码的价值在于：它让你亲手看见『模型的输出是决定、程序是执行者、结果再喂回模型』这个核心循环。看完这段手工实现，后面无论是 LangChain 的 AgentExecutor 还是 LangGraph 的图，你都能一眼看穿它们只是把同样的循环包装得更工程化。",
        "case": "手写实现时，模型返回的可能是 `{\"function\": \"get_weather\", \"arguments\": {\"city\": \"北京\"}}` 这样的结构化文本。程序解析它 → 调用真实的天气函数 → 把结果 `{\"北京\": \"晴，15度\"}` 作为 tool 消息回传 → 模型据此回答『北京今天晴，15 度』。看到结果里有真实查询的数据，你就明白了 Agent 为什么比纯聊天可靠——它确实『查了』而不是『编了』。",
    },
    "s2-core-c5-s2": {
        "content": "跑通最小 Agent 后，要清醒地看到它缺什么——这正是工程化的方向。它缺少：终止条件（如果没有，模型可能无限循环调工具）；错误处理（工具抛异常怎么办，没有兜底）；记忆（上一轮的结果没有跨会话保留）；并发与限流（多用户同时用会怎样）；日志与调试（出错了看不出哪一步的问题）。阶段二到阶段五的课程，本质就是在给这个最小闭环逐步补齐这些工程能力。\n\n另一条重要认知：手工实现让你理解原理，但生产环境几乎不会手写循环——LangChain/LangGraph 等框架把这些工程能力（循环控制、记忆、错误处理、可观测性）都封装好了。所以正确的学习路径是『先手写懂原理，再上框架提效率』——你将来用框架时，知道它在替你处理什么，而不是黑盒地用。",
        "case": "手写的天气 Agent 在用户问『北京和上海哪个暖和』时可能陷入问题：模型要分别调两次工具，如果循环没有限制次数、没有去重，可能重复查询浪费调用。给循环加上『最多 5 步、同一工具不重复调用』的约束后，行为才变得可控。这个小例子说明：Agent 的工程化不是锦上添花，而是让它从『能跑』走向『可靠』的必要条件。",
    },

    # ================= s2-agent-arch Agent 架构模式 =================
    "s2-arch-c1-s1": {
        "content": "ReAct（Reasoning and Acting，推理与行动）是最经典、也是理解 Agent 绕不开的架构模式。它的核心是一个循环：Thought（推理）——模型思考『当前情况是什么、下一步该做什么』；Action（行动）——模型决定调用哪个工具、传什么参数；Observation（观察）——程序执行工具并返回真实结果；然后带着这个观察结果回到 Thought，继续循环。如此反复，直到模型判断任务完成，输出最终回答。\n\n这个循环的精髓是『推理与行动交替进行』：模型不一次性给答案，而是每走一步都基于上一步的真实结果（Observation）调整下一步。这就避免了『闭眼一次猜到底』——哪怕第一次行动选错了工具，观察到结果后也能纠正。ReAct 是后续所有复杂架构（Plan-Execute、Agentic RAG 等）的底层 DNA。",
        "case": "用户问『北京明天穿什么』。ReAct 循环：Thought『我需要查北京的天气』→ Action 调用 get_weather(city=北京) → Observation 返回『明天 25℃，多云』→ Thought『25 度适合穿短袖』→ 输出最终回答『明天北京 25 度，建议穿短袖』。注意中间真实发生了一次工具调用，答案基于真实天气数据而非模型猜测——这就是 ReAct 让 Agent 可靠的机制。",
    },
    "s2-arch-c1-s2": {
        "content": "ReAct 为什么有效？因为它把『思考过程』变成了可观察、可纠正的显式步骤。直接让模型给答案，是让它『一次性押注』——多步任务里任何一步想错，最终答案就错了，而且你无法定位错在哪一步。ReAct 把推理写成 Thought→Action→Observation 的显式链条后：第一，每一步基于上一步的真实反馈，错误能在早期被发现和纠正（比如工具返回『查无此城市』，模型会换个城市重试而不是继续往下编）；第二，整个过程可以记录和回放，出了问题能精确知道是哪一步决策失误；第三，工具返回的真实数据为模型提供了『事实锚点』，大幅降低幻觉。\n\n从训练角度看，ReAct 也契合 LLM 的训练模式——模型在训练中见过大量『思考再行动』的文本，被要求显式推理能激活更可靠的处理路径。理解『为什么有效』，你就明白了为什么几乎所有 Agent 框架都默认采用这个循环。",
        "case": "对比两种设计：甲设计让模型直接回答『北京到上海高铁多久』，模型可能凭记忆给出不准确的时长；乙设计用 ReAct——Thought『需要查真实车次』→ Action 调铁路查询工具 → Observation 返回真实车次与时长 → 回答。同样的问题，ReAct 版本因为中间查询了真实数据，回答准确且可溯源。显式的推理—行动交替，就是可靠性的来源。",
    },
    "s2-arch-c2-s1": {
        "content": "Plan and Execute（先规划再执行）是 ReAct 的另一种组织方式：不再边走边想，而是先让模型生成一份完整计划（Planner），再逐个执行计划步骤（Executor）。典型流程：Planner 把大任务拆成步骤清单（如『①查库存 ②算总价 ③生成订单』）；Executor 依次执行每一步；每步完成后，可让 Planner 检查计划是否需要调整。相比 ReAct 的『走一步看一步』，这种两阶段架构更省 Token、更可控。\n\n适用场景：任务结构清晰、步骤可预见（流程化业务）。局限也很明显：如果任务在执行中暴露出的情况与计划不符（比如查库存发现商品下架），固定计划就缺乏灵活性——需要结合 ReAct 在局部做修正。架构选择的本质，是在『计划先行（高效但僵化）』与『边走边想（灵活但费 token）』之间，按任务的确定性程度做权衡。",
        "case": "做一个『月度经营报告生成』Agent：Planner 先生成计划『①连接数据库取本月营收/订单数据 ②调用分析模型计算环比 ③按模板生成报告』；Executor 依次执行，每步结果都被程序校验。因为流程高度固定，用 Plan-Execute 比 ReAct 少了很多无谓的推理轮次，成本更低、更可控。当某个月数据结构异常时，Executor 报错，Planner 调整计划补充数据清洗步骤——两阶段配合完成了灵活修正。",
    },
    "s2-arch-c2-s2": {
        "content": "选 Plan-Execute 还是 ReAct，本质是看任务的确定性。Plan-Execute 擅长『流程明确、步骤固定』的任务：进销存处理、报表生成、审批流等——这类任务你能提前列全步骤，计划先行能省下大量试错轮次。ReAct 擅长『结果不可预知、需要探索』的任务：开放式的资料研究、故障排查、用户意图模糊的场景——这类任务计划赶不上变化，边走边想更稳妥。\n\n实操建议：不要二选一，而是混合。对整体确定性高、局部可能出意外的任务，用 Plan-Execute 搭骨架，在每一步内部用 ReAct 处理局部不确定性；或让 Planner 定期检查计划、发现偏差就更新计划。架构是工具，任务是需求，选型永远以『能不能更可靠更省钱地完成任务』为准。",
        "case": "一个财务对账 Agent 整体流程固定（取数→对账→差异分析→出报告），用 Plan-Execute 高效完成；但『差异分析』这一步常因数据异常产生意料外的情况，所以该步内部用 ReAct：模型根据差异内容决定查哪个表、调哪个工具，一步步定位。固定骨架 + 灵活细节，就是混合架构的典型形态。",
    },
    "s2-arch-c3-s1": {
        "content": "Reflection（反思）机制，是在 Agent 生成答案之后，再加一轮『自我批判与修正』。流程是：先生成第一版输出；然后让模型切换成『评审者』视角，审视自己刚才的输出——逻辑是否严密、是否遗漏关键点、是否符合约束；根据评审意见，重新生成改进版。这个『生成→反思→修正』的闭环，显著提升输出质量，代价是多一次甚至多次 LLM 调用。\n\nReflection 最适合质量敏感的产出：代码（让 Agent 自己找 bug 再修复）、长文（自查逻辑连贯与遗漏）、需要严格遵守约束的任务。工程上有两种实现：单模型自我反思（同一个模型分饰『作者』与『评审』两个角色）与双模型互评（一个生成、一个评审，评审不合格则重生成）。注意反思要有轮次上限，避免『无限自我怀疑』。",
        "case": "一个代码生成 Agent 初始输出一段带 bug 的函数。启用 Reflection 后：模型被要求『以资深代码评审视角找出这段代码的问题』，发现自己漏了空指针判断；随即重写修复。开启反思后，生成代码的单元测试通过率从 68% 提升到 91%。代价是单次任务多调用 1-2 次 LLM——在代码质量敏感的场景，这笔成本完全值得。",
    },
    "s2-arch-c3-s2": {
        "content": "Reflection 的成本控制，核心是给『反思』设边界。每次反思都是一次完整的 LLM 调用（输入=原文+评审提示词，输出=评审意见或新版本），成本和时间都成倍增加。三个控制手段：第一，设反思轮次上限（如最多 2 轮），超出则采用当前最优版本，防止无限循环；第二，分级反思——只在『值得反思』的任务上开启（代码、正式文档），对快速问答直接输出，不做反思；第三，用评分门槛决定是否重生成——评审给分高于阈值就直接采用，低于阈值才重写，避免每个答案都走完整反思流程。\n\n本质是质量与成本的权衡：把有限的额外 Token 花在『高风险高价值』的产出上。工程上可以统计『反思轮次与质量提升』的关系，找到边际收益递减点，作为成本与效果的平衡线。",
        "case": "团队给反思设了『最多 2 轮 + 评分门槛』：评审分 ≥8 分（10 分制）直接采用，低于 8 分才重写，且最多重写 2 次。结果：约 60% 的答案一次通过，平均每任务仅多 0.8 次调用，成本增加约 15%，而质量指标提升明显。相比之下，不做门槛、无限反思的方案成本翻了 3 倍，质量却没有同比例提升。反思必须带刹车。",
    },
    "s2-arch-c4-s1": {
        "content": "多 Agent 架构的价值，来自『分工』带来的三件事。一是角色专业化：不同 Agent 承担不同角色（如产品经理、架构师、程序员），各自把领域知识做深，避免一个 Agent 既要懂需求又要写代码还要测试导致顾此失彼。二是并行加速：彼此独立的子任务由多个 Agent 同时执行，能显著缩短整体耗时。三是独立审查：一个 Agent 生成、另一个 Agent 独立检查，避免『自己检查自己的错误』的盲区——这与人工质检的原理一致。\n\n理解多 Agent 的价值，同时要认清它的边界：多 Agent 意味着多次 LLM 调用（成本上升）、串行时延迟增加、调试变难（错误会级联）、上下文信息传递可能爆炸。价值与代价是一体两面。学习路线的建议是：多 Agent 是高级武器，先掌握单 Agent 做到极致，再判断是否需要。",
        "case": "软件开发 Agent 是典型的多 Agent：产品经理 Agent 产出需求 → 架构师 Agent 设计技术方案 → 程序员 Agent 写代码 → 测试 Agent 检查并反馈。四个角色分工明确，测试 Agent 能独立发现程序员 Agent 的 bug（独立审查的价值）。但每次迭代都要调用 4 次以上 LLM，成本明显高于单 Agent——多 Agent 不是免费的，它是用成本换分工带来的质量与并行。",
    },
    "s2-arch-c4-s2": {
        "content": "『单 Agent 哪里不够』是决定是否上多 Agent 的判据。先认真问自己：单 Agent 到底在哪些具体环节撑不住了？是工具太多导致选型混乱（→ 可按工具分组交给不同 Agent）、还是角色冲突（→ 拆成不同角色的 Agent）、还是任务可并行（→ 多个 Agent 同时跑）、还是需要独立审查（→ 生成 + 检查分离）。只有当你能明确指出『单 Agent 具体哪里不够』时，多 Agent 才有意义。\n\n反过来，如果只是『听说多 Agent 更高级』就想上，那会收获成本翻倍、延迟变高、调试困难的痛苦。工程上有个朴素原则：能用单 Agent 解决的，绝不上多 Agent；即使确实需要，也先从一个最小的多 Agent 形态（如 Supervisor + 2 个执行者）起步，验证价值后再扩展。",
        "case": "一个客服 Agent 初期工具只有 3 个，单 Agent 完全够用，团队却强行拆成『售前/售后』两个 Agent，结果两个 Agent 频繁抢任务、上下文还要互相传，成本和延迟都上去了，效果没变好。后来工具增长到 20 多个（查订单/查物流/算优惠/客服工单…），单 Agent 频繁选错工具，才真正触发『工具太多选不过来』，此时按业务分组引入多 Agent 才带来实际收益。多 Agent 是为解决具体问题而上的，不是为用而用。",
    },
    "s2-arch-c5-s1": {
        "content": "架构选型，本质上是对任务特征做评估后的决策。四类经典对应：确定性流程（步骤固定、结果可预见）→ Plan-Execute，省 Token 且可控；探索性任务（目标明确但路径未知、需要试错）→ ReAct，边走边想最灵活；质量敏感（代码、正式文档，错误代价高）→ 在 ReAct 或 Plan-Execute 之上叠加 Reflection，用额外成本换质量；可并行的独立子任务 → 才考虑多 Agent。\n\n选型时的思维方式：先判断任务属于哪种『形状』（固定流程？开放探索？质量敏感？并行子任务？），再匹配架构，而不是先选框架再硬套。实践中一个任务往往混合了多种形状——用主干架构 + 局部架构组合，而不是单一架构打天下。关键是要能讲出『为什么这么选』：因为任务 XX，所以用 YY 架构，它解决了 ZZ 问题。",
        "case": "做一个『行业研究报告生成』Agent：整体是固定流程（收集→分析→成文），用 Plan-Execute 提高效率；但『收集信息』这一步需要搜索并筛选大量资料，结果不可预知，内部用 ReAct 边查边判断；最终成文质量要求高，最后加一轮 Reflection 自查。三层架构各司其职，比任何单一架构都更贴合任务的真实形状。",
    },
    "s2-arch-c5-s2": {
        "content": "真实生产系统中的 Agent 极少是单一架构，更多是组合与混合。两种常见组合：一是分层混合——外层用 Plan-Execute 搭骨架，内层每一步用 ReAct 处理局部不确定性（前文财务对账的例子）；二是阶段混合——不同阶段用不同架构，如『收集阶段 ReAct（探索）+ 生成阶段 Reflection（质量）』。混合架构的本质，是把任务拆成几个子阶段，为每个子阶段选最合适的处理模式。\n\n组合时注意两点：一是接口清晰——各架构/阶段之间的输入输出要结构化，便于衔接；二是成本意识——每叠加一层（ReAct 循环、Reflection 反思、多 Agent 分工）都增加调用次数，混合不是越多越好，而是『哪里需要才加哪里』。架构是为服务质量与成本服务的，不是炫技。",
        "case": "一个故障排查 Agent 采用混合架构：接到报障后先用 ReAct 逐步排查（查看日志→定位异常→判断原因），排查出根因后，对『修复方案』用 Reflection 自查一遍（避免给出有副作用的修复）。整条链路里 ReAct 负责探索、Reflection 负责质量，各管一段。架构混合得像装配流水线，每一段都只解决一个具体问题。",
    },

    # ================= s2-tool-calling 工具调用 Tool Calling =================
    "s2-tool-c1-s1": {
        "content": "Function Calling（函数调用）的原理，是让 LLM 负责『决定调哪个函数、传什么参数』，而真正执行由你的程序完成。实现上：你把可用工具的『说明书』（函数名、功能描述、参数 schema）作为结构化定义传给模型；模型根据用户意图，在需要时返回一个结构化的『调用请求』（如 `调用 get_weather，参数 city=北京`），而不是直接执行任何代码——它没有执行能力，它只负责『决策』。\n\n这一步设计精妙之处在于解耦：模型不直接操作你的系统（安全），程序不负责理解意图（交给模型），两者各司其职。模型输出的调用请求是标准 JSON，程序解析后真正调用函数。这个『模型决定、程序执行』的分工，是所有工具调用的底层范式，也是 Agent 能安全地使用外部系统的基础。",
        "case": "客服 Agent 注册了 get_order 工具。用户说『查一下我的订单』，模型并不直接访问数据库，而是返回结构化请求 `{\"name\":\"get_order\",\"args\":{\"order_id\":\"2024-001\"}}`；你的程序解析请求、查询数据库、把结果返回给模型，模型再组织成自然语言回答。整个过程模型从未直接接触数据库，安全边界清晰。",
    },
    "s2-tool-c1-s2": {
        "content": "调用闭环是工具调用的完整工作流，一共四步循环：第一步，模型收到用户问题与工具定义，输出结构化调用请求；第二步，你的程序解析请求、执行对应的真实函数；第三步，把执行结果（数据、状态或错误）作为『工具消息』回传给模型；第四步，模型基于真实结果继续——要么再发起下一次调用，要么给出最终回答。四步循环往复，直到任务完成。\n\n理解这个闭环，就理解了 Agent 的『手』是怎么工作的。关键点有三个：结果必须结构化回传（让模型能读懂）；结果回传后模型可能继续调用（所以是循环不是一次）；每一步都可能失败（要有错误处理）。后续 LangChain 的 ToolNode、LangGraph 的工具节点，本质都是把这个闭环工程化封装。",
        "case": "天气 Agent 完整闭环：用户问『上海周末下雨吗』→ 模型调用 get_weather(上海) → 程序返回『周六小雨，周日多云』→ 模型看到结果，判断还需不需要查更细，不需要则回答『周六有小雨，出行带伞』。如果第一次查询的返回是『查无此城市』，模型会修正参数（比如把『上海』改为标准城市名）再调一次——闭环让错误能自我纠正。",
    },
    "s2-tool-c2-s1": {
        "content": "工具 schema 是给模型看的『工具说明书』，结构上包含三部分：name（工具名）、description（工具是干什么的、什么时候用它）、parameters（参数定义，用 JSON Schema 描述每个参数的类型、是否必填、可选枚举值）。参数定义越精确，模型越能生成正确的调用。例如 `get_weather` 的参数 `city`：类型 string、必填、可给枚举（['北京','上海',...]）——模型就不会传一个数字进去。\n\n写 schema 的关键是『替模型着想』：模型看不到你的函数实现，它只依赖这份 schema 决定怎么调用。所以描述要讲清『这个工具解决什么问题、参数怎么传、什么情况别用它』。工具 schema 的质量，直接决定模型能否正确选用工具——这是工具调用里性价比最高的优化点，因为它只需要你写清楚文字，就能显著改善模型行为。",
        "case": "对比两个 schema：`get_price 参数 product_id`（含糊）vs `get_price 参数 product_id：查询商品当前售价，适用于用户询问价格或优惠，product_id 为商品唯一编号`（清晰）。前者模型常传错参数或误用；后者模型能准确判断『该不该调、传什么』。实际项目里，把工具描述写好的收益，往往比换更强的模型还大。",
    },
    "s2-tool-c2-s2": {
        "content": "工具描述的质量直接决定 Agent 的成败，这是工具调用里最容易被低估、也最值得打磨的环节。描述要回答三个问题：这个工具做什么（功能）、什么时候用它（触发条件）、什么时候不要用它（边界）。比如『查天气』的描述可以写『获取指定城市当前及未来几天的天气，适用于用户询问天气、温度、降雨或穿衣建议；若是查询机票请勿使用』——明确的边界能阻止模型拿它硬套不相关的任务。\n\n描述的黄金法则是『越具体越好，特别是触发条件与边界』。模型在决定『调哪个工具』时，是在比较所有工具的 description 做选择——描述相似的工具体系最容易让模型选错（比如『查天气』和『查机票』都含城市与日期参数时）。用触发条件区分它们，能显著降低选错率。这条经验在 Agent Skills 章节会再次出现：描述的边界就是能力的边界。",
        "case": "一个电商 Agent 同时有『查物流』和『查退款进度』两个工具，用户问『我的快递到哪了』，模型却调了退款工具——因为两个工具描述都只有一句『查询相关订单信息』，模型无法区分。给两个工具分别写明触发条件（『用户询问包裹位置、派送进度时用』vs『用户询问退款到账、审核状态时用』）后，选错率从 30% 降到接近 0。描述不清 = 选错工具 = 任务失败。",
    },
    "s2-tool-c3-s1": {
        "content": "工具参数校验是 Agent 的『第一道防线』：模型生成的参数不一定合法，必须在执行前校验。常见问题：传了空值、类型不对（数字传成字符串）、枚举外的非法值（不存在的城市名）、缺少必填参数。工程上在每个工具入口做统一校验：类型转换（把字符串转成目标类型）、必填检查、枚举/范围校验，校验失败返回友好的错误信息而不是直接抛异常让整个 Agent 崩溃。\n\n好的错误信息要『对模型友好』：返回『城市「北京城」不存在，请检查城市名是否正确』，比抛一个 Python 异常更能帮助模型自我修正——模型看到这个错误，会换一个合法参数重试。参数校验层就像机场安检，宁可多查一道，也不放非法请求进入业务逻辑。",
        "case": "天气工具收到 `city: 上海浦东新区（空）` 这类脏数据。加校验后：非空检查拦截空值；类型检查把数字转成字符串；枚举校验发现城市名不标准时，返回『城市名不规范，建议用标准城市名如「上海」』。模型读到这个提示后自动修正参数重试。有了校验层，Agent 从『偶发崩溃』变得『能自我修正』，稳定性显著提升。",
    },
    "s2-tool-c3-s2": {
        "content": "即使加了参数校验，工具调用仍可能失败——这时候 Agent 要有『重试与澄清』的韧性。两种典型的应对：一是重试——参数校验发现可修正的问题（城市名不标准、时间格式不对），把修正后的参数再调一次，或让模型重新生成参数；二是澄清——信息确实不完整（比如用户没说要查哪个城市的天气），模型可以反问用户『请告诉我要查哪个城市』，而不是硬着头皮用残缺参数调用。\n\n设计原则：可自动修正的优先自动修正（重试），信息缺失不可推断的才澄清（问用户）。两者都要有次数/轮次上限，防止无限重试或反复追问惹恼用户。把『重试与澄清』内建到 Agent 的循环里，是把工具调用的偶发失败，转变成可控、可恢复的常规流程。",
        "case": "用户说『查一下天气』没说城市。差的 Agent 会用空参数调用导致报错；好的 Agent 判断『城市信息缺失且无法推断』，反问『请问要查哪个城市的天气？』。用户补上『深圳』后再正常调用。实测加入澄清机制后，客服 Agent 因参数不全导致的失败率下降 80%，用户体验也更好——该问就问，比瞎试更专业。",
    },
    "s2-tool-c4-s1": {
        "content": "工具执行完拿到结果后，怎么把结果『喂』回模型，直接影响模型的后续判断。核心原则：结构化回传。把工具结果整理成清晰、可读的文本或 JSON 回传给模型——比如 `{\"city\":\"北京\",\"temp\":15,\"weather\":\"晴\"}`，而不是把一串混乱的原始日志丢给模型。结构化让模型能准确读取字段，据此组织回答或继续决策。\n\n同时要给结果『定性』：区分正常结果与错误结果。正常结果直接给数据；错误结果要带说明（『查询失败：服务超时』），让模型知道该重试、改参数还是直接告诉用户。结果回传的另一个要点是控制长度——结果可能很大（比如一个列表），要按需裁剪或摘要，只把与当前问题相关的部分回传，避免撑爆上下文（下一小节详述）。",
        "case": "订单查询工具返回一个大对象（含 20 个字段、含敏感信息）。直接回传会导致上下文臃肿且可能泄露不该给模型看的数据。正确做法：只提取与当前问题相关的字段（订单号、状态、金额），组织成简洁 JSON 回传。用户问『我的订单到哪了』，回传 `{\"status\":\"已发货\",\"物流\":\"上海中转站\"}` 就足够模型回答，既省 Token 又保护了无关数据。",
    },
    "s2-tool-c4-s2": {
        "content": "工具返回的数据可能非常庞大——一个 API 返回上千行列表、一份文档全文——直接回传给模型会迅速撑爆上下文窗口，甚至触发超窗报错。长结果处理的三个策略：一是截断——只保留与当前问题相关的部分（按字段或按行筛选）；二是摘要——用一次模型调用或程序逻辑把长内容压缩成要点；三是分段回传——分批给模型，模型处理一段再要下一段（较少用，复杂）。\n\n实战中最常用的是『按需截断』：让工具本身支持过滤参数（只返回需要的字段/页数），或在回传前程序化裁剪。记住工具结果的『有效信息密度』往往很低——模型只需要那几条关键数据，不需要整张表。管理好回传长度，既省 Token 又避免超窗，是工具调用工程化的基本功。",
        "case": "一个搜索 Agent 的搜索工具每次返回 50 条结果（每条还有冗长摘要）。直接回传会把上下文占满且模型根本读不过来。优化：回传前只保留 top 5 条、每条截断到 100 字。模型同样能准确完成『总结主要观点』的任务，但上下文占用减少 80%，响应速度和成本都显著改善。长结果截断，是让 Agent 在真实数据规模下还能正常工作的关键。",
    },
    "s2-tool-c5-s1": {
        "content": "把前面所有知识串成一个可运行的天气查询 Agent：用 FastAPI（或纯脚本）实现一个完整闭环——定义 get_weather 工具、把工具 schema 交给模型、循环处理『模型发起调用→程序执行→结果回传』、最终输出自然语言回答。这是阶段二最重要的动手练习，因为它验证了你是否真正理解了工具调用的全流程。\n\n实现时建议按四步走：第一步，只跑通单次调用（问题→模型→工具→回答），不处理循环；第二步，加入循环（模型可能连续调用多次工具）；第三步，加入错误处理与澄清（参数非法、信息缺失）；第四步，加日志打印每一步（Thought/Action/Observation），让循环过程可见。跑通后你会获得一个对工具调用闭环的完整、直观、可复现的理解——这是后续所有 Agent 开发的地基。",
        "case": "实现时常见的一个卡点：模型的工具调用请求有时以 JSON 包裹在文本里（`根据结果，我需要调用 get_weather...`），需要你写解析逻辑提取出干净的 `{\"function\":...,\"arguments\":...}`。这个解析看似小，却是『模型输出』到『程序执行』的关键桥接。很多框架（LangChain 等）帮你处理了这一步，但手写一遍能让你真正理解它。",
    },
    "s2-tool-c5-s2": {
        "content": "把天气 Agent 扩展，是巩固工具调用理解的阶梯式练习。第一级：加第二个工具——比如 get_coordinates(城市→经纬度)，让 Agent 学会『根据场景在多个工具中选择』，观察它在两个工具之间如何决策。第二级：加边界处理——城市不存在时怎么兜底回答、工具连续失败几次后怎么止损。第三级：加业务工具——把天气换成真实业务（查订单、算优惠），体会工具调用在真实产品里的样子。\n\n扩展的核心收获是『决策质量』：工具越多，模型选错的概率越大，你越能体会到工具描述与 schema 质量的重要性。这条练习线，正好铺向阶段三的 RAG（把检索器包装成工具）与 LangChain 的 @tool 封装。每扩展一步，你都该追问：模型为什么选对了/选错了？是描述问题还是 schema 问题？",
        "case": "给天气 Agent 加第二个工具 get_flight（查询航班），用户问『北京到上海的高铁』——模型可能误调航班工具。这个『选错』本身就是最好的教学案例：你会去检查两个工具的描述是否足够区分『高铁 vs 航班』，从而理解工具描述边界对决策的决定性影响。把每次选错当成一次优化工具描述的机会，你的 Agent 会越用越准。",
    },

    # ================= s2-memory Agent 记忆系统 =================
    "s2-mem-c1-s1": {
        "content": "LLM 的本质是无状态的：每次请求模型都是从零开始，它『不记得』上一轮的对话。要让 Agent 有连续性，就必须由系统替它『记忆』。业界有三种主流的记忆解法：一是短期记忆——把完整的对话历史作为消息拼进上下文（最简单，但受窗口限制）；二是长期记忆——用向量数据库存储知识/偏好，按需检索相关片段注入（可跨会话、可扩展，但需要检索）；三是结构化记忆——把关键事实（用户订单号、偏好）存入传统数据库，需要时查出来注入（精准但需要设计 schema）。\n\n三者不是互斥而是互补：现实中的 Agent 通常『短期记忆保流畅（历史全量或滑动窗口）+ 长期记忆保积累（向量库存知识与偏好）+ 结构化记忆保精准（数据库存事实）』。理解这三条路线，你就知道为什么阶段二会有专门的记忆章节，也知道后续 RAG 与记忆系统技术同源——都是『外部存储 + 检索 + 注入上下文』。",
        "case": "一个多轮购物助手：用户在第 1 轮说了『我要 4 个』，第 5 轮问『帮我改到 6 个』。短期记忆（历史消息）让模型记得『之前要 4 个』；但如果会话很长，历史可能被截断——此时把『购买数量』这类关键事实提取到结构化存储，随时可查，就避免了『失忆』。三类记忆配合，才能支撑跨多轮的连续服务。",
    },
    "s2-mem-c1-s2": {
        "content": "记忆需要分层设计，不同层次的记忆扮演不同角色。短期记忆（Working Memory）：本次会话内的完整上下文——包括系统提示词、用户输入、模型历史回复、工具调用记录；它决定模型『这一轮对话能否连贯』，特点是量大、需要精打细算、会话结束即作废。长期记忆（Long-term Memory）：跨会话沉淀的信息——用户的长期偏好、历史结论、领域知识；它决定模型『是否了解这个用户、这个领域』，特点是需要持久化存储与检索，能跨会话复用。\n\n设计记忆系统时，最常犯的错是『把什么都塞进短期记忆』——结果上下文爆炸、成本飙升、还会超窗。正确做法是给信息分层：频繁变动的细节放短期（滑动窗口），稳定的偏好与知识沉淀到长期（向量库），关键事实放结构化存储（数据库）。分层的本质，是让每一条信息都待在它最经济的位置。",
        "case": "一个客服 Agent 的改进对比：初版把所有历史都塞进上下文，第 20 轮就超窗；改造后——最近 10 轮对话全量（短期）、用户常见问题与已解决的历史问题摘要存入向量库（长期）、用户的关键身份信息（会员等级、常用地址）存数据库（结构化）。模型既连贯又了解用户，且上下文始终可控。分层不是技术炫技，是让记忆在成本和效果上都可持续。",
    },
    "s2-mem-c2-s1": {
        "content": "短期记忆的工程核心是『上下文窗口管理』——对话越长，历史越多，窗口越不够用。两种主流策略：一是滑动窗口——只保留最近 N 轮对话，最古老的部分直接丢弃；简单有效，但可能丢掉早期的重要信息。二是摘要压缩——把较早的对话用一次 LLM 调用浓缩成一段摘要（如『用户已确认购买 3 个、地址是上海』），把摘要保留在上下文中，丢掉原始消息；信息保留率高，但每次压缩要消耗一次调用。\n\n实战中常两者结合：最近 10 轮全量保留（保证流畅），更早的压缩成摘要（保留要点），必要时再加向量检索从历史中找回具体细节（进阶）。窗口管理的目标，是在『有限的窗口里装下最有价值的信息』——每次添加新消息时都想一想：什么可以留、什么可以压、什么可以扔。",
        "case": "一个 60 轮的长对话客服会话：全部保留必然超窗。用『最近 10 轮 + 更早部分摘要』后，第 60 轮的模型仍记得『用户在第 3 轮说过自己是会员』——因为摘要里保留了这条关键事实。对比初版直接截断到最近 10 轮，用户在第 30 轮报过的会员号被丢掉，模型不得不重复询问，体验下降。摘要压缩保住了关键信息，同时控制了窗口。",
    },
    "s2-mem-c2-s2": {
        "content": "会话隔离是记忆系统的安全底线：不同用户、不同会话之间的对话历史绝不能互相串扰。实现上通过唯一的会话标识（session_id）区分：每个用户、每次会话分配独立 ID，记忆读写都以 session_id 为键。这样用户 A 的历史绝不会出现在用户 B 的上下文里。\n\n隔离的意义不只是隐私——它还保证记忆的正确性（串扰会让模型用 A 的信息回答 B 的问题，产生灾难性错误）和可管理性（每个会话可独立清理、独立审计）。工程实现要点：session_id 通常由用户 id + 会话 id 组成；登录用户用 user_id 维度隔离，匿名/未登录用户也要有临时会话维度；所有记忆存储（消息历史、向量库、数据库）都必须带 session 维度。会话隔离看似基础，却是多用户 Agent 系统不犯糊涂的前提。",
        "case": "一个多用户购物助手，如果没做会话隔离，用户 B 问『我的订单到哪了』时可能读到用户 A 的订单——这是严重的数据事故。加了 session_id（=user_id）隔离后，每个用户只能读到自己的记忆。实测：某团队因早期漏了隔离，线上出现跨用户上下文污染事故，整改后彻底解决。会话隔离不是锦上添花，是必须一开始就做对的基础设施。",
    },
    "s2-mem-c3-s1": {
        "content": "长期记忆的技术底座是『文本向量化 + 相似度检索』。第一步，Embedding：用嵌入模型把每段文本转成一个高维向量，语义相近的文本向量距离近（『如何退换货』和『退货流程』的向量很接近）。第二步，相似度检索：把用户问题也向量化，在库里找距离最近的 K 条（top-k）。第三步，注入：把检索到的相关片段拼进上下文，让模型基于这些『记忆』回答。\n\n这个『向量化—检索—注入』的流程，与阶段三的 RAG 完全同源——长期记忆本质上就是一个『个人知识库 RAG』。区别只在数据来源：长期记忆存的是『这个用户的历史信息』，RAG 存的是『领域文档知识』。理解这一点很重要：你学完记忆系统，等于给 RAG 打了前站；学完 RAG，等于把记忆系统扩展到了任意知识。",
        "case": "让 Agent 记住『用户偏好』：每次对话结束，把用户表达过的偏好（『喜欢简洁回答』『常用顺丰』）向量化存入长期记忆库。下次新会话，用户说『寄快递』，Agent 把问题向量化检索，命中『常用顺丰』这条记忆，自动询问或直接按顺丰处理。这就是长期记忆的闭环——存进去、检出来、用起来。",
    },
    "s2-mem-c3-s2": {
        "content": "Chroma 是入门向量数据库的最佳选择：零配置、本地运行、API 简单。核心用法只有几步：创建 collection（集合，相当于一个向量库）；`collection.add` 把文本、向量（或让 Chroma 自动用内置 embedding）与元数据（metadata，如来源、时间）写进去；`collection.query` 传入查询文本，返回最相似的 top-k 条及相似度分数。元数据是 Chroma 的亮点——你可以在 add 时给每条记忆打上来源、类型、时间标签，query 时按元数据过滤（如只查某一类记忆）。\n\nChroma 适合原型与中小规模（本地几万条记忆完全够用）；生产大规模场景再考虑 FAISS（更快的检索库）、Milvus/Weaviate（分布式向量库）。学 Chroma 的价值在于掌握『向量库怎么存怎么查』的通用心智模型，将来迁移到任何向量库都是换 API 的事。",
        "case": "用 Chroma 存记忆：`collection.add(ids=['mem-1'], documents=['用户偏好顺丰'], metadatas=[{'type':'偏好','user':'A'}])`；查询时 `collection.query(query_texts=['用户喜欢什么快递'], n_results=3, where={'user':'A'})` 返回相关记忆。整个过程本地运行、无需配置数据库，几行代码就能把『记忆的存与取』跑通——这是进入长期记忆开发的快速通道。",
    },
    "s2-mem-c4-s1": {
        "content": "记忆检索不是简单地『把 top-k 都取回来』，而是要设计检索策略提高命中质量。常用策略：一是相关性优先——按向量相似度排序取 top-k，保证内容相关；二是时效加权——对带时间的记忆，近期的重要性更高，检索结果按时间做加权或过滤（比如只看最近 30 天的）；三是元数据过滤——按类型/来源过滤（只查『偏好』不查『历史问题』），缩小检索范围、提升精度；四是数量控制——k 值要适中（太少漏信息，太多稀释注意力和烧 Token），通常 3-5 条起步。\n\n检索质量直接决定『注入的记忆有没有用』——检索到无关记忆不仅没用，还会误导模型。所以记忆系统的功夫一半在『存得对』（结构化、带标签），一半在『检得准』（策略、过滤、加权）。可以用一个简单的回归测试：准备一批标准提问，评估每次检索到的记忆是否命中预期，持续优化检索策略。",
        "case": "一个跨会话偏好记忆库，用户三个月前说过『喜欢幽默回答』，上周又改了『现在要专业正式』。如果不做时效加权，模型可能命中旧偏好，用幽默语气回答，用户觉得不专业。加入时间过滤（优先最近 30 天）+ 类型过滤（只查偏好）后，命中『专业正式』的新偏好，行为立刻正确。检索策略的设计，直接影响记忆是否『用对』。",
    },
    "s2-mem-c4-s2": {
        "content": "记忆的写入与更新，是保证记忆系统不『长歪』的关键。三个原则：第一，提炼写入——不是把原始对话全存进去，而是把有价值的结论、偏好提炼成简短条目再存（如把『用户说上次用中通很慢』提炼为『偏好：快递尽量不用中通』）；第二，更新优先于追加——用户改变了某个偏好或信息，要更新旧记忆而不是再追加一条冲突的（否则检索时新旧冲突，模型无所适从）；第三，去重与清理——重复的记忆要合并，过期的记忆（如一次性的临时需求）要删除。\n\n实现上，可以在每次会话结束时做一个『记忆更新步骤』：让模型从本次对话中提取值得记住的信息、判断与已有记忆的关系（新增/更新/删除），然后程序执行变更。这套『会话末提炼』机制，让记忆库始终简洁、准确、反映用户最新状态。",
        "case": "用户第一次说『默认地址用北京』，第二次改成『搬到上海了』。如果只追加，库里就有两条冲突地址；正确做法是更新——找到旧的『北京』地址条目，更新为『上海』。实测：不做更新的系统在用户改信息后频繁答错（用了旧地址），加更新机制后准确率回到正常。记忆更新不是优化，是刚需。",
    },
    "s2-mem-c5-s1": {
        "content": "一个完整的记忆整合 Agent，把三类记忆协同起来。工作流程分三段：会话开始时——从长期记忆库检索该用户的相关偏好与历史结论，注入 system 上下文，让模型『一开始就了解用户』；会话进行中——短期记忆以滑动窗口保存最近对话，保证多轮连贯；会话结束时——运行『记忆提炼』步骤，把本次对话的新信息（新增偏好、更新的信息、重要结论）写入长期记忆库与结构化存储。\n\n整合的关键是『检索时机』与『写入时机』：检索太早（还没想清楚要什么）或太频繁（每轮都检索，烧 Token）都浪费；写入太粗（存对话原文）或太迟（会话异常中断就丢了）都失效。推荐的最小方案：会话开始检索一次 + 会话结束提炼一次，中间靠短期记忆兜底。这套三阶段整合，是生产级 Agent 记忆系统的标准骨架。",
        "case": "一个跨多天的学习助手：用户周一问了『帮我制定 Python 学习计划』，周二回来说『继续上次的计划』。会话开始时的检索让模型想起周一的计划内容，直接续上；周二结束时提炼『已完成 Python 基础一章』写入记忆。到周三再问『下一步学什么』，模型能基于累积记忆给出连贯建议。长期记忆让 Agent 从『每次重新认识用户』变成『持续服务用户』。",
    },
    "s2-mem-c5-s2": {
        "content": "记忆系统不是免费的，设计时必须权衡成本与隐私。成本侧：长期记忆的存储（向量库、数据库）有资源开销；检索每次调用 Embedding 有 Token 成本；记忆提炼（会话末让模型总结）是额外的 LLM 调用。工程上要控制：检索次数（每会话 1-2 次足够）、记忆条目数量（精华而非全量）、提炼频率（长会话才提炼）。隐私侧：记忆里可能存了用户敏感信息，必须遵循三条底线——敏感信息（密码、身份证号、完整对话）不入库；记忆按用户严格隔离且用户可查看、可删除；符合数据合规要求（隐私政策里说明记忆用途）。\n\n记忆系统的价值与风险并存：它让 Agent 更懂用户，也可能让 Agent 掌握过多用户信息。设计者的责任，是在『记得住』与『守得住』之间找到平衡——只存必要信息、严格隔离、给用户控制权。",
        "case": "一个笔记助手最初把用户全部笔记向量化入库做『跨笔记检索』，虽方便但存在隐私风险（笔记含个人敏感内容）。重新设计后：只索引笔记标题与摘要而非全文，检索只返回引用而非内容，且用户在设置页可一键清除记忆库。功能保留了，隐私风险大幅降低。记忆系统的成熟，既看技术也看它对用户数据的敬畏。",
    },
}
