"""阶段一 · AI 大模型基础 — 小节内容扩充。

38 个小节，每个含：content（详实理论讲解）+ case（实战案例分析）。
"""

SECTION_ENRICH: dict[str, dict] = {
    # ================= s1-llm-basics 大模型基础概念 =================
    "s1-llm-c1-s1": {
        "content": "大语言模型（LLM）的本质，是一台学习过海量文本后学会『接着往下说』的概率机器。它做的事情非常朴素：给定一段上文，预测下一个最可能的词（更准确地说是 Token），然后把这个预测出来的词接回去，再预测下一个，如此循环，直到拼出一整段回复。换句话说，模型并不是在『想清楚答案』，而是在计算『在这个上下文里，最像人话的下一个词是什么』。\n\n为什么这个机制能产生智能的假象？因为训练数据覆盖了几乎所有人类书面表达，模型从中学会了词语搭配、句式、常识与推理模式的统计规律——所以它写出的句子总是通顺的，观点也常是合理的。但必须清醒：它生成的是『最可能』而非『最正确』。这意味着它的回答质量，取决于你提供的信息是否足够、问题是否清晰，而不是它内部有个『真相库』。",
        "case": "让 LLM 写一段月度工作总结，你会发现它能把句子组织得相当专业、结构完整，但一旦涉及具体数字——比如本月销售额——它很可能随口编一个『增长 15%』。这就是『接话机器』的本质：它擅长把话接得漂亮，却不知道你公司的真实数字。开发者在调用时，必须把事实性信息放进提示词或检索上下文里，而不能指望模型『知道』。",
    },
    "s1-llm-c1-s2": {
        "content": "LLM 的能力边界需要一把刻度尺来衡量。它擅长的事包括：文本生成与润色、内容总结、语言翻译、改写与风格迁移、代码编写与解释、头脑风暴、从非结构化文本中提取结构化信息、以及多轮对话。这些任务的共同点，是『不依赖外部事实，只依赖语言模式』。\n\n它不擅长的事同样明确：精确的数学计算（尤其是大数运算）、实时信息（它不知道训练完成之后的事）、事实核查（它会一本正经地编造）、多步且需要严密逻辑的复杂推理（容易在某一步跳错）、以及长上下文中保持前后一致（容易『忘』前面的约束）。理解这条边界，是避免『用错地方』的第一步：能用则用，不能用就交给检索增强、计算引擎或人工。",
        "case": "一家客服团队曾试图让 LLM 直接回答『今天是几号、优惠是否还有效』这类实时问题，结果模型经常给出过时的答案，用户投诉上升。改用『LLM 负责理解意图 + 系统实时查询数据库』的组合后，准确率回到 99%。这个案例说明：把实时事实交给系统、把语言生成交给模型，各司其职。",
    },
    "s1-llm-c2-s1": {
        "content": "注意力机制（Attention）是 Transformer 架构的灵魂，它解决的核心问题是：模型在处理一个词时，应该『看』上下文中的哪些词，以及每个词有多重要。以『苹果』为例，在『苹果是很有营养的水果』和『苹果发布了新手机』两句中，『苹果』的含义截然不同——注意力机制通过计算当前词与句子中每个词的相关性权重，让模型自动聚焦到决定含义的关键词（前者看『水果』，后者看『手机』）。\n\n从工程角度看，自注意力最重要的贡献是『并行性』：不同于 RNN 必须按顺序逐词处理（无法并行、难以记住长距离依赖），Transformer 在计算每个位置时都可以同时参考所有其他位置，因此能在大规模 GPU 集群上高效并行训练。这也是为什么『海量数据 + 更大模型 + 并行算力』能一路推到今天的 GPT、Claude 等千亿级模型。",
        "case": "理解注意力机制，最直观的例子是机器翻译中的歧义消解：把英文『The bank is on the river bank』翻成中文时，Transformer 会让『bank』同时关注『river』，从而正确译为『河岸』而非『银行』。这个『让每个词知道该看谁』的机制，正是大模型理解上下文的基础，也是后面 Agent 开发中『把相关检索结果拼进上下文模型就能读懂』的根本原因。",
    },
    "s1-llm-c2-s2": {
        "content": "从 Transformer 架构到大模型，实际是一条『预训练—指令微调—对齐』的流水线。预训练阶段：模型在海量无标注文本上反复练习『预测下一个词』，学到的是通用的语言知识与世界常识——此时的模型只会接着话头写下去，不一定『听懂人话』。指令微调阶段：用『指令—正确回答』的标注数据继续训练，让模型学会遵循指令、按格式作答，相当于给它补上『与人协作的礼仪』。对齐阶段（RLHF 等）：让模型回答更符合人类偏好——更有用、更诚实、更少有害内容。\n\n这条流水线解释了为什么『提示词』如此重要：指令微调让模型把『你怎么问』和『我怎么答』的对应关系刻进了参数，一个清晰的指令能激活它训练时见过的良好回答模式，而模糊的指令则会触发平庸甚至错误的模式。",
        "case": "开发者常遇到的『换一套说法效果完全不同』，根源就在指令微调：模型在训练时见过大量『请用列表形式总结』『你是一名资深律师』这类指令，一旦你的提示词贴近这些训练模式，输出质量会明显提升。这也是为什么阶段一专门强调 Prompt 工程——它不是玄学，而是通过优化提问方式，让模型命中它被训练出来的协作能力。",
    },
    "s1-llm-c3-s1": {
        "content": "选择模型之前，先对市场格局有个全局观。海外阵营：OpenAI 的 GPT 系列（通用能力均衡、工具调用成熟）、Anthropic 的 Claude 系列（长文本、写作与代码能力出色，上下文窗口大）、Google 的 Gemini（多模态原生，图文理解强）。国产阵营：百度文心一言、阿里通义千问、字节豆包、月之暗面 Kimi（长上下文）、DeepSeek（开源、性价比高、推理能力强）、智谱 GLM（中文语义理解细腻）。\n\n各家模型在中文语境、代码能力、长文本处理、API 价格、合规要求上各有侧重。没有『最好』的模型，只有『最适合当前任务』的模型。作为开发者，重要的是建立『多模型可切换』的能力，而不是押注某一家。",
        "case": "一家出海 SaaS 公司最初只接 OpenAI，后来因成本与合规压力评估国产模型，发现通义千问在处理中文客服工单时准确率不输 GPT，而成本只有约三分之一。他们最终把『代码生成』留在 Claude、把『中文客服』切到国产模型，年省数十万元。这个案例的启示是：不要锁死单一供应商，按任务类型做模型矩阵。",
    },
    "s1-llm-c3-s2": {
        "content": "模型选型没有绝对答案，但有四个可复用的决策维度。第一，任务类型：代码开发优先 Claude 或 GPT；中文创作、文案可选通义、Kimi；数学推理可选 DeepSeek。第二，成本预算：小模型或国产模型显著便宜，高频调用场景要算清单位成本。第三，数据隐私与合规：涉及用户隐私或敏感业务数据时，优先国内合规模型，或干脆走本地部署（见 Ollama 章节）。第四，上下文长度与并发需求：长文档处理需要大窗口模型，高并发需要稳定的 API 服务。\n\n实际落地建议：先用最小可用的模型跑通功能，再根据效果与成本逐项升级——不要一上来就上最强最贵的模型。",
        "case": "做一个中文客服知识库时，团队最初默认用国际头部模型，但评审发现：数据需要境内留存（合规要求）、单次问答成本偏高。改选国产模型后，既满足了数据不出境，单次调用成本也下降了约 60%，而中文问答准确率几乎无差异。选型的本质，是把『能力、成本、合规』三项放到一起权衡，而不是只看名气。",
    },
    "s1-llm-c4-s1": {
        "content": "Token（词元）是文本进入模型之前被切分成的原子单位，也是 API 计费的基本单元。它既不是字也不是词，而是模型分词器（Tokenizer）按统计规则切出的片段：常见英文单词可能一个 Token 搞定（如 hello），生僻词可能拆成多个；中文大致是 1 个汉字约等于 1 到 1.5 个 Token，一个英文字符则可能不到 1 个 Token。\n\nToken 的概念之所以重要，是因为它同时决定了两件事：一是成本——几乎所有商业 API 都按『输入 Token 数 + 输出 Token 数』计费，估算预算前必须先估 Token；二是能力——模型的上下文窗口以 Token 为上限，一段话占多少 Token，决定了你最多能塞进去多少信息。粗略估算：约 1000 个汉字 ≈ 1200～1500 Token，这是做成本预算时常用的换算。",
        "case": "一个调用量较大的应用，若每条请求的 system 提示词有 800 字（约 1000 Token），按 100 万次调用、每千 Token 计价换算，仅提示词的固定开销就相当可观。优化手段很直接：精简 system 提示词、去重、缩短历史——把固定 Token 砍到一半，成本几乎立省一半。凡是『看着不多』的固定 Token，在高频调用下都会被放大成真实账单。",
    },
    "s1-llm-c4-s2": {
        "content": "上下文窗口（Context Window）指模型一次请求最多能容纳的 Token 总数，包括系统提示词、历史对话、检索内容和模型输出。它的意义在于：窗口内的信息模型都能『看到』，窗口外的信息模型完全『不记得』。当输入超出窗口，API 会直接报错（context length exceeded），即使不报错，超出部分也会被截断——模型对『超窗内容』一无所知。\n\n工程上有三种主流应对：滑动窗口（只保留最近 N 轮对话）、摘要压缩（把旧对话浓缩成几句话再放进窗口）、向量检索（把历史或知识库外置，需要时检索相关片段注入）。这三种方法后续都会在记忆系统与 RAG 章节展开，这里先建立『窗口是稀缺资源，要精打细算』的意识。",
        "case": "一个多轮客服 Agent 在对话进行到第 30 轮时突然『失忆』，反复追问用户已经提供过的订单号。排查发现：历史对话把 8K 的上下文窗口塞满了，早期消息被截断。改造为『最近 20 轮全量 + 更早的对话摘要成一段』后，模型既能记住关键信息，也不再触发超窗报错。这就是滑动窗口 + 摘要压缩的典型应用。",
    },
    "s1-llm-c5-s1": {
        "content": "幻觉（Hallucination）指模型生成看起来合理、实则与事实不符的内容——它不是在撒谎，而是在『概率上最顺』的地方补上了不存在的细节。产生幻觉的根本原因是：模型学到的是语言规律而非真实世界，它无法区分『它知道的』和『它编的』。知识过时则指模型训练数据有截止日期，训练之后发生的事它一概不知。\n\n应对幻觉没有银弹，但有组合拳：一是给足上下文（把事实资料通过提示词或检索放进输入，让模型『照着说』）；二是要求引用（让模型标注回答依据，无法引用就明确说不知道）；三是对关键信息做程序化校验（价格、日期、库存这类，直接查系统而不是让模型凭记忆回答）。开发 Agent 时尤其要把这三条写进系统提示词。",
        "case": "某资讯产品上线初期让模型直接总结最新政策，结果多次输出过期或编造的内容。改造方案是『检索优先』：先向量检索相关政策原文，再让模型『仅基于检索到的内容回答，找不到就说明』，同时在回答尾部附上原文链接。上线后错误率从 12% 降到 1% 以下。这正是 RAG（检索增强生成）的雏形，也是阶段三的重点内容。",
    },
    "s1-llm-c5-s2": {
        "content": "LLM 的两类固有局限需要开发者心里有数。第一类是推理上限：面对需要多步严谨推导的任务——复杂数学、长链条逻辑、需要遵守多个相互制约的约束——模型容易『跳步』或『自相矛盾』。这不是它不聪明，而是自回归生成的每一步都只看前面，长链推理的误差会累积。第二类是偏见与倾向：训练数据中潜藏的刻板印象、地域或职业偏见，会以『看起来理所当然』的方式出现在输出里，且模型难以自我察觉。\n\n工程应对：复杂推理任务先拆成多步（分步提问、逐步验证，呼应后面的思维链），对高风险场景（招聘、金融、医疗）加规则校验与人工兜底，敏感话题的输出做审查。记住：把 LLM 当作『聪明的实习生』而不是『可靠专家』，重要决策永远需要校验环节。",
        "case": "一个 AI 招聘助手在筛选简历时，多次对女性候选人的『稳定性』给出负面倾向的自动评价。整改后：涉及这类判断的输出全部改为『仅呈现客观事实，不下倾向性结论』，并增加合规人工复核。这个案例提醒我们：偏见是训练数据的投影，系统设计者必须主动加护栏，而不能假设模型『中立』。",
    },

    # ================= s1-prompt 提示词工程 =================
    "s1-prompt-c1-s1": {
        "content": "一条高质量提示词，本质上是一份『给 AI 的清晰任务书』，核心是三要素。第一，任务动词明确：用『总结』『改写』『生成』『分类』『提取』这类具体动词告诉模型要做什么，而不是含糊的『帮我看看』。第二，背景上下文充分：模型对你一无所知，你需要的业务背景、读者对象、已有材料都要给足，模型才能做出贴合的产出。第三，输出格式具体：字数、列表或表格、标题层级、语气风格，写得越具体，越能约束模型输出符合预期。\n\n三要素背后是同一个原理：模型在训练中见过大量『清晰指令 → 良好回答』的对应，你的提示词越接近训练中的优质样例，越能激活优质输出。反过来，只给一句『写得好一点』，模型只能猜你想要什么，结果必然是平庸的。",
        "case": "对比两条提示词：『帮我写个周报』vs『请以销售岗视角，把本周完成的 A 客户续约、B 客户方案提交、C 客户流失预警三项工作，写成 3 条要点式周报，每条不超过 40 字，语气简洁专业』。前者得到的是一段正确的废话，后者直接可用。差距不在模型，而在任务书的质量——这就是 Prompt 工程的第一课。",
    },
    "s1-prompt-c1-s2": {
        "content": "Prompt 效果差的常见根因是『模糊』与『期望错位』。模糊的表现：『写得好一点』『优化一下』『帮我看看这段』——模型不知道好是什么标准、优化哪个方向、看什么问题，只能按默认的『平均水准』输出，结果自然不痛不痒。期望错位的表现：你想让它给你把关，它却直接改写；你想让它输出 3 条，它给你 10 条——因为你没说清楚边界。\n\n破局的方法是『把期望量化』：长度（多少字、多少条）、风格（正式/口语/幽默）、受众（给领导看/给新人看）、语气（专业/亲和）、禁忌（不要用术语/不要承诺效果）。写提示词时，想象你是在给一个能力很强但毫不知情的同事布置任务——凡是你能想到的上下文和边界，都值得写进去。",
        "case": "运营同学让 AI 帮忙优化一条活动文案，提示词只有『优化一下』，结果 AI 把口语化的文案改成了官腔，适得其反。加上约束后：『保持原稿语气和长度，把卖点「满 100 减 20」提前到第一句，结尾加一句限时紧迫感』，产出一版直接可用。模糊指令的代价不是模型变笨，而是它替你做了你本想说清楚的选择。",
    },
    "s1-prompt-c2-s1": {
        "content": "Few-shot（少样本学习）的核心思想：与其用文字描述『我要什么格式』，不如直接给模型 2 到 3 个『输入—输出』示例，让它模仿。示例比规则更强大，因为示例同时传递了格式、风格、推理方式和边界。比如要让 AI 把客户差评分类，与其写『分为物流、质量、态度三类』，不如给三条已经标好的例子：『①快递太慢——物流 ②外包装破损——质量 ③客服爱答不理——态度』。\n\n写示例要讲究：示例要覆盖正常情况，最好再补一个边界情况（如同时涉及两类的怎么处理），否则模型只会照抄你给的例子，遇到没见过的形态就乱分类。示例本身就是上下文的一部分，占用 Token，所以贵精不贵多——选最典型、最能界定边界的样例。",
        "case": "一个电商团队用 Few-shot 让 AI 自动给差评打标。最初只用文字描述分类规则，准确率约 70%；加入 5 条带标签的真实差评作为示例（含一条『快递慢但客服态度好』的混合情况），准确率跃升到 92%。示例让模型『看到了』规则具体长什么样，而不是靠它自己猜规则的含义。",
    },
    "s1-prompt-c2-s2": {
        "content": "Few-shot 示例不是免费的：每一条示例都会占用上下文窗口的 Token，并在按量计费时增加成本。示例越多，模型输出的稳定性越好，但成本也越高；示例太少，则起不到界定边界的作用。这是一个典型的『质量—成本』权衡。\n\n取舍的原则是：只保留真正必要的示例。三条经验——一是优先选『能界定边界』的示例（覆盖正常情况和易混情况），而不是选『内容精彩』的示例；二是示例要尽量精简，去掉无关细节；三是如果一个任务用一条规则描述就能说清，就不要用示例——规则比示例更省 Token。实践中，先用规则描述 + 1 到 2 条示例起步，效果不稳再加示例，边加边看成本。",
        "case": "一个自动生成 SQL 的 Agent，最初放了 8 条查询示例，单次请求 Token 开销大、延迟高。分析后发现多数示例是重复的 SELECT 形态，最终精简为 3 条（普通查询、多表连接、带条件过滤各一），Token 减少一半以上，准确率没有下降。示例的边际收益会递减——多出来的那些，往往是浪费。",
    },
    "s1-prompt-c3-s1": {
        "content": "思维链（Chain of Thought，CoT）的原理，是把模型的『思考过程』外显出来：不让它直接给答案，而是要求它先写下推理步骤，再给出结论。直接要答案时，模型容易在几层推理之间『跳步』——它知道下一步该怎么做，却常常跳过中间环节直接说出一个看似合理实则错误的结论。而一旦强制它把每一步写出来，每一步都基于上一步的实际输出，错误就被拦在早期，而不是在最后一步突然出现。\n\n思维链之所以有效，还有一个工程原因：模型在训练时见过大量『分步骤推导』的文本，触发它用这种模式思考，相当于唤醒了它更可靠的处理能力。需要注意：只有任务真的需要多步推理（数学、逻辑、规划）时，CoT 才有价值；简单任务用 CoT 只会白白增加输出 Token 和延迟。",
        "case": "在数学应用题上，直接让模型输出答案的准确率约 60%；改提示词『请先列出已知条件，再分步骤计算，最后给出答案』后，准确率提升到 85%。同样的题，只是多了一句『一步步来』，结果天差地别。这就是把隐式的思考过程显式化带来的收益，也是后续 ReAct Agent『推理—行动—观察』循环的思想源头。",
    },
    "s1-prompt-c3-s2": {
        "content": "思维链有三种落地姿势，按成本从低到高排列。第一种是零样本触发：不需要任何示例，只在提示词末尾加一句『让我们一步步思考』或『请先推理再回答』，就能让模型进入分步模式——成本最低，效果随任务复杂度递增。第二种是给一条示例思路：在 Few-shot 示例中附上『思考过程 + 最终答案』，让模型照这个范式推演，稳定性和可控性更高，但占用更多 Token。第三种是结构化输出：明确要求模型把输出分成『思考过程』与『最终答案』两个部分（甚至用 JSON 承载），便于程序解析和后续校验。\n\n使用建议：能零样本触发就不加示例；需要更稳定的推理才上示例；要做程序化校验（比如 Agent 里判断推理是否合理）就一定要结构化。同时记住，CoT 不是万能的——它只对『推理型任务』有意义，对纯事实问答、创意写作，强制分步反而画蛇添足。",
        "case": "在 Agent 开发中，想让模型输出既推理又结论且能被代码读取，最稳妥的做法是结构化 CoT：要求模型返回 JSON，含 steps（推理步骤数组）与 answer（最终答案）两个字段。这样程序既能展示推理过程给用户看，也能对 answer 做进一步校验。实测在需要多步工具调用的任务里，结构化 CoT 比自由输出减少了约 40% 的无效重试。",
    },
    "s1-prompt-c4-s1": {
        "content": "角色扮演（Role Prompting）之所以有效，是因为指令微调让模型学会了『不同身份对应不同知识域与语气』。当你给它一个专业角色，就相当于告诉它：调用你训练时见过的『这个职业该怎么说话、该关注什么』的整套模式。写角色设定的四件套是：身份（你是一位资深后端面试官）、经验年限（有 10 年大厂经验）、专业领域（专注高并发系统）、输出语气（专业、结构化、由浅入深）。\n\n写法上有几个要点：角色越具体越好——『资深律师』不如『擅长劳动仲裁的资深律师』；把角色的『立场』也说清楚——『站在新手角度把技术讲明白』能激活它当老师的模式；角色设定与任务指令分开写，逻辑更清晰。角色扮演的边界是：它改变的是输出风格与关注点，不会凭空增加模型没有的知识——如果它本身不懂，角色再资深也编不出来。",
        "case": "让 AI 生成面试题，『帮我出 5 道题』得到的是泛泛而谈；改成『你是一位有 10 年经验的 Java 后端面试官，擅长分布式系统，请为招聘一名高级开发出 5 道渐进难度的题，并附上考察要点』，得到的题目立刻专业、有梯度、可评审。同一个模型，只是多了一个精准的角色设定，产出质量判若两个产品。",
    },
    "s1-prompt-c4-s2": {
        "content": "场景化约束是角色扮演的延伸：不仅告诉模型『你是谁』，还要告诉它『你面对的是谁、处于什么场景』。同样是讲解一个技术概念，给 12 岁孩子讲要用生活化比喻、给程序员讲可以直接上术语——受众不同，模型默认的输出方式就不同。场景化约束通常包括：目标读者（给谁看）、场景（工作报告/教学/营销/评审）、风格限制（不要用术语/幽默一些/控制在两页内）、禁忌（不要承诺效果/不要出现敏感词）。\n\n把这些写进提示词，本质是在替模型框定『回答的可行域』。没有约束时，模型会自己选一个『最平庸的默认』——通常是通篇官腔或大而化之；有了约束，它才会真正为你这个具体场景生产内容。这也是为什么专业 Prompt 往往像一份『需求说明书』，而不是一句话。",
        "case": "同一份产品说明，要输出给『投资人看的融资摘要』和『给客服看的操作手册』，内容重点完全不同。用场景化提示词分别约束受众与形式后：前者聚焦市场规模与壁垒、用数据说话；后者聚焦操作步骤与异常处理、用编号清单。模型没有被换，只是被准确告知了『谁在看、看什么』，产出的价值就完全不一样。",
    },
    "s1-prompt-c5-s1": {
        "content": "写 Prompt 应该像写代码一样迭代，而不是一次成型。标准流程是四步循环：第一，写一版快速跑通——别追求完美，先拿到一个基线结果；第二，诊断——逐项对照输出看哪里不符合预期，是内容不全、格式不对、风格不对，还是逻辑错了；第三，针对性修改——结果不全就补充要求，格式乱就加示例，风格偏就加角色与语气约束，每次只改一个点；第四，记录对比——保留改动前后的版本，验证这次的修改是否真的有效。\n\n迭代中最容易犯的错是『一次改太多』：同时改了三四处，效果变好了却不知道是哪一处的功劳，下次无法复用。专业做法是每次只改一处、用同一组测试输入验证，把 Prompt 当作品质工程来管理。",
        "case": "一个团队花了两周优化客服回答提示词，每次都『哪里不对就顺手改几处』，效果却原地踏步。改用迭代方法论后，他们建立了一个含 20 条标准问题的评测集，每次只改一个维度并跑评测：先补输出格式（准确率提升）、再补角色设定（语气改善）、最后补边界说明（拒答率下降）。两周内各项指标显著提升，且每次改动都可追溯。Prompt 优化没有捷径，但有方法论。",
    },
    "s1-prompt-c5-s2": {
        "content": "提示词模板化，是把验证有效的提示词沉淀为可复用的『参数化模板』：把固定部分写死，把会变化的部分用占位符（如 {主题}、{受众}、{数据}）留出来，调用时传入实际值。好处有三：一是复用——同一套已验证的提示词结构，套不同的主题/受众就能批量生产；二是稳定——模板里的优质部分被固化下来，不会每次重写时走样；三是协作——模板成为团队资产，新手也能直接套用成熟 Prompt。\n\n模板化的进阶是分层设计：把『系统级约束』（角色、输出格式、安全边界）与『任务级内容』（本次要处理的具体数据）分开，前者基本不动，后者按次填充。这套思想与后面的 Agent Skills（能力技能化）一脉相承——你现在沉淀的每个模板，都是未来 Skill 的雏形。",
        "case": "内容团队每天要产 20 条小红书文案，最初每条都从零写提示词，质量参差。把验证过的模板固化后：`【角色】资深小红书运营【任务】围绕{主题}写一条种草文案【格式】标题≤20字+正文200字+3个话题标签【风格】口语化、有代入感`，每天只需替换{主题}。一周后文案的平均互动数据提升了约 30%，且新人也能稳定产出。模板化让优质提示词从『个人手艺』变成了『团队资产』。",
    },

    # ================= s1-api-dev 大模型 API 调用与 AI 开发规范 =================
    "s1-api-c1-s1": {
        "content": "把网页对话变成代码调用，是从『使用 AI』跨入『开发 AI 应用』的第一步。OpenAI 风格的 Chat Completions 接口是事实标准：向 `/chat/completions` 发一个 JSON 请求，包含 `model`（模型名）和 `messages`（对话消息数组），响应里的 `choices[0].message.content` 就是模型的回答。messages 数组是核心，它是一份完整的对话记录：每条消息有 `role`（system/user/assistant）与 `content`（内容）。\n\n最小的可用调用只有三行核心逻辑：构建 messages、发送请求、取出 content。建议用官方 SDK（如 openai Python 包）而非裸 HTTP，SDK 帮你处理了 URL、鉴权、重试等细节。把第一次调用跑通后，再逐步加入多轮对话、参数调整与错误处理——你写的不是『一次请求』，而是『一个会对话的程序』。",
        "case": "新手常见的第一个坑是只发一条 user 消息，模型答完就结束，无法延续上下文。正确姿势是把历史对话全部放进 messages：第一轮发 [system, user]，第二轮发 [system, user, assistant, user]，让模型『看到』完整的聊天记录。很多『多轮对话突然失忆』的问题，根源就是每次只发了最后一条消息，而不是完整的 messages 数组。",
    },
    "s1-api-c1-s2": {
        "content": "messages 数组中三种角色的分工，决定了模型如何理解每一次请求。system 消息设定行为基调——相当于给模型一个『岗位说明书』，规定它是什么角色、遵循什么规则、如何输出；它在整个对话中通常只出现一次，位于最前面。user 消息是用户输入——来自用户的提问或指令。assistant 消息是模型的历史回复——把前几轮模型的回答放回去，模型才能基于自己的上一轮输出继续，这是多轮对话的记忆基础。\n\n工程要点：system 提示词是注入业务规则和约束的最佳位置（角色、输出格式、安全边界都放这里），user 消息则只承载本次要处理的具体内容。把『固定的规则』放 system、把『变化的数据』放 user，是结构化对话的标准做法。另外注意，system 提示词也会被计入 Token 成本，要写得精炼。",
        "case": "做一个法律咨询问答时，正确的结构是：system 里写『你是一名严谨的法律助理，仅基于中国现行法律法规回答，不确定时明确说明并建议咨询律师，不使用绝对化表述』；user 里放用户的具体问题。这样同一套 system 约束可以服务海量不同问题，既保证了合规边界，又避免了在每条 user 消息里重复贴规则导致 Token 浪费。",
    },
    "s1-api-c2-s1": {
        "content": "国内大模型平台在接口设计上有一个巨大的便利：绝大多数提供 OpenAI 兼容接口。这意味着你只需要在 SDK 里改两个配置——`base_url`（换成该平台的网关地址）和 `api_key`（换成该平台的密钥）——就能把代码从 OpenAI 无缝切到通义、智谱、DeepSeek 等。对开发者而言，这大大降低了切换和混用模型的门槛。\n\n兼容不是 100% 等同：各平台在模型命名、某些参数支持度、限流策略上仍有差异，但核心的 Chat Completions 流程是通用的。开发时建议把『模型名 + base_url + key』作为配置项抽离，业务代码不写死具体供应商——这样后续换模型、加模型都是配置变更，而不是代码重构。这也是 Agent 开发中『多模型路由』的基础。",
        "case": "一个项目最初只用 OpenAI，后来因成本与备案考虑切换国产模型，改造量小到惊人：只改了 base_url 和 api_key 两处配置，业务代码一行没动就切到通义千问跑通。这充分说明国产平台采用 OpenAI 兼容标准带来的工程红利——多供应商切换应该像换一个配置项一样简单，而不是推倒重来。",
    },
    "s1-api-c2-s2": {
        "content": "多模型接入抽象，是为了让业务代码与具体模型解耦而做的一层封装。核心是一个工厂模式：外部传入一个模型配置（名称、base_url、api_key），工厂返回一个统一的调用接口（通常是 `chat(messages) -> str`）。业务代码只依赖这个统一接口，完全不感知底层用的是哪家、哪个模型。\n\n抽象带来的直接收益：一是可切换——同一套代码，切换模型只改配置；二是可降级——主模型不可用或超预算时，自动切换到备用模型（降级策略）；三是可对比——给不同模型配不同配置，A/B 测试效果。进阶方案是接入模型路由层（model router）：根据任务类型（简单问答用便宜小模型、复杂推理用强模型）、成本预算、并发状况动态选模型。注意抽象要克制——不要把各家独有的能力（比如某家的特殊参数）强行抹平，必要时允许透传。",
        "case": "一个 AI 助手需要同时服务『快速问答』（高并发、低成本）和『深度推理』（低并发、高质量）两种场景。通过统一的模型抽象层，他们配置了两条路由规则：普通问题走国产小模型（成本低、响应快），复杂推理问题走强模型（质量高）。上线后整体成本下降约 50%，而深度推理场景的质量不受影响。抽象层让『按需选模型』成为可能。",
    },
    "s1-api-c3-s1": {
        "content": "temperature 和 top_p 是两个控制输出随机性的参数，理解它们能让你把模型调成『稳定输出器』或『创意生成器』。temperature（温度）：取值 0 到 2，数值越高，模型越敢于选择概率低的词，输出越发散、有创意；越低则越保守，总是选最高概率的词，输出更稳定、更可复现。top_p（核采样）：按概率从高到低累加，覆盖到 top_p 比例的候选词范围内采样——0.1 表示只从概率最高的 10% 的候选里选，越高则候选越多。\n\n两者都能控制随机性，但不要同时调——固定一个（通常是 temperature），调另一个即可。实用建议：写代码、提取结构化信息、做事实问答用低温（0～0.3），保证稳定准确；创意写作、头脑风暴、起标题用高温（0.7～1.0），让输出多样；大多数任务用默认值（通常 0.7 或 1）起步再微调。",
        "case": "一个自动生成订单摘要的功能，默认 temperature=1，结果同一订单每次生成的摘要措辞都不同，甚至偶尔出现语病，下游解析出错。把 temperature 调到 0.2 后，输出趋于稳定一致，解析错误率几乎归零。反过来，一个营销文案生成器把 temperature 从 0.3 提到 0.9 后，文案风格明显更鲜活，A/B 测试点击率提升。同一参数，用对场景就是利器。",
    },
    "s1-api-c3-s2": {
        "content": "max_tokens 和停止符（stop）共同约束模型的输出。max_tokens 限制生成的最大长度——它不仅控制输出规模，还直接决定成本上限（输出按 Token 计费）和响应延迟（生成的 Token 越多越慢）。设置时要注意：max_tokens 是『本次生成的最大值』，不是『必须生成的量』，模型可能提前停止（比如它觉得回答完了）。如果 max_tokens 设得太小，长回答会被硬生生截断，出现『话没说完』。\n\nstop 序列则是让模型在遇到指定字符串时提前停止生成——比如让模型输出到『###』或『}』就停下，常用于结构化输出的截断控制。两者的配合用法：给输出一个合理的 max_tokens 上限防止失控（配合成本预算），再用 stop 精确定位生成终点。注意 max_tokens 只算输出，输入的 Token 由上下文决定，别把两者混淆。",
        "case": "一个让 AI 生成 JSON 的功能，最初 max_tokens 设得很大且没设 stop，结果模型偶尔会在 JSON 之后继续输出解释文字，导致解析失败。改造：把 max_tokens 收紧到业务所需上限，并加上 stop=['}']，模型输出到 JSON 结束就停止。解析成功率从 90% 提升到 99%+，单次调用成本也随之下降——输出被精确控制，不再浪费。",
    },
    "s1-api-c4-s1": {
        "content": "流式输出（Streaming）让模型逐字或逐块地返回结果，而不是等全部生成完才一次性返回。实现上是在请求里加 `stream=True`，模型就会以流（SSE，Server-Sent Events）的形式推送增量 chunk，客户端收到一个就渲染一个，产生『打字机』效果。\n\n流式输出有三个价值：一是体验——用户几乎立刻看到第一个字，感知延迟从『等十几秒』降到『等一两秒』，尤其对长回答提升明显；二是进度感——用户能看到内容在生成，减少等待焦虑；三是架构——流式是构建『实时 Agent』的基础（Agent 边推理边输出过程）。实现要点：把流式推送接到 HTTP 响应流（FastAPI 的 StreamingResponse）或 WebSocket，前端逐块渲染；注意流式时错误也可能分块到达，错误处理要兼容流式形态。",
        "case": "把一段 800 字的分析从非流式改成流式后，用户的『等待感知』大幅下降：虽然总时长几乎不变（约 10 秒），但用户在第 1 秒就看到开头，等待焦虑明显缓解，用户留存与好评显著提升。对需要长时间生成的 Agent 场景，流式几乎是标配——让用户『看着它在思考』，比让用户面对空白屏幕等 10 秒，体验完全是两个量级。",
    },
    "s1-api-c4-s2": {
        "content": "真实世界没有永远稳定的 API，错误处理是上线前的必修课。常见错误分四类：一是 429（限流）——请求太频繁，需要退避重试（如指数退避：等 1s、2s、4s 后重试）；二是 4xx 参数错误——请求本身有问题（模型名不对、参数非法），重试无用，要修正代码；三是超时——模型响应太慢，加大 timeout 或把请求拆小；四是 context length 超限——输入超过窗口，需要截断历史或压缩上下文。\n\n工程上要建立『失败分层』：区分可重试（限流、超时）与不可重试（参数错、鉴权失败），对可重试的做有限次退避重试，对不可重试的立即报错并记录；同时给每个调用设超时上限，防止慢调用拖垮整个请求。一个健壮的调用层，能让应用在模型波动时依然稳定。",
        "case": "一个高峰期的客服 Agent 频繁收到 429 限流，导致用户看到超时错误。排查后做了三件事：给所有调用加 30 秒超时、对 429 做指数退避（最多 3 次）、限流集中时段自动切换到备用模型。改造后请求成功率从 85% 回升到 99.5% 以上。教训很简单：模型 API 会波动，调用层必须做好『失败不是异常而是常态』的准备。",
    },
    "s1-api-c5-s1": {
        "content": "API Key 是访问模型的凭证，泄露 = 账户被冒用 + 账单失控。最安全的做法是绝不把 Key 写进代码，而是通过环境变量注入：把 Key 放在项目根目录的 `.env` 文件里，用 `python-dotenv` 在启动时加载进环境变量，代码里从 `os.getenv('API_KEY')` 读取。同时把 `.env` 加进 `.gitignore`，确保它永远不会被提交到仓库。\n\n几个关键纪律：第一，任何提交到 Git 的代码都不能含明文 Key——提交前检查一遍，历史里出现过 Key 的要立刻轮换；第二，生产环境用真正的密钥管理（云厂商的 Key Vault / 环境变量注入），而不是共享的 .env；第三，给 Key 设权限与预算——只授权必要的模型、设置调用上限，防止被盗用后产生巨额账单；第四，定期轮换 Key。代码里硬编码 Key，迟早会出事，只是时间问题。",
        "case": "有开发者把含 OpenAI Key 的 `.env` 误提交到公开 GitHub 仓库，几分钟内就被爬虫扫描到，账户在一夜之间产生数千美元调用费用。这类事故完全可以避免：只要把 .env 写进 .gitignore、提交前自查、并用密钥管理服务轮换一次 Key。安全不是可选项，是工程的基本盘。",
    },
    "s1-api-c5-s2": {
        "content": "工程化的脚本结构，核心是『输入—处理—输出』三分离。输入阶段负责读取数据与配置（文件、参数、环境变量）；处理阶段是核心业务逻辑（调用模型、处理结果）；输出阶段负责落盘或返回（写文件、打印、写数据库）。三阶段分离让每个部分都可以独立测试、独立修改，也符合『函数只做一件事』的原则。\n\n依赖管理是让项目『换台机器就能跑』的关键：用虚拟环境（venv 或 conda）隔离项目依赖，用 `requirements.txt`（或 pyproject.toml）固定依赖清单，用 `pip freeze` 或锁定版本号保证可复现。原则是『环境是可再生的』——任何新人或 CI 拿到代码，`pip install -r requirements.txt` 就能跑起来，而不是靠某台机器的运气。",
        "case": "接手一个 AI 项目时发现它『只有作者本人能跑』——依赖没固定版本，Python 环境混乱，脚本逻辑和输出混在一起。重构为三分离 + requirements.txt + venv 后，任何同事 clone 下来 3 分钟就能跑通。工程化的成本是当下的几分钟整理，收益是团队协作和长期维护的顺畅。",
    },

    # ================= s1-ollama Ollama 本地模型部署 =================
    "s1-ollama-c1-s1": {
        "content": "本地模型与云端 API 是互补而非对立的两条路线，选型依据是场景对『成本、隐私、能力、运维』的权重。本地模型（如 Ollama 拉取的 qwen2.5）的优势：免费且无限调用（不按 Token 计费）、数据完全不出本地（隐私与合规安全）、可离线运行（断网也能调试）、响应无网络延迟。劣势：能力上限低于顶级云端模型（尤其长文与复杂推理）、需要硬件（显存）支撑、并发能力有限。\n\n云端 API 的优势：能力更强、并发稳定、免运维（不用管 GPU）；劣势：按量计费、数据要出境（合规风险）、依赖网络。最务实的策略是『本地开发，云端上线』：开发期所有 Demo 先本地跑通（免费、快速、隐私），上线时再切到云端大模型。",
        "case": "一个做企业知识库的项目，开发期团队 5 人每天要跑上百次问答调试，若都用云端 API 成本不菲且数据敏感。他们把开发环境切到本地 Ollama（qwen2.5 系列），调试零成本、数据不出内网；上线后切到云端商用模型，兼顾能力与稳定。两种环境共用同一套 OpenAI 兼容接口，切换只改一行配置。这就是『本地开发、云端上线』的标准姿势。",
    },
    "s1-ollama-c1-s2": {
        "content": "把本地模型放进你的开发工作流，核心价值是『用极低的边际成本反复试错』。在阶段三的 RAG 与 Agent 开发中，你会反复跑同一套代码调试效果——向量检索是否命中、工具调用是否走对、提示词是否有效。如果每次都调用云端 API，调试成本（钱 + 限流 + 延迟）会让人不敢大胆实验；而本地模型让你可以毫无顾忌地跑、改、再跑。\n\n定位上有两点提醒：本地模型是用来『验证逻辑』的，不是用来『对比最终效果』的——逻辑跑通了，再切云端模型看真实效果，两者能力差异不影响流程验证；同时本地模型也能用于生产（数据敏感或成本敏感场景），但要评估它的能力是否达标。实践顺序建议：先装好 Ollama 拉一个模型，把本阶段所有 Demo 都在本地跑通。",
        "case": "一位学员在做客服 Agent 时，最初用云端模型调试，每次跑一遍要等 5-10 秒还担心限流，调试节奏被严重拖慢。切换到本地 Ollama 后，同样代码几乎瞬时返回，他把大量时间花在『调提示词、调工具描述』上，Agent 效果快速迭代成熟；最终切回云端模型上线时，由于逻辑已验证，切换几乎没有返工。本地模型最大的价值，是让你敢于高频实验。",
    },
    "s1-ollama-c2-s1": {
        "content": "Ollama 让本地跑大模型变得像装普通软件一样简单。安装完成后，核心命令只有几个：`ollama pull <模型名>` 下载模型（如 `ollama pull qwen2.5:7b`）、`ollama run <模型名>` 启动并进入对话（也可直接跟一句问题做一次问答）、`ollama list` 查看本地已下载的模型、`ollama ps` 查看当前运行中的模型、`ollama stop` 停止运行。默认服务地址是 `http://localhost:11434`，启动后即常驻一个本地 API 服务。\n\n注意事项：首次 pull 需要下载几个 GB 的模型文件，取决于网络；运行时会占用内存/显存（7B 量化模型约需 4-6GB 内存），机器配置不够可以选更小的模型（如 qwen2.5:3b 或更小的量化版）。命令行的 `ollama run` 主要用于快速体验和测试，真正的程序调用走 API 接口（下一小节）。",
        "case": "首次上手最快路径：`ollama pull qwen2.5:7b` 下载完成后，`ollama run qwen2.5:7b` 直接进入对话，输入『用一句话解释什么是大模型』体验一下本地对话的即时响应。之后 `ollama list` 能看到 qwen2.5:7b 已在本地。整个过程中数据完全不出你的电脑——这正是本地部署对隐私敏感场景的核心价值。",
    },
    "s1-ollama-c2-s2": {
        "content": "选本地模型，本质是在『能力、显存、速度』三者间取平衡。参数规模是关键变量：7B 左右的模型（如 qwen2.5:7b）适合日常开发调试，能覆盖多数文本任务；13B/14B 在代码与推理上更强，但需要更多显存；32B 以上接近云端中小模型的能力，但对硬件要求高。量化（Quantization）是控制显存的另一把钥匙：把模型权重压缩（Q4、Q8 分别表示 4bit、8bit 量化），能显著降低显存占用和加载速度，代价是精度轻微下降——对开发调试场景，Q4 量化完全够用。\n\n实用建议：显存 8G 以内选 7B 的 Q4 量化版；16G 可上 14B；32G+ 可尝试更大模型。先用小模型把流程跑通，再按需升档。注意 Ollama 的模型命名里常含量化标识（如 `:7b-q4_K_M`），按需选择。",
        "case": "一位学员的笔记本只有 8GB 显存，直接拉 14B 模型导致运行极慢甚至 OOM。改为 `qwen2.5:7b-q4_K_M`（7B 的 4bit 量化版）后，响应速度提升数倍，占显存约 5GB，日常调试流畅。他后来换了 24GB 显存的机器，才升级到 14B 模型追求更好的代码质量。选模型不是越大越好，而是『够用 + 跑得动』。",
    },
    "s1-ollama-c3-s1": {
        "content": "Ollama 最大的工程便利，是默认提供 OpenAI 兼容的 HTTP 接口：服务地址 `http://localhost:11434/v1`，几乎可以直接套用你写过的 openai SDK 代码——只改 base_url 和 api_key（随便填个占位值即可）。这意味着『本地模型』与『云端 API』在你的代码里几乎无缝切换，这是 Ollama 能融入真实开发工作流的根本原因。\n\n除了 OpenAI 兼容层，Ollama 还提供原生接口：`POST /api/chat`（对话）、`POST /api/embed`（向量化）等，功能更全、更贴近 Ollama 原生能力。实际开发建议：统一走 OpenAI 兼容接口（代码与云端一致），只有用到 Ollama 特有功能（如原生流式、多模态）时才用原生接口。",
        "case": "一套已经用 openai SDK 写的代码，切换到本地模型只改了 2 行：`base_url='http://localhost:11434/v1'` 和 `api_key='ollama'`（占位）。同样的 `chat.completions.create()` 调用，从云端无缝切到本地。这也是为什么学习路线反复强调『开发用本地、上线用云端』——因为切换成本低到可以忽略，你可以在两种环境之间自由来回。",
    },
    "s1-ollama-c3-s2": {
        "content": "Ollama 的原生 API 在需要精细控制本地推理时更有价值。`POST /api/chat` 可以直接传入 messages 并设置 stream=true 获得流式返回；`POST /api/generate` 适合单轮生成。流式在本地同样重要：本地模型虽然快，但长回答仍然需要时间，流式让前端能实时渲染，体验更好。\n\n原生接口与 OpenAI 兼容接口的取舍：兼容接口胜在与云端代码一致、生态工具好接入；原生接口胜在参数更贴近 Ollama（如 keep_alive、format 等），以及直连本地服务少一层转换。个人建议：90% 的场景用 OpenAI 兼容接口，遇到『需要原生能力』（如更细粒度的推理参数控制）再切原生。关键是两条路都了解，知道它们在什么情况下互补。",
        "case": "在开发一个本地文档问答工具时，用 OpenAI 兼容接口跑通核心流程；需要展示『模型边生成边输出』的效果时，改用原生 `/api/chat` 接口开启 stream，前端逐字渲染，演示效果非常直观。两套接口平时各司其职，关键时互补——这也是为什么两种方式都要掌握。",
    },
    "s1-ollama-c4-s1": {
        "content": "把前面所有能力串起来的第一个里程碑，是写一个『读本地文件 → 用本地模型回答』的脚本：从磁盘读取一份 Markdown/文本，把内容拼进 system 或 user 提示词，调用本地模型做总结或问答，全程数据不出机器。这个脚本虽小，却完整覆盖了『输入—处理—输出』的工程结构，也预示了后面 RAG 的雏形——差别只在于：RAG 是检索相关片段，这里是整篇塞进上下文。\n\n实现要点：文件读取要处理编码（UTF-8）与路径；文档可能超窗口，先做截断或分段；把文档内容与用户问题分开放进提示词（如 system 里写『以下是文档内容：…』，user 里问问题），让模型明确区分『资料』与『问题』。跑通这个脚本，你就完成了从『会调用 API』到『会做一个 AI 工具』的跨越。",
        "case": "一份 3000 字的公司制度文档，用脚本读取后问本地模型『年假怎么休』，模型基于文档内容准确回答并标注依据段落。整个过程中文档和数据没有离开本机，敏感的公司制度不会上传到任何云端。这个『读文件—问答』脚本，就是企业知识库问答的最原始形态——阶段三会把『整篇塞入』升级为『向量检索』。",
    },
    "s1-ollama-c4-s2": {
        "content": "Ollama 不仅能跑对话模型，也能跑嵌入（Embedding）模型，如 `nomic-embed-text`、`bge-m3` 等。Embedding 模型的作用是把文本转成向量，语义相近的文本向量距离近——这是阶段三 RAG（检索增强生成）实现『语义搜索』的技术底座。Ollama 提供 `POST /api/embed` 接口做向量化，同样支持 OpenAI 兼容的 `/v1/embeddings`。\n\n在本阶段你只需要建立两个认知：一是本地不仅能有免费的对话模型，也有免费的向量模型，整套 RAG 都可以本地闭环（免费 + 数据不出本地）；二是本章的『整篇文档塞进上下文』与 RAG 的『检索相关片段再塞进上下文』，差别在『检索』这一步——向量化就是检索的基础。有了这个衔接，进入阶段三学习 RAG 时你会觉得非常自然。",
        "case": "为阶段三的 RAG 提前做准备：`ollama pull nomic-embed-text` 拉取本地向量模型，然后用 `/api/embed` 把几段公司制度文本向量化，再对用户问题向量化、计算相似度，能明显看出『问题』与『相关段落』的向量距离更近。这一步亲手验证『语义相似 = 向量距离近』，会让你对 RAG 的理解不再是抽象概念。",
    },
}
