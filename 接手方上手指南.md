# 接手方上手指南 · 智学 AI（押金式 AI 教学服务平台）

> 本文件是**交给新接手方**的快速上手指南。
> 说明：打包时已移除原 owner 的私有密钥（`backend/.env`、小程序 AppID、微信支付证书等），
> 你需要按下面的清单自己申请并配置 **你自己的** 密钥。所有密钥都写在 `backend/.env`，该文件不会进 git，也不会发给别人。

---

## 1. 这是什么项目

一个「押金式培训」的 AI 学习平台：用户免费试用 5 天，缴 ¥199 押金解锁全部功能，学完课程、做完作业、通过考核后**全额退押金**。面向零基础学员的 AI Agent 开发学习路线。

- **前端**：Taro 4 + React 18 + TypeScript + Zustand（一套代码编译「微信小程序 + H5」）
- **后端**：FastAPI + SQLite（默认）/ MySQL（生产）
- **AI**：智谱 GLM（主）+ DeepSeek（可选），负责 AI 导师问答、能力测评、作业评审、课程推荐

## 2. 你需要准备的环境（本机首次开发）

| 依赖 | 版本要求 | 用途 |
|------|----------|------|
| Node.js | **Node 18+**（建议 18/20 LTS） | 前端编译（Taro 4） |
| Python | **3.11**（3.10–3.12 均可） | 后端 API |
| npm | 随 Node 安装 | 前端依赖 |
| pip / venv | 随 Python 安装 | 后端依赖 |
| 微信开发者工具 | 最新稳定版 | 预览/上传小程序 |
| （可选）Docker | 最新 | 一键部署后端 |

> 前端不需要各自装 Taro，项目自带 `@tarojs/cli`，`npm install` 后直接用 `npm run` 即可。

## 3. 你要申请/准备哪些 Key（都在 `backend/.env.example` 里有占位）

| Key / 配置 | 必填? | 去哪拿 | 说明 |
|------|------|--------|------|
| `ZHIPU_API_KEY` | ✅ 必填 | 智谱开放平台 https://open.bigmodel.cn （注册后「API Keys」创建） | AI 导师/测评/评审/推荐的主路径都走它 |
| `JWT_SECRET` | ✅ 必填 | 自己生成随机串 | 登录 Token 签名密钥。生成：`python -c "import secrets;print(secrets.token_hex(32))"`。**生产不填会启动报错** |
| `WECHAT_APPID` | ✅ 必填* | 微信公众平台 https://mp.weixin.qq.com → 小程序 →「开发管理 → 开发设置」 | 小程序 AppID。*开发模式设 `DEV_MODE=true` 时可暂不填 |
| `WECHAT_SECRET` | ✅ 必填* | 同上（AppSecret 处生成） | 微信登录 code2session |
| `DEEPSEEK_API_KEY` | 可选 | https://platform.deepseek.com | 作业评审的 pro 档更强、standard 档更省；不填自动全走智谱 |
| `WXPAY_MCHID` 等 6 项 | 可选* | 微信商户平台（需企业主体） | 微信支付押金收退。*未配置时押金走「占位路径」不真实收钱，**生产必须配齐否则报名被拒** |
| `WX_SUB_TEMPLATE_*`（3 个） | 可选 | 小程序后台「订阅消息」申请模板 | 打卡/作业/学习提醒；不填则静默不推送 |
| `DB_*` | 可选 | 无（本地用 SQLite 默认即可） | 生产切 MySQL 时才需要 |
| 小程序 `appid`（`project.config.json`） | ✅ 必填 | 同 WECHAT_APPID | **打包时已清空为 `touristappid`**，用你自己的 AppID 替换，否则开发者工具无法导入预览 |

> `backend/.env.example` 是模板：把它复制为 `backend/.env`，再逐项填你自己的真实值。
> **绝对不要把 `.env`、密钥、AppID 截图发给任何人或提交到 git。**

## 4. 后端：首次运行（一分钟跑起来）

```bash
cd backend
python -m venv .venv
# Windows: .venv\Scripts\activate     Linux/macOS: source .venv/bin/activate
pip install -r requirements-runtime.txt   # 精简运行时依赖（不含 torch，最快）

cp .env.example .env                # 然后按 §3 填你自己的 key；开发先设 DEV_MODE=true
python -m database                  # 首次：自动建表 + 灌种子课程/项目数据
python main.py                      # 启动（读取 config 的 HOST/PORT，默认 http://localhost:8010）
```

> 注意：直接敲 `uvicorn main:app` 会用 uvicorn 自己的默认端口 **8000**（本机常被其它服务占用），
> 请用 `python main.py`，或显式写 `uvicorn main:app --reload --port 8010`。

- 健康检查：`GET http://localhost:8010/health` → `{"status":"ok"}`
- 运营后台：`http://localhost:8010/admin`（默认账号 `admin` / 密码 `admin123`，`python -m database` 播种；**上线前务必改掉默认密码**）
- 端口说明：默认 **8010**（本机 8000 常被其它服务占用）；改端口用 `PORT=8011 uvicorn main:app`，
  H5 端同步 `DEV_API_TARGET=http://localhost:8011 npm run dev:h5`

> 完整版（含本地训练依赖 torch/transformers/peft）：`pip install -r requirements.txt`。
> 仅当你要本地重训 AI 模型时才需要；日常跑服务用 `requirements-runtime.txt` 即可（线上镜像也只装这个）。

## 5. 前端：首次运行

```bash
# 回到项目根目录
npm install

# 微信小程序（开发模式，watch 编译到 dist/）
npm run dev:weapp

# 用微信开发者工具「导入项目」选择 dist/ 目录，
# project.config.json 里的 appid 替换成你自己的，即可预览
```

H5 端（浏览器预览）：

```bash
npm run dev:h5      # 默认 http://localhost:10087
```

连接后端的地址在 `src/config/env.ts`（小程序端默认 `http://localhost:8010`；H5 端默认走同源相对路径，
由 dev server / Nginx 的 `/api`、`/static` 反向代理转发），生产可用构建时覆盖：
```bash
TARO_APP_API_BASE=https://你的域名 npm run build:weapp
TARO_APP_API_BASE=https://你的域名 npm run build:h5
```
开发时若真机/工具连不上 localhost，临时打开开发者工具「详情 → 本地设置 → 不校验合法域名」。
网页端（H5）真机调试：后端与前端都已在 `0.0.0.0` 监听，手机连同一 Wi-Fi 直接访问
`http://<电脑局域网IP>:10087` 即可（详见 `DEPLOY.md` §2.1）。

## 6. 跑测试（可选但建议）

```bash
# 后端：96 项，conftest 自动用临时库、自动开 DEV_MODE，不会污染数据
cd backend && python -m pip install -r requirements-dev.txt && python -m pytest tests/ -q

# 前端质量门禁
npm run typecheck      # TypeScript 类型检查
npm run lint           # ESLint
npm run test           # Vitest 单测
```

## 7. 目录结构速览

```
├── src/                  前端（Taro + React）：pages/components/services/store/types
├── backend/              FastAPI 后端
│   ├── routers/          接口路由（auth/pay/deposit/courses/ai/access/…）
│   ├── services/         业务逻辑（模型路由、权限、支付、订阅消息…）
│   ├── database.py       建表 + 种子数据
│   ├── db.py             SQLite / MySQL 方言层
│   └── tests/            测试套件
├── deploy/               Docker / Nginx / HTTPS 证书目录
└── docs/                 技术文档
```

## 8. 常见坑 & 上线前必读

- **生产别再开 `DEV_MODE=true`**：否则微信登录接受任意 code、后台可模拟切换任意用户，非常危险。上线置 `DEV_MODE=false` + 填真实 AppID/Secret。
- **押金收付未配置商户号时**：生产环境用户报名会被直接拒绝（503），这是有意为之；仅开发模式走占位。正式上线要配齐 `WXPAY_*` 并用真实小额支付/退款各测一笔。
- **Token 密钥**：`JWT_SECRET` 生产必须换强随机串，否则登录凭证可被伪造。
- **域名**：小程序线上要求 HTTPS + 已备案域名，且要在小程序后台配置 request 合法域名。
- **数据库**：默认 SQLite 单机够用；高并发再切 MySQL（设 `DB_ENGINE=mysql` + 连接参数）。
- **部署上线全流程**（Docker/HTTPS/提审清单）见 `DEPLOY.md`，很详细，直接照做。
- **完整功能列表与技术细节**：见 `README.md`。