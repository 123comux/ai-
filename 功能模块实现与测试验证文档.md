# AI 教学服务平台 · 全功能模块实现指南与测试验证文档

> 适用：从 0 到 1 · AI 教学服务平台（零基础 AI 教学小程序，押金式培训模式）
> 技术栈：前端 Taro 4 + React 18 + TS；后端 FastAPI + SQLite；AI：智谱/DeepSeek + RAG
> 用途：供开发者与其他 AI 工具按模块直接执行实施与验证
> 版本：v2.0 ｜ 编制：2026-08

---

## 阅读指引

本文档覆盖项目**全部后端路由模块**（14 组），每个模块统一结构：

1. **模块职责** —— 干什么、给谁用
2. **接口清单** —— 所有端点与方法
3. **数据设计** —— 涉及的表/字段/文件
4. **实现要点** —— 核心逻辑、代码骨架
5. **测试方案** —— 单测/集成/UAT 用例
6. **验证标准** —— 可量化验收
7. **风险与应对**

> 模块间依赖：auth（登录）→ deposit（押金）依赖 auth；mine_data 依赖 auth；其余内容模块独立。

---

## 模块 01：认证登录（auth.py）

**职责**：微信小程序登录，换取 openid，签发 HMAC token，提供 `get_current_user` 依赖保护业务接口。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/auth/login | 微信登录，返回 token + user |
| POST | /api/auth/wechat-login | 别名/兼容入口 |

**数据设计**：`users` 表（id, openid UNIQUE, nickname, avatar, created_at）。

**实现要点**
- `auth_utils.py`：`create_token(user_id)` 生成 `"{user_id}.{exp}.{hmac}"`；`verify_token` 常量时间比较验签、校验过期。
- `login`：code → `_code2session` 调微信 → `get_user_by_openid` 查/建 → 签 token。
- `DEV_MODE` 时任意 code 映射 `dev_{code}`，本地免 AppSecret 可用。
- `get_current_user`：读 `Authorization: Bearer` → 验签 → 返回用户 dict；失败 401。

**代码骨架**
```python
# auth_utils.py
def create_token(user_id): return f"{user_id}.{exp}.{_sign(payload)}"
def verify_token(token):  # 验签 + 过期 → user_id | None
# auth.py
@router.post("/login")
async def login(body: WechatLoginRequest):
    user = _resolve_user(body.code, body.nickname, body.avatar)
    return {"token": create_token(user["id"]), "user": user}
```

**测试**
| 层 | 用例 | 预期 |
|----|------|------|
| 单测 | 合法/篡改/过期/格式错 token | 分别返回 user_id / None |
| 集成 | login 成功返回 token；无 token 访问受保护接口 | 200 / 401 |
| 集成 | DEV_MODE 同 code 重复登录 | 同一 user |
| UAT | 开发者工具登录显示昵称；退出重进保持登录 | 通过 |

**验证标准**：登录成功 ≥99%；受保护接口无 token 100% 401；token 过期后失效。
**风险**：AppSecret 缺失→DEV_MODE 兜底；token 泄露→短过期；openid 冲突→UNIQUE 约束。

---

## 模块 02：押金式培训三锁考核（deposit.py）

**职责**：报名收押金 → 三锁（时间/过程/考核）进度 → 达标全额退费。核心商业模式。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/deposit/config | 押金配置（金额/时限/考核分） |
| POST | /api/deposit/enroll | 报名，记录押金 |
| GET | /api/deposit/status | 查询三锁进度与达标判定 |
| POST | /api/deposit/stage-assessment | 记录阶段考核分 |
| POST | /api/deposit/homework | 记录作业通过 |
| POST | /api/deposit/project-submit | 提交实战项目（通过/未过） |
| POST | /api/deposit/refund | 达标后全额退费 |

**数据设计**：`user_deposits`（user_id, amount, status: enrolled/refunded/expired, enrolled_at, refunded_at）；`user_stage_assessments`（user_id, stage, score）；`user_video_progress`（看完视频）。

**实现要点**（三锁判定集中在 `_compute_status` 纯函数）
- 时间锁：`now <= enrolled_at + DEPOSIT_TIME_LOCK_DAYS(90)`。
- 过程锁：完课率 = 已看视频/总视频 ≥100% 且作业全过。
- 考核锁：五阶段均分 ≥ `DEPOSIT_PASS_SCORE(85)` 且实战项目通过。
- refund：仅 `enrolled` 可退，置 `refunded`，记录金额；幂等防重复退。

**代码骨架**
```python
def _compute_status(user_id, deposit) -> dict:
    return {"eligible": time_passed and process_passed and assess_passed,
            "time_lock": {...}, "process_lock": {...}, "assessment_avg": avg}

@router.post("/refund")
async def refund(user=Depends(get_current_user)):
    if deposit["status"] != "enrolled": raise HTTPException(400, "非可退状态")
    if not _compute_status(...)["eligible"]: raise HTTPException(400, "未达标")
    # 置 refunded，记录金额
```

**测试**
| 层 | 用例 | 预期 |
|----|------|------|
| 单测 | 全达标/缺考核/超时/未交项目 四场景 | eligible 分别 True/False/False/False |
| 集成 | enroll→逐项提交→status 点亮→refund | 全链路 200 |
| 集成 | 未达标 refund / 二次 refund | 400 |
| UAT | 押金页报名→学习→考核→退费 | 状态正确流转 |

**验证标准**：三锁判定全部用例通过；达标退费链路端到端通；未达标/超时/重复退均被拒。
**风险**：微信支付未接→占位+记录；退费过高→严门槛；重复退→状态机幂等；时区→统一 UTC。

---

## 模块 03：课程体系（courses.py）

**职责**：课程列表/详情/分类/章节，前端学习承载。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/courses | 列表（limit/offset/topic/difficulty） |
| GET | /api/courses/topics | 分类话题 |
| GET | /api/courses/{course_id} | 详情含章节 |

**数据设计**：`courses` 表 + `chapters` JSON 字段；章节结构 `{title, summary, duration_minutes, sections[], video_bv}`。

**实现要点**：`_normalize_chapters` 把原始章节标准化（content_summary→summary、sub_chapters→sections 等别名兼容）；`_db_to_course` 转前端结构。

**测试**
- 单测：章节标准化（各别名输入→统一结构）。
- 集成：列表/详情/分类返回正确。
- UAT：课程详情页章节完整展示、可进入学习。

**验证标准**：章节字段无缺失；空课程数据返回空态不报错。
**风险**：章节结构不一致→`_normalize_chapters` 兜底。

---

## 模块 04：项目实践（projects.py）

**职责**：项目列表/详情/分步推进，进度持久化。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/projects | 列表（difficulty/tech/limit） |
| GET | /api/projects/topics | 难度分类 |
| GET | /api/projects/{project_id} | 详情 |
| POST | /api/projects/{project_id}/advance | 推进一步 |

**数据设计**：`project_progress.json`（{project_id: 完成步数}）；项目含 `steps[]`。

**实现要点**：`_apply_progress` 合并进度（完成步标 completed、当前步 current、其余 pending）；`advance` 幂等。

**测试**
- 单测：progress 合并、advance 幂等（已完成再推进不重复）。
- 集成：推进→进度变化→完成态。
- UAT：项目分步推进、完成沉淀作品集。

**验证标准**：advance 后进度正确；重复推进不越界。
**风险**：JSON 并发→加锁/迁移。

---

## 模块 05：视频学习（videos.py）

**职责**：视频列表/详情/按课程过滤/观看完成记录（驱动押金过程锁与学习统计）。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/videos | 列表（course_id 过滤） |
| GET | /api/videos/{video_id} | 详情 |
| GET | /api/videos/course/{course_id} | 某课程视频 |
| POST | /api/videos/{video_id}/complete | 标记看完（记时间戳+分钟） |

**数据设计**：`video_progress.json`（watched: {video_id: {at, minutes}}）；多用户后迁移 `user_video_progress` 表。

**实现要点**：`_mark_completed` 写 watched，带观看时长与时间戳；统计按分钟聚合。

**测试**
- 单测：watched 写入/读取。
- 集成：complete 后 stats 增加、押金过程锁推进。
- UAT：看视频→标记完成→进度/统计更新。

**验证标准**：complete 幂等；进度实时反映。
**风险**：时长统计失真→按真实观看分钟。

---

## 模块 06：学习路径（learning_paths.py）

**职责**：按方向组织课程/项目节点，顺序解锁、进度追踪。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/learning-paths | 全部路径 |
| GET | /api/learning-paths/{path_id} | 单条路径含节点 |
| POST | /api/learning-paths/{path_id}/nodes/{node_id}/complete | 标记节点完成 |

**数据设计**：`learning_paths.json`；`path_progress.json`（{path: [completed_node_ids]}）。

**实现要点**：`_apply_user_progress` 计算节点状态（locked/current/completed）；`complete_node` 校验顺序解锁，重复课程自动完成。

**测试**
- 单测：顺序解锁、重复课程自动完成、进度按视频粒度。
- 集成：完成节点1→节点2解锁；未完成前节点标记返回400。
- UAT：按路径学习进度实时涨；方向切换进度独立。

**验证标准**：节点状态流转正确；解锁校验严格。
**风险**：进度 JSON 并发→加锁/迁移。

---

## 模块 07：能力测评（assessment.py）

**职责**：测评出题/收卷，生成六维能力报告。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/assessment/questions | 取题（count） |
| POST | /api/assessment/submit | 提交，生成报告 |

**数据设计**：`assessment_questions.json`；`user_ability_reports` 表。

**实现要点**：`submit_assessment` 计算六维得分 + 可选 `self_description` 走 AI 融合（`_fuse_with_ai`/`_fuse_direction`）。

**测试**
- 单测：得分计算、AI 融合逻辑。
- 集成：提交→报告生成→我的页展示。
- UAT：测评后雷达图正确、推荐方向合理。

**验证标准**：报告六维齐全；提交幂等。
**风险**：AI 融合失败→纯规则兜底。

---

## 模块 08：岗位对标（user_data.py 部分）

**职责**：输入岗位描述，AI 结合能力报告分析匹配度/技能/差距/推荐。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/job-matching | 默认结果 |
| POST | /api/user/job-matching/analyze | AI 分析岗位描述 |

**实现要点**：`_skill_mastered`（技能关键词→维度，维度分≥60 算掌握）、`_skill_dimension_score`；`analyze_job_matching` 智谱 AI 提取技能 + 后端算 matchScore，失败回退关键词。

**测试**
- 单测：掌握判定阈值（30分能力→待提升）、匹配度计算。
- 集成：不同岗位返回差异化结果。
- UAT：输入岗位→AI 分析→匹配度/技能/推荐展示。

**验证标准**：不同岗位结果明显不同；30 分能力不虚高。
**风险**：AI 输出不可靠→后端规则约束。

---

## 模块 09：学习统计与能力报告（user_data.py 其他）

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/user/ability-report | 能力报告 |
| GET | /api/user/learning-records | 学习记录（按日聚合） |
| GET | /api/user/learning-stats | 统计（streak/分钟/完成数） |

**实现要点**：`get_learning_stats` 从 `video_progress.json` 实时聚合（连续学习天数 streak、总分钟、完成视频/项目数）。

**测试**
- 单测：streak 计算、按日聚合。
- 集成：看完视频后 stats 更新。
- UAT：首页/我的页统计实时正确。

**验证标准**：统计与真实行为一致。
**风险**：数据源为空→默认 0 兜底。

---

## 模块 10：AI 能力中心（ai.py）

**职责**：AI 导师问答（RAG + 双模型）、能力文本分析、课程推荐、模型信息。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/tutor/chat | 导师问答 |
| POST | /api/assessment/analyze | 能力文本分类 |
| GET | /api/recommend/courses | 课程推荐 |
| GET | /api/ai/models | 模型信息 |

**实现要点**：`tutor_chat` 智谱优先（`chat_zhipu` + 课程 RAG），失败回退本地 Qwen（`tutor_service`）；推荐用线程池+超时防冷加载阻塞。

**测试**
- 单测：chat_zhipu 正常/异常/上下文注入。
- 集成：问答正常；智谱不可用回退成功；推荐超时不阻塞。
- UAT：导师问答合理、推荐可点跳转。

**验证标准**：问答 P95<5s；智谱不可用不中断；推荐不阻塞接口。
**风险**：Token 成本→分层调用；限流→本地兜底。

---

## 模块 11：内容管理（content.py）

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/content/banners | Banner 轮播 |
| GET | /api/content/directions | 学习方向 |
| GET | /api/content/menu-items | 菜单项（section） |

**测试**：单测过滤排序；集成新增内容前端可见；UAT 后台改内容小程序即时更新。
**风险**：缓存→加失效策略。

---

## 模块 12：个人中心（mine_data.py）

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET/POST | /api/mine/portfolio | 作品集 |
| GET/POST/PUT/DELETE | /api/mine/goals[/{id}] | 学习目标 |
| GET/POST/DELETE | /api/mine/favorites[/{id}] | 收藏 |

**实现要点**：作品集取 progress>0 项目；目标 CRUD；收藏后端回填 title/cover/path（`_resolve_favorite`），UNIQUE 去重。

**测试**
- 单测：CRUD、收藏去重。
- 集成：收藏→列表→取消；目标增删改查。
- UAT：个人中心三功能全流程可用。

**验证标准**：CRUD 通过；收藏唯一约束生效。
**风险**：多用户隔离→加 user_id。

---

## 模块 13：知识库检索（knowledge.py）

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| GET | /api/knowledge/search | 检索 |
| GET | /api/knowledge/topics | 话题 |
| GET | /api/knowledge/count | 计数 |
| GET | /api/knowledge/browse | 浏览 |

**测试**：检索返回相关片段；支撑 RAG 问答。
**验证标准**：检索命中率、RAG 回答包含资料内容。

---

## 模块 14：后台管理（admin.py + admin_panel.py）

**职责**：管理员认证 + 通用 CRUD（tables 泛型接口）运营课程/内容/用户。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/admin/login | 管理员登录（sha256） |
| GET | /api/admin/tables | 表列表 |
| GET/POST/PUT/DELETE | /api/admin/{table}[/{id}] | 泛型 CRUD |

**实现要点**：`verify_token` 校验 Bearer admin token；泛型接口按表名操作（白名单校验防注入）。

**测试**
- 单测：admin token、泛型 CRUD、表名白名单。
- 集成：登录→增删改某表。
- UAT：后台改课程→小程序可见。

**验证标准**：泛型 CRUD 正确；非白名单表被拒。
**风险**：SQL 注入→参数化+白名单。

---

## 模块 15：学习激励（打卡/排行榜/社区）（community.py）

**职责**：平台化功能——每日 AI 打卡、学习排行榜、学习社区（帖子/回复/点赞），提升学习留存与互动。

**接口清单**
| 方法 | 路径 | 说明 |
|------|------|------|
| POST | /api/community/checkin | 每日打卡（UNIQUE(user,date) 防重复） |
| GET | /api/community/checkin/status | 打卡状态（今日是否打、连续天数） |
| GET | /api/community/leaderboard | 学习时长排行榜 |
| POST | /api/community/posts | 发帖 |
| GET | /api/community/posts | 帖子列表（含回复数） |
| GET | /api/community/posts/{id} | 帖子详情 + 回复 |
| POST | /api/community/posts/{id}/reply | 回复 |
| POST | /api/community/posts/{id}/like | 点赞 |

**数据设计**：`checkins`（user_id, checkin_date UNIQUE）、`community_posts`（user_id, title, content, category, likes）、`community_replies`（post_id, user_id, content）。

**实现要点**
- 打卡连续天数 `_checkin_streak`：从今天（或昨天）往前数连续有打卡记录的天数。
- 排行榜按 `user_video_progress` 累计分钟排序。
- 所有接口需 `get_current_user` 鉴权。

**前端**：`pages/community`（打卡/排行榜/社区三 tab）+ `pages/communityPost`（帖子详情）；"我的"页加入口。

**测试**
- 单测：连续天数计算（今天打卡/昨天打卡/断卡）。
- 集成：打卡→状态→重复打卡 400；发帖→列表→详情→回复→点赞。
- UAT：打卡连续天数增长、排行榜实时更新、发帖/回复/点赞全流程。

**验证标准**：打卡唯一约束生效；排行榜按时长正确排序；社区 CRUD 全通过。
**风险**：打卡刷连续天数→按日历日校验；点赞重复→MVP 暂不幂等。

---

## 通用测试环境与质量基线

**环境**
- 后端：`uvicorn main:app`（8000），DEV_MODE=true。
- H5：`npm run dev:h5`（10087）。小程序：微信开发者工具导入 `dist/`。

**质量指标**
- 核心接口成功率 ≥99%；关键接口 P95 <3s。
- 单测覆盖核心纯函数 ≥60%；集成覆盖全部路由。
- 小程序 + H5 双端核心流程一致。

**分层测试要求**
| 层级 | 覆盖 | 占比 |
|------|------|------|
| 单元 | 三锁判定/token/统计/章节标准化 | ≥60% |
| 集成 | 全部 14 路由接口链路 | ≥40% |
| UAT | 每个模块核心业务流 | 端到端 |

**上线前提（11.9）**：微信企业认证、域名备案、HTTPS、小程序提审。

---

# 第二部分：前端实现指南

> 前端技术栈：Taro 4 + React 18 + TypeScript + Zustand + CSS Modules（Sass）
> 架构：单仓库双端输出（小程序 → `dist/`，H5 → `dist-h5/`），一套代码。

---

## F1. 前端架构与工程

**目录职责**
| 目录 | 职责 |
|------|------|
| src/pages/ | 页面（每个页面 index.tsx + index.module.scss + index.config.ts） |
| src/components/ | 跨页复用组件 |
| src/store/ | Zustand 全局状态 |
| src/services/ | API 封装（api.ts）、云函数（cloud.ts） |
| src/utils/ | 格式化工具 |
| src/styles/ | SCSS 变量与主题 |

**关键配置**（config/index.ts）
- `designWidth: 375`，rpx→px 适配。
- `outputRoot`：小程序 `dist/`，H5 `dist-h5/`（分离避免互相覆盖）。
- `devServer.port: 10087`，`/api` 代理到 `http://localhost:8000`。

**测试**：小程序 `npm run dev:weapp`；H5 `npm run dev:h5`；构建 `npm run build:weapp|h5`。
**验证**：两套产物各自可访问，互不覆盖；H5 代理 `/api` 通后端。

---

## F2. 状态管理（Zustand）

**useUserStore**（用户与登录态）
- 状态：`isLoggedIn / token / userId / nickname / avatar / grade / major / targetDirection`。
- `login()`：`Taro.login` 取 code → `wechatLogin(code)` → 存 token → 更新状态。
- `restore()`：启动读本地 token → `fetchMe()` 校验，失效则清空。
- `logout()`：清 token 与状态。
- H5/DEV 下 `Taro.login` 不可用 → 用 `dev_{Date.now()}` code 兜底（后端 DEV_MODE 接受）。

**useLearningStore**（学习数据）
- 状态：`abilityReport / currentPath / courses / projects / selectedTopic`。
- 首页选择方向 → `setSelectedTopic` → 学习页预过滤。

**代码骨架**
```ts
// useUserStore.ts
login: async () => {
  const res = await Taro.login();
  const code = res?.code || `dev_${Date.now()}`;
  const data = await wechatLogin(code);
  Taro.setStorageSync('aishi_token', data.token);
  set({ isLoggedIn: true, token: data.token, ...data.user });
}
```

**测试**
- 单测：login/restore/logout 状态流转；token 持久化。
- 集成：app 启动自动 restore → 未登录自动 login。
- UAT：登录后首页显示昵称；重启保持登录；退出后清空。

**验证**：启动流程（restore→login）不阻塞首页渲染；token 失效自动重登。
**风险**：H5 无微信登录 → dev code 兜底；token 失效 → restore 清除。

---

## F3. API 服务层（services/api.ts）

**封装**：`apiGet/apiPost/apiPut/apiDelete` 统一 `Taro.request`，自动拼 `API_BASE=http://localhost:8000`，POST/PUT 带 JSON header，非 200 抛错（读 `detail` 显示业务提示）。

**按功能分组的导出函数**（供页面调用）
| 分组 | 函数 |
|------|------|
| 认证 | wechatLogin, fetchMe |
| 押金 | fetchDepositConfig, enrollDeposit, fetchDepositStatus, submitStageAssessment, passHomework, submitProject, refundDeposit |
| 课程 | fetchCourses, fetchCourseDetail, fetchCourseCategories |
| 项目 | fetchProjects, fetchProjectCategories, advanceProject |
| 视频 | fetchVideos, fetchCourseVideos, completeVideo |
| 路径 | fetchLearningPath(s), completeLearningPathNode |
| 测评 | fetchAssessmentQuestions, submitAssessment |
| 用户 | fetchAbilityReport, fetchLearningRecords, fetchLearningStats, fetchJobMatchingResult, analyzeJobMatching |
| AI | askTutor, analyzeAbility, getRecommendedCourses |
| 个人 | fetchPortfolio, fetchGoals, createGoal, updateGoal, deleteGoal, fetchFavorites, addFavorite, removeFavorite |
| 内容 | fetchBanners, fetchDirections, fetchMenuItems |

**代码骨架**
```ts
async function apiPost<T>(path, body, timeoutMs = 30000): Promise<T> {
  const res = await Taro.request({ url: buildUrl(path), method: 'POST',
    header: { 'Content-Type': 'application/json' }, data: body, timeout: timeoutMs, dataType: 'json' });
  if (res.statusCode !== 200) throw new Error(res.data?.detail || `HTTP ${res.statusCode}`);
  return res.data;
}
```

**测试**：单测 buildUrl 去重斜杠；集成各接口 200；非 200 抛错信息正确。
**验证**：H5 代理与直连后端均通。

---

## F4. 通用组件（components/）

| 组件 | 职责 | 要点 |
|------|------|------|
| CourseCard | 课程卡 | 封面+标题+节数/时长；无进度条（已移除避免误导） |
| ProjectCard | 项目卡 | 难度标签+技术栈+进度 |
| PathCard | 路径卡 | 方向徽章+进度条+完成项 |
| ProgressBar | 进度条 | 百分比+颜色 |
| AssessmentCard | 测评卡 | 评分+等级+推荐方向 |
| RadarChart | 五维雷达图 | H5 用 SVG、小程序用 Canvas（createCanvasContext），坐标按屏宽换算 |

**RadarChart 跨端关键点**
```tsx
const IS_WEAPP = process.env.TARO_ENV === 'weapp';
// 小程序端：pxSize = 500 * windowWidth / 750（避免绘制坐标与显示尺寸不匹配裁切）
// H5 端：SVG viewBox 自适应
```

**测试**：RadarChart 小程序/H5 双端无裁切；各卡片点击跳转正确；ProgressBar 百分比正确。
**风险**：Canvas 尺寸不匹配→坐标换算（已修）。

---

## F5. 页面实现（18 + 押金页）

> 每个页面：`index.config.ts`（导航标题）+ `index.tsx` + `index.module.scss`。

### F5.1 首页 home
- 数据：Banner、统计、方向、路径、推荐课程/项目、AI 推荐。
- `useDidShow` 每次显示刷新（从其他页返回进度同步）。
- 交互：选方向→`setSelectedTopic`→跳学习页；点课程/项目→详情。
- **测试**：UAT 首页各区块展示、方向过滤、跳转正确。

### F5.2 学习页 learn
- 课程分类 + 列表 + AI 推荐；按 `selectedTopic` 预过滤。
- **测试**：分类切换、推荐可点、返回首页入口。

### F5.3 项目页 project
- 项目列表 + 难度筛选。
- **测试**：筛选、详情跳转。

### F5.4 我的页 mine
- 用户信息、统计、能力报告（RadarChart）、功能菜单（fetchMenuItems）。
- **测试**：统计实时、菜单跳转、雷达图显示。

### F5.5 押金页 deposit（核心）
- 押金配置展示、报名、三锁进度、考核提交、退费。
- 数据：fetchDepositConfig/fetchDepositStatus/enrollDeposit/submitStageAssessment/refundDeposit。
- **测试**：报名→三锁进度点亮→达标→退费全流程；未达标提示。

### F5.6 能力测评 assessment
- 问卷（fetchAssessmentQuestions）→ 提交（submitAssessment）→ 报告。
- **测试**：答题、提交、报告生成。

### F5.7 课程详情 courseDetail
- 课程信息、章节目录、视频入口、收藏按钮、标记完成。
- **测试**：章节展开、收藏切换、完成状态。

### F5.8 章节详情 chapterDetail / 学习路径 learningPath / 项目详情 projectDetail / 视频 video / AI导师 tutor / 岗位对标 jobMatching / 作品集 portfolio / 目标 goals / 收藏 favorites / 设置 settings / webview
- 各自对接对应 API（见 F3 分组），UAT 覆盖核心业务流。

---

## F6. 前端验证清单

- [ ] 启动：restore→login 自动、首页不白屏
- [ ] 押金：报名→三锁→退费全流程可用
- [ ] 课程/项目/视频/路径：列表、详情、进度正确
- [ ] 测评/岗位对标/AI导师：AI 功能正常
- [ ] 个人中心：作品集/目标/收藏/设置可用
- [ ] 双端：小程序 + H5 核心流程一致
- [ ] RadarChart：双端无裁切
- [ ] 控制台无报错，非 200 接口提示友好

> 前端页面与后端 14 模块一一对应，可参照第一部分的接口清单联调。前端验证以"页面可操作、数据正确、双端一致"为准。

