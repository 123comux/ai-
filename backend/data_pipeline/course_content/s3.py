"""阶段三 · Agent 开发实战 — 小节内容扩充（RAG / MCP / Agent Skills / LangChain / LangGraph / Agentic RAG）。"""

SECTION_ENRICH: dict[str, dict] = {
    # ================= s3-rag RAG 检索增强生成 =================
    "s3-rag-c1-s1": {
        "content": "RAG 要解决的三大痛点，本质都是『模型的知识不够用』。第一，知识过时：模型的知识截止于训练完成时，训练之后的新政策、新产品、新事件它一概不知道，让它回答必然过时。第二，幻觉：面对不确定的信息，模型倾向于『顺着编』而不是『承认不知道』，编造看似合理的错误内容。第三，私有数据：模型完全不了解你公司的内部文档、产品手册、私有知识库，你问它任何内部问题它都只能瞎猜。\n\nRAG（Retrieval-Augmented Generation，检索增强生成）的思路极其朴素：不给模型自己发挥，而是『先把相关资料找出来，再让模型照着资料回答』。把检索到的真实资料拼进提示词，模型就基于这些资料作答——知识不过时（资料是实时的）、不易幻觉（有据可依）、能回答私有数据（资料来自你的文档）。理解这三大痛点，你就明白了 RAG 为什么是当前企业知识库、智能客服等场景的标配。",
        "case": "一家保险公司的客服系统直接用 LLM 回答理赔政策，结果模型经常引用已废止的旧条款（知识过时）或编造不存在的理赔条件（幻觉）。引入 RAG 后：先把最新版《理赔条款手册》向量化入库，用户提问时检索相关条款原文，让模型『仅基于检索到的条款回答，查不到就说不清楚』。上线后理赔问题回答准确率从 65% 提升到 95% 以上。三大痛点，被一个『先检索、再回答』的思路同时缓解。",
    },
    "s3-rag-c1-s2": {
        "content": "RAG 的完整流程是一条清晰的处理管线，六个环节：①加载文档——把 PDF、Markdown、Word 等格式解析成纯文本；②切分文本——把长文档切成适合检索与模型处理的小块（chunk）；③生成向量——用 Embedding 模型把每个文本块转成向量；④存入向量库——把向量连同原文、来源元数据一起存进向量数据库；⑤检索相关片段——用户提问时，把问题向量化，在库里找最相似的 top-k 块；⑥交给模型回答——把检索到的片段 + 用户问题拼进提示词，模型基于片段作答。\n\n这条管线就是 RAG 的骨架，后续所有优化（分块策略、检索质量、Agentic 化）都是在这六步的某一环上做文章。本课程的方法是『先手写跑通完整 pipeline』——亲手实现加载、切分、向量化、建库、检索、回答，你就对每一步的原理和坑有了第一手理解；之后再上 LangChain 等框架，你会发现框架只是把这条管线标准化封装了。",
        "case": "一条最小的 RAG 管线跑通后的效果：把公司《请假制度》文档（5 页）加载→切成 20 个 chunk→向量化入 Chroma→用户问『年假可以跨年吗』→检索命中相关条款→模型基于条款回答『可以，但最迟次年 X 月前使用』并附上条款位置。用户得到的是『检索到的真实条款 + 基于条款的准确回答』，而非模型的凭空猜测。这就是 RAG 的全流程价值。",
    },
    "s3-rag-c2-s1": {
        "content": "文档加载是 RAG 的第一环，目标是『把各种格式的文档变成干净可用的纯文本』。主流格式各有方案：PDF 用 PyPDF2 / pdfplumber 提取文字（扫描版 PDF 需要 OCR）；Markdown 本身是纯文本，直接读取，但要注意保留标题层级（切分时有用）；Word 用 python-docx 提取段落；HTML 可用 BeautifulSoup 抽取正文。加载阶段的另一项工作是清洗：去掉页眉页脚、页码、版权行这些噪声，合并被分页截断的表格或段落。\n\n加载质量直接决定下游检索质量——『垃圾进、垃圾出』在 RAG 里尤其明显。如果 PDF 提取出来是乱码或缺失文字，后面再怎么优化检索也无济于事。所以加载阶段要建立验证习惯：抽样检查提取结果，确认文字完整、结构可读。对开发者而言，先支持最常见的『PDF + Markdown』两种格式，就能覆盖大多数知识库场景。",
        "case": "某团队导入一份 200 页的 PDF 产品手册，用默认解析后检索经常漏掉关键信息。排查发现：PDF 是图文混排，文字提取后段落顺序错乱，且页眉页脚被当成正文。改用 pdfplumber 按坐标提取正文、清洗掉页眉页脚后，检索命中率明显提升。文档加载看似『导入就行』，实则是 RAG 质量的第一道关卡——解析得越干净，后面越省心。",
    },
    "s3-rag-c2-s2": {
        "content": "文本分块（Chunking）是 RAG 里对效果影响最大的环节之一，因为『块』是检索的基本单位——块太小，单个块信息量不足，模型读不懂；块太大，语义被稀释，检索精度下降且浪费 Token。两个核心参数：chunk_size（每块的字数）与 overlap（相邻块的重叠字数）。overlap 的作用是避免在切分边界处把完整语义切断——比如一句话被从中间切开，检索时两半都不完整。\n\n除了大小，更重要的是『按语义边界切』：优先在段落、标题、句子边界处切，而不是硬按固定字数切。用 Markdown 的标题层级切分（每个小节一块）往往比纯按字数切效果好得多——因为标题本身就是天然的语义单位。分块策略没有万能参数，需要结合你的文档特点与检索效果来调：文档结构化强（有标题）就按结构切；纯长文就按『段落 + 合适大小 + overlap』切。",
        "case": "同一份文档，用『每 500 字硬切，无 overlap』检索『如何退款』，命中了一个把『退款条件』和『退款流程』切成两块的碎片，模型回答不完整；改成『按段落切 + 每块 max 300 字 + overlap 50 字』后，『退款条件』整块被命中，回答完整准确。一次分块策略的调整，检索效果立竿见影——分块不是细节，是 RAG 的核心调优点之一。",
    },
    "s3-rag-c3-s1": {
        "content": "Embedding（嵌入）是 RAG 的『语义翻译官』：把一段文本映射成一个高维向量，让『语义相近』在向量空间里表现为『距离相近』。为什么向量能表达语义？因为 Embedding 模型是在海量语料上训练的，它学会了把语义相近的文本映射到相近的位置——『如何退货』与『退货流程说明』的向量距离就很近，而『如何退货』与『今天天气不错』的向量距离很远。\n\n这是 RAG 与『关键词搜索』的根本区别：关键词搜索只能匹配字面相同，RAG 能匹配『意思相同但说法不同』——用户问『东西坏了咋办』，也能命中文档里的『商品损坏处理流程』。理解 Embedding 的语义特性，你就明白了为什么 RAG 能在『用户问法五花八门』的真实场景里还能检得准。也正因如此，Embedding 模型的选择（本地 Ollama 的 nomic-embed-text / 云端的 OpenAI Embedding）会影响语义理解的细致程度。",
        "case": "用户问『货不对板怎么处理』，用关键词搜索『货不对板』可能搜不到任何文档（文档里写的是『商品与描述不符』）；但用向量检索，问题向量与『商品与描述不符的处理流程』向量距离很近，能正确命中。这就是语义搜索相对关键词搜索的核心优势——它不要求字面一致，只要求意思相关。",
    },
    "s3-rag-c3-s2": {
        "content": "向量数据库承担 RAG 的『存储与检索』，Chroma 是最容易上手的入门选择。核心操作两步：建库与检索。建库：`collection.add(ids=..., documents=[块文本], metadatas=[来源、页码])`，把文本块、向量（可让 Chroma 自动调用内置 embedding，也可传入自算向量）、元数据一起写入；同时设置 persist 目录实现持久化（重启不丢）。检索：`collection.query(query_texts=[问题], n_results=k)`，Chroma 自动把问题向量化并返回最相似的 top-k 块及其相似度分数。\n\nChroma 适合原型与中小规模，本地跑、零配置。要注意的工程点：数据量增长后检索速度会下降，需要索引优化；向量库与业务库的备份策略（向量数据也是资产）。学 Chroma 的重点是掌握『向量库的存与取』这个通用心智——将来换 FAISS（更快的本地库）或 Milvus（分布式），API 形态大同小异，原理完全一致。",
        "case": "用 Chroma 建企业知识库：把《制度手册》切成 200 块，`add` 进 collection（每条带元数据『来源：制度手册-第三章』）；用户问『出差报销标准』，`query` 返回 top-3 相关块及分数，其中命中『差旅费报销标准』这一块，模型据此准确回答并标注『依据制度手册第三章』。从建库到检索，Chroma 用几十行代码就把 RAG 的存储检索环节跑通了。",
    },
    "s3-rag-c4-s1": {
        "content": "检索到相关片段后，如何把片段和问题组织进提示词，直接影响回答质量。标准做法是把检索结果拼进 system 或 user 消息，并明确告诉模型：你只能基于这些资料回答，资料不足就明说『未找到相关资料』，不要凭自己的知识编造。这个『约束模型基于资料』的系统提示词，是 RAG 抑制幻觉的关键——模型本来倾向自由发挥，你得用明确的指令把它锚定在资料上。\n\n拼装时还要注意：把每块资料标上编号或来源（如『【资料 1】…』），让模型回答时能引用；资料要经过筛选（只放 top-k，别把不相关的也塞进去，反而干扰模型）；资料总量控制在模型能消化的范围（几块即可，不要贪多）。『检索—拼装—约束回答』这一环，决定了 RAG 是『读懂了资料再答』还是『拿资料当摆设乱答』。",
        "case": "同样检索到 3 块资料，两条提示词效果迥异：差的（『请回答：…』，资料随意贴）模型可能无视资料自行发挥；好的（system 里写『你是知识库助手，仅基于以下资料回答；资料不足请明确说不知道：\n【资料1】…\n【资料2】…』）模型会引用资料作答。实测把『仅基于资料回答 + 资料不足明说』写进 system 后，客服问答的幻觉率下降约 80%。约束模型基于资料，是 RAG 回答质量的第一保障。",
    },
    "s3-rag-c4-s2": {
        "content": "引用溯源是让 RAG 回答『可验证』的机制：不仅告诉用户答案，还要标注答案的依据来自哪份文档、哪个部分。实现的关键是让元数据（metadata）从加载一路传递到回答：加载时给每块打上『来源文档、章节、页码』标签，检索时这些元数据随块一起返回，拼装提示词时把来源信息一并给模型，并要求模型在回答中标注出处（如『依据《制度手册》第三章』）。\n\n引用溯源的价值有三：一是可信度——用户能看到答案有据可依，而不是凭空而来；二是可追溯——答案出错时能回溯到原始文档定位问题（是文档错了还是引用错了）；三是可运营——能统计哪些文档被高频引用，反哺知识库质量。工程上，引用溯源通常与『前端展示引用』配合：回答下方列出引用片段，用户可点击查看原文。",
        "case": "一个员工自助问答平台，初版回答没有引用，员工经常质疑答案来源，且答案出错时无法定位。加上引用溯源后：回答自带『依据《考勤制度》第 3 章』的标注，点击可查看被引用的原文段落；客服团队也能从出错回答快速回溯到具体文档，发现并修正了两处过期制度。引用溯源让 RAG 从『给个答案』升级为『给个可验证、可追溯的答案』。",
    },
    "s3-rag-c5-s1": {
        "content": "检索质量是 RAG 效果的瓶颈，而查询改写与混合检索是两组常用进阶优化。查询改写（Query Rewriting）：用户的问题往往口语化、含歧义，直接拿去检索效果差；可以先用一次 LLM 调用把问题改写成更适合检索的形式——补全省略的主语、把口语转成规范查询词、去掉冗余。比如『那个能退吗』改写成『该商品是否支持无理由退货』。混合检索（Hybrid Search）：向量检索擅长语义匹配但不擅长精确匹配（比如型号『X200』），关键词检索（BM25）擅长精确匹配但不懂语义；两者结合（同时跑向量 + 关键词，结果融合去重）能互补，显著提升召回。\n\n这两组优化的适用场景不同：问题口语化严重、检索常常『想当然』时优先做查询改写；文档里术语、型号、编号密集、纯向量检索漏召回时优先做混合检索。优化原则是『先诊断再优化』——用一批标准测试问题观察检索失败的模式，再对症下药，而不是盲目叠加优化手段。",
        "case": "用户问『上个月报销的那个东西』，直接向量检索命中一堆无关内容（缺主语、指代不明）。做查询改写后，先让模型把问题补全为『上月提交的报销申请是什么』，检索准确率明显提升。另一个案例：检索『X200 型号』时纯向量检索常漏，加上 BM25 关键词检索融合后，型号精确命中。查询改写解决『问得不清楚』，混合检索解决『匹配不精确』，双管齐下把检索质量拉到新台阶。",
    },
    "s3-rag-c5-s2": {
        "content": "Rerank 与 HyDE 是检索质量优化里更进阶的两招。Rerank（重排）：向量检索先粗召回 top-50，再用一个更精细的『交叉编码器』模型逐条计算『问题与每条的真正相关度』，重新排序取 top-5。粗召回求全、精排求准——向量检索快但粗，Rerank 慢但准，两者配合兼顾效率与精度，是目前工业界提升检索精度的首选方案。HyDE（假设性文档嵌入）：先用 LLM 根据问题生成一段『假设的完美答案』，再用这段假设答案去检索——假设答案的语义往往比原始问题更贴近目标文档，从而提高召回相关性。\n\n两者的代价：Rerank 需要额外的重排模型（本地可跑 bge-reranker，云端有专门 API）；HyDE 需要一次额外的 LLM 生成。使用时按需取舍：对检索精度要求高的场景（法律、医疗）值得上 Rerank；对『问题太泛、直接检索效果差』的场景值得试 HyDE。原则依旧是先诊断检索失败的具体模式，再决定用哪一招。",
        "case": "一个法律问答 RAG，纯向量检索 top-5 常混入不相关法条。加 Rerank 后：先用向量粗召回 50 条，再用重排模型精排取 top-5，相关法条的命中率显著提升，回答质量随之提高。另一场景用户问『公司不给加班费合法吗』，直接检索效果一般；用 HyDE 先生成假设回答『加班费支付的法律规定』再检索，命中了更精准的《劳动法》相关条款。两招都是针对『检索不够准』的精准施策。",
    },
    "s3-rag-c6-s1": {
        "content": "手写 RAG 的最终形态，是把它封装成一个可用的企业知识库问答系统。推荐架构：FastAPI 提供三个核心接口——上传文档（接收文件 → 解析 → 分块 → 向量化 → 入库）、问答（检索 → 拼装 → 生成 → 返回带引用的回答）、知识库状态查询（文档数、chunk 数）。存储用 Chroma（向量）+ 本地或云端 Embedding；模型用本地 Ollama（开发调试）或云端（上线）。部署用 Docker 容器化，一条命令跑起来。\n\n系统设计的关键决策：文档去重（同一文档重复上传不能产生重复块，按文档 hash 幂等处理）；接口鉴权与限流（知识库可能是内部数据）；异步处理（大文档解析入库耗时长，用异步任务避免阻塞）。这个系统麻雀虽小五脏俱全，覆盖了『上传—入库—问答』的完整产品链路，是求职作品级项目的基础形态。",
        "case": "一个可运行的最小架构落地：FastAPI 的 `POST /documents` 上传 PDF 并后台解析入库，`POST /query` 接收问题、检索 Chroma、调 LLM 生成、返回 `{answer, references:[{chunk, source, score}]}`。用 docker-compose 把 API + Chroma 编排起来，`docker-compose up` 一条命令跑通。演示时上传一份制度文档，提问后回答带引用，效果直观——这就是『能讲清架构、能现场演示』的 RAG 项目雏形。",
    },
    "s3-rag-c6-s2": {
        "content": "做 RAG 项目时，能『讲清楚每一个技术选择的原因』是面试加分的核心。准备的要点：为什么选 Chroma（本地、够用、上手快）；为什么用本地 Embedding（数据隐私 + 成本，与云端对比过效果）；为什么这么分块（按语义边界 + 试验结果）；引用如何一路传递（metadata 从加载到回答）；检索质量如何评估（准备过标准测试集，量化过准确率）；将来如何优化（Rerank、查询改写、Agentic RAG）。每一个选择都要有『为什么』，而不是『教程这么写的』。\n\n同时准备一个『演示脚本』：从上传文档、到提问、到带引用的回答、到检索可视化，一气呵成且能应对突发（比如现场改问题、现场传新文档）。能讲清楚设计取舍、能现场演示、能回答问题，是『作品级项目』的三个硬标准——这正是学习路线里反复强调的『不要做教程复读机，要做能自圆其说的作品』。",
        "case": "面试官追问『为什么这块检索不到，你怎么办』时，有准备的回答是：『我先看是召回问题还是排序问题——如果向量检索没召回到相关块，可能是分块切碎了语义，我会调整分块或加混合检索；如果召回了但排到后面，我会加 Rerank。我会用测试集量化前后来验证』。这种『有方法、有工具、能量化』的回答，与『我调了参数』形成鲜明对比——展示的是工程思维而非碰运气。",
    },

    # ================= s3-mcp MCP 模型上下文协议 =================
    "s3-mcp-c1-s1": {
        "content": "传统 AI 集成外部工具的痛点，是『每个工具都要专门写一套对接代码』。想象你要让 Agent 访问文件系统、数据库、GitHub、搜索、浏览器——每个工具都有自己的鉴权方式、参数格式、协议和错误处理，你得为每一个写一遍『连接、鉴权、传参、解析、处理错误』的样板代码。工具越多，重复代码越多，且每个工具的对接都耦合在应用里，改动一个工具就要改应用代码。这是典型的『N 个工具 × M 个应用』的集成爆炸。\n\n更麻烦的是：这套对接逻辑和 Agent 的智能逻辑纠缠在一起，让应用变得难维护、难扩展——新加一个工具要重新开发测试，换成另一个 AI 应用又要重来一遍。MCP 正是为解决这个根本矛盾而生：把『工具对接』从应用里抽出来，标准化成可复用的协议层。",
        "case": "一个团队先后让 Agent 接了 5 个工具，每次集成都要写：OAuth 鉴权、参数序列化、错误重试、返回解析。当第 6 个工具要接入时，他们意识到这 5 份对接代码互相独立、无法复用，维护成本爆炸。这触发了他们对 MCP 方案的调研——他们想要的，是『工具一次封装、处处复用』，而不是每接一个就重写一遍。",
    },
    "s3-mcp-c1-s2": {
        "content": "MCP（Model Context Protocol，模型上下文协议）的承诺，是给『模型—工具』通信立一个统一标准：所有工具能力都封装成符合 MCP 规范的 Server，任何支持 MCP 的客户端（AI 应用、IDE、Agent）都能即插即用。就像 USB 标准化了外设连接一样，MCP 标准化了『模型访问外部工具』的方式——你不用再为每个工具写定制对接，工具提供商也不用为每个 AI 应用适配。\n\n具体带来的收益：一是复用——同一个 MCP Server（比如文件系统 Server）能被无数客户端使用，一次开发到处可用；二是生态——社区已经涌现大量现成 Server（数据库、GitHub、浏览器、搜索…），开箱即用；三是解耦——应用的『智能逻辑』与『工具对接』分离，换工具、加工具都不动应用主逻辑。理解『一次封装、处处复用』，你就抓住了 MCP 的核心价值。",
        "case": "一家公司把内部『订单查询系统』封装成一个 MCP Server。此后：公司的智能客服 Agent 能用它，内部 AI 运营助手也能用它，甚至员工的 AI 编程工具（如 IDE）都能直接调用——同一个 Server，多个客户端复用，零重复开发。对比之前每个应用都要为订单系统写一遍对接，MCP 一次封装解决了所有后续接入。",
    },
    "s3-mcp-c2-s1": {
        "content": "MCP 的架构是清晰的三层：Client（客户端）——运行 AI 应用的进程，负责向 Server 发起请求、接收结果；MCP Server——把外部能力（文件系统、数据库、API）封装成标准化可调用的工具，一个 Server 通常对应一类资源；外部资源——Server 底层连接的真实系统（文件、数据库、GitHub 等）。通信协议采用 JSON-RPC（一种轻量 JSON 请求响应协议），Client 通过它向 Server 发起『调用工具』『读取资源』等请求。\n\n理解这个分层，关键是『MCP Server 是个中间翻译层』：Client 不用关心底层资源怎么访问，只需按 MCP 标准向 Server 要能力；Server 负责把标准请求翻译成对真实资源的操作，再返回结果。分层带来灵活与安全——换底层资源只动 Server，Client 无感知；高危操作也可在 Server 层统一管控。",
        "case": "把『数据库访问』封装成 MCP Server 后：Client 只需说『执行查询：SELECT …』，Server 负责连接数据库、执行、返回结果，甚至可以在 Server 层统一做 SQL 白名单校验（只允许 SELECT，禁止 DELETE）。这样 Agent 获得了访问数据库的能力，又不用直接面对数据库凭证和权限——分层同时带来了能力与安全。",
    },
    "s3-mcp-c2-s2": {
        "content": "MCP 协议定义了三种核心能力类型，理解它们才能设计好 Server。Tools（工具）：可执行的操作，比如『查询订单』『发送消息』——有输入参数、会返回结果，对应 Agent 的『手脚』，这是最常用的类型。Resources（资源）：可读取的数据，比如一个文件、一张表——Agent 可以读取但不直接执行操作，对应 Agent 的『眼睛』。Prompts（提示词模板）：可复用的提示词片段——把常用指令模板化，供客户端直接引用，对应 Agent 的『经验库』。\n\n三者分工清晰：Tools 做事、Resources 看数据、Prompts 提供方法论。设计 MCP Server 时的第一件事，就是判断你要暴露的能力属于哪一类——是『让 Agent 执行动作』（Tool）还是『让 Agent 读取数据』（Resource）还是『给 Agent 现成的处理模板』（Prompt）。分清类别，才能把能力封装得恰到好处。",
        "case": "做一个『文件管理』MCP Server：把『读文件』『列目录』封装为 Resources（Agent 查看数据），把『创建文件』『修改文件』封装为 Tools（Agent 执行动作），再提供一个『总结文件要点』的 Prompt 模板。这样一个 Server 同时给 Agent 提供了观察能力、操作能力和方法论，且每类能力的权限边界（读 vs 写）天然分开，便于安全管控。",
    },
    "s3-mcp-c3-s1": {
        "content": "MCP 的生态价值在于『现成 Server 多到用不完』，掌握常用的几类就能覆盖大多数需求。文件系统 Server：让 Agent 读写本地/远端文件（开发调试、文档处理常用）。数据库 Server：让 Agent 查询数据库（SELECT 白名单、读写隔离可控）。GitHub Server：让 Agent 查仓库、看 issue、提 PR（研发助手核心）。此外还有搜索、浏览器、Slack/钉钉、时间/日历等各类 Server，社区还在快速增长。\n\n使用方式很简单：把需要的 Server 配置进你的客户端（如 Claude Desktop、IDE 插件、自定义 Agent 的 MCP 配置），声明暴露哪些工具，Agent 就能直接调用。选择 Server 的原则：优先用生态成熟的开源 Server（维护好、坑少），不要什么都自己造；确认 Server 的权限边界符合你的安全要求（文件 Server 只挂需要操作的目录）。",
        "case": "一个研发助手通过配置三个现成 Server 快速获得能力：GitHub Server（查代码、看 PR）、文件系统 Server（读写项目文件）、搜索 Server（查文档资料）。整个配置过程只需声明 Server 与权限，没有写一行对接代码，Agent 就具备了「查代码—改代码—搜资料」的完整研发能力。这就是现成 Server 生态的效率红利。",
    },
    "s3-mcp-c3-s2": {
        "content": "配置与调试 MCP Server，是实际使用中最常遇到的环节。配置要点：在客户端配置里声明要用的 Server（一般包括 Server 名称、启动命令或地址、暴露的工具清单）；声明时明确权限边界——只暴露必要工具、限定操作范围（文件 Server 只挂指定目录、数据库 Server 只开 SELECT）。调试要点：用客户端自带的 MCP 调试工具（如 IDE 的 MCP 面板）逐个验证工具能否被正确调用；检查 Server 日志定位调用失败原因（参数格式错、权限不足、连接失败）。\n\n常见的坑：Server 启动了但客户端连不上（端口/地址配错）；工具调用报权限错误（配置里没声明该工具）；参数格式与文档不符（Server 版本差异）。调试思路遵循『先验证连接、再验证单个工具、最后验证组合流程』，逐步缩小问题范围。",
        "case": "配置文件系统 Server 时只把 `/tmp/workspace` 挂给它，Agent 就只能在那个目录读写——想让它碰别的目录会被拒绝，这是安全边界。调试时发现 Agent 调用『写文件』失败，查看 Server 日志发现是权限配置漏了 write 权限声明，补上后正常。配置与调试的本质，是『先圈定安全范围、再逐个验证能力』。",
    },
    "s3-mcp-c4-s1": {
        "content": "MCP 与 Function Calling 的关系经常被混淆，厘清它们的关键是『分层』。Function Calling 是 LLM 的一项能力——模型能根据工具定义，输出『调用哪个函数、传什么参数』的结构化决策；它解决的是『模型怎么选工具』。MCP 是工具的分发协议——它规定『工具怎么被定义、怎么被调用』的标准；它解决的是『工具怎么被接入』。\n\n两者是互补而非替代：MCP 帮你把各种工具标准化成统一的 Server，Function Calling 让模型在众多标准工具中做出选择。一个完整的 Agent 链路是：MCP 把工具标准化接入 → 模型用 Function Calling 决定调用哪个工具 → 程序执行 → 结果回传。所以不必纠结『用 MCP 还是 Function Calling』——你通常两个都用：Function Calling 是模型的决策机制，MCP 是工具接入的标准化底座。",
        "case": "一个 Agent 接入了 5 个 MCP Server（共 20 个工具）。模型决定调哪个工具，靠的是 Function Calling 能力——它比较 20 个工具的描述，选出最合适的并生成调用参数。而 MCP 负责的是『这 20 个工具以统一标准接入，不用为每个写对接』。两者分工清晰：Function Calling 管『决策』，MCP 管『接入』。",
    },
    "s3-mcp-c4-s2": {
        "content": "MCP 与 A2A 的角色区分，是理解 AI 集成全景图的关键。MCP（Model Context Protocol）管的是『Agent ↔ 工具』——让 Agent 能统一访问外部工具、文件、数据库等资源，是『Agent 连世界』的协议。A2A（Agent-to-Agent）协议管的是『Agent ↔ Agent』——让不同的 Agent 之间能互相通信、协作、交接任务，是『Agent 连同行』的协议。两者一个连『工具』，一个连『Agent』，是不同层面的协作标准。\n\n当前生态中，MCP 更成熟、应用更广（工具接入是当前最迫切的需求）；A2A 尚在早期（多 Agent 协作还多以框架内实现为主）。理解这个区分，有助于你在设计系统时对号入座：需要接外部工具 → MCP；需要多个 Agent 协作 → A2A 或框架内编排（LangGraph 等）。两者的角色边界，也对应了阶段三（工具接入）与阶段四（多 Agent）的学习重心。",
        "case": "一个企业智能助手：通过 MCP 接入公司数据库、文件、工单系统（连工具）；同时它内部由『客服 Agent』和『财务 Agent』两个子 Agent 协作，二者通过 A2A 风格的消息机制交接（用户问报销，客服 Agent 把任务交给财务 Agent）。MCP 管『接工具』、A2A 管『接 Agent』，两者各司其职，共同构成完整的企业 AI 集成。",
    },
    "s3-mcp-c5-s1": {
        "content": "动手实践 MCP 的最小集成：把一个文件系统 MCP Server 接入一个最小 Agent，让它完成『读文件 → 总结 → 写新文件』的闭环任务。具体步骤：启动（或声明）一个文件系统 MCP Server；在 Agent 客户端里配置暴露它的工具；向 Agent 提问『读取 /workspace/note.md 并总结成 5 条要点写入 summary.md』；观察 Agent 依次调用『读文件』工具（拿到内容）、调用『写文件』工具（写入总结）。\n\n这个练习验证了 MCP 集成的核心链路：配置声明 → 工具调用 → 真实文件操作 → 结果反馈。跑通后，你会直观理解『MCP Server 是中间翻译层』——Agent 从头到尾没接触文件路径底层，只是通过 Server 的标准化工具完成了操作。这为后续把各种能力封装成 MCP Server、或接入更复杂的现成 Server 打下了实践基础。",
        "case": "集成文件系统 Server 后提问『总结 note.md』，观察日志看到 Agent 的调用链：`调用 read_file(path=/workspace/note.md) → 返回内容 → 模型生成 5 条要点 → 调用 write_file(path=summary.md) → 写入成功`。整个流程 Agent 通过标准 MCP 工具完成，没有写一行文件操作代码——这就是『Agent 通过 MCP 获得真实能力』的最小实证。",
    },
    "s3-mcp-c5-s2": {
        "content": "MCP 带来强大能力的同时，也放大了安全责任——『工具越强，约束越要严』。核心安全边界：一是最小权限——Server 只暴露必要工具、只挂必要目录/库/表，Agent 拿不到它不该碰的资源；二是操作白名单——高危操作（写文件、删数据、发消息）要么不放行、要么加审批；三是执行可审计——记录 Agent 的每次工具调用，事后可追溯；四是数据边界——敏感数据不通过 Server 暴露给模型。\n\n工程实践三条：部署 Server 用独立低权限账号；把『读』与『写』能力分开声明（Agent 默认只读，需要写时再单独授权）；高危工具配合人工确认。记住 MCP 只是协议，它不负责安全——安全是你配置 Server 时主动设计的。给 Agent 的每一分能力，都要问一句『它真的需要吗？万一被滥用会怎样？』",
        "case": "一个文件管理 Agent 默认只有『读』权限，用户明确要求『把文件 A 的内容追加到文件 B』时才触发『写』授权，且写操作进入审计日志。这样的设计下，即使 Agent 被恶意提示词诱导，也无法随意篡改文件——因为写能力是受控的。对比一个给 Agent 全权限文件 Server 的场景，一旦被诱导执行 `rm -rf` 就是灾难。安全边界不是可选项，是 MCP 集成的第一原则。",
    },

    # ================= s3-skills Agent Skills 能力技能化 =================
    "s3-skills-c1-s1": {
        "content": "大 Prompt 的弊端，随着 Agent 能力增多会越来越明显。把所有能力写进一个巨大的 system 提示词，会带来四类问题：一是上下文臃肿——几十项能力描述占用大量 Token，挤占了真正的业务数据空间；二是互相干扰——『写周报』和『做摘要』的描述堆在一起，模型反而分不清边界，容易选错；三是维护困难——改一个能力要动整段提示词，牵一发动全身；四是能力无法复用——同一个能力（如『按模板生成报告』）在不同 Agent 里都要复制一遍提示词。\n\n这些问题在能力少时（2-3 项）不致命，一旦 Agent 需要十几项能力，大 Prompt 就会变成一团理不清的乱麻。Agent Skills 的提出，正是针对这个困境：把能力从『塞进一个 Prompt』改为『拆成独立的技能模块』，每个技能封装自己的输入、处理、输出，Agent 按需调用。",
        "case": "一个客服 Agent 的 system 提示词最初塞了 8 项能力（查订单、查物流、算优惠、售后、投诉、知识库、翻译、闲聊），共 2000 多字，模型经常把『查物流』和『查订单』搞混，且每次请求都要为这 2000 字买单。把能力拆成独立 Skills 后，模型只在需要时才加载对应 Skill 的描述，上下文清爽了，选错的概率也降了。大 Prompt 的乱，是拆分动机的根源。",
    },
    "s3-skills-c1-s2": {
        "content": "Skill 的思想，是把 Agent 的每一项能力做成『可复用、可组合、职责单一』的独立模块。一个 Skill 封装的是『输入 → 处理 → 输出』的完整闭环：输入是它需要的数据（如『周报要点』），处理是它的执行逻辑（如『按模板组织成专业周报』），输出是它的产物（如『成型的周报文本』）。Agent 面对任务时，判断该调用哪个 Skill，把当前任务的输入喂给它，拿到输出再继续。\n\nSkill 与函数的区别在于：Skill 通常以『提示词模板 + 可选工具』的形式存在，是『能力封装』而非代码函数；它的输入输出是自然语言级的。Skill 带来的价值：复用（一个 Skill 到处用）、组合（多个 Skill 串成复杂流程）、维护（改一个 Skill 不影响其他）、测试（单个 Skill 可独立验证）。这套思想与工程上的『模块化』一脉相承——把复杂系统拆成小单元，每个单元职责清晰。",
        "case": "把『周报生成』做成一个 Skill：输入是本周的零散记录，输出是格式化的周报。这个 Skill 被多个场景复用——行政要用它、销售要用它、研发也要用；且不同团队可以基于同一个 Skill 框架微调各自的模板，而不用重写。Skill 让『能力』从『写在某段 Prompt 里』变成了『可复用的资产』。",
    },
    "s3-skills-c2-s1": {
        "content": "Skill 描述的质量，是决定 Agent 能否正确选用的第一要素——描述写得糊，Agent 就选错或不用。一个合格的 Skill 描述要回答四件事：功能（这个 Skill 做什么）、输入（它需要什么数据、什么格式）、输出（它产出什么、什么格式）、边界（什么时候用它、什么时候不要用它）。比如『周报生成』的描述：『输入：本周工作的零散记录列表；输出：按公司模板生成的周报文本；适用：用户需要汇总每周工作、写周报时；不适用：需要分析数据趋势（那是数据分析 Skill 的职责）』。\n\n写描述的黄金法则：站在 Agent 的视角想——它面对一个任务时，凭这段描述能不能判断『该不该用这个 Skill』。描述里的每一个词都可能影响 Agent 的决策，所以要像写 API 文档一样严谨：触发条件具体、边界明确、示例（如果有）贴切。描述不清晰 = Agent 选错 = 任务失败，这条规律在工具描述、Skill 描述、MCP 工具描述里一以贯之。",
        "case": "两个『摘要』Skill，描述一个写『对文本做总结』，另一个写『输入：任意文本；输出：按『背景-问题-结论』三段式的摘要，≤200 字；适用：用户要求提炼要点、缩写长文、生成执行摘要时』。实测中，Agent 对后者的选用准确率显著更高——因为它能准确判断『这个任务符合那个 Skill 的输入输出』。描述不是可选项，是 Skill 好不好用的决定性因素。",
    },
    "s3-skills-c2-s2": {
        "content": "Skill 的边界，是描述里最容易忽略、却最能提升选用准确率的部分。写清楚『这个 Skill 不做什么』，能阻止 Agent 拿它硬套不合适的任务。比如翻译 Skill 的边界写『仅处理中英文互译，不处理编程语言、不处理口语化润色』，Agent 面对『帮我把这段代码注释翻译成中文』时，就会判断这不属于翻译 Skill 而走代码处理路径。\n\n边界的作用机制：Agent 在多个 Skill 之间选择时，本质是在比较『每个 Skill 的描述与当前任务的匹配度』——边界写得越清楚，匹配判断越精确，选错概率越低。尤其当两个 Skill 功能相近时（两个都是『生成报告』，一个偏运营一个偏财务），边界是区分它们的关键。实践中，写完 Skill 的『功能描述』后，一定要再问一句：它的边界是什么？把它明确写出来。",
        "case": "客服 Agent 有『订单查询』和『物流查询』两个相近 Skill。给物流查询加边界『仅回答包裹位置、配送进度；涉及金额、退款、订单状态请用订单查询 Skill』后，用户问『我的快递到哪了』稳定命中物流 Skill，问『这笔钱什么时候退』稳定命中订单/退款 Skill，两个相近 Skill 不再互相抢活。边界让相近的能力各司其职。",
    },
    "s3-skills-c3-s1": {
        "content": "多 Skill 协作，是把简单能力组合成复杂能力的核心。三种基本组合模式：串联（Pipeline）——前一个 Skill 的输出是后一个的输入，如『摘要 → 翻译 → 润色』；条件选择（Branching）——根据任务类型选择不同 Skill，如『按数据类型选统计 Skill 或文本 Skill』；并行（Parallel）——多个独立的子任务同时用不同 Skill 处理，最后汇总。实际任务往往是几种模式的组合。\n\n设计组合时，关键是『Skill 的输出格式要为下游做好准备』——串联时前一个 Skill 产出的格式要能被下一个 Skill 直接消费（所以输出格式的约定很重要）；条件选择时判断条件要清晰；并行时要能汇总。组合的复杂度同样受成本约束——每多串联一个 Skill 就多一次 LLM 调用，所以『能用一个 Skill 完成就别拆两个』，组合是为了解决单 Skill 解决不了的问题，不是为了炫技。",
        "case": "一个『把会议纪要转成英文行动清单』的任务，由三个 Skill 串联：纪要整理 Skill（把杂乱会议记录整理成要点）→ 行动项提取 Skill（从要点里挑出『谁、何时、做什么』的行动项）→ 翻译 Skill（把行动项翻成英文）。每个 Skill 只做一件事，输出格式对接顺畅，最终产物规范可用。组合的价值，就是让每个 Skill 保持简单，复杂由组合产生。",
    },
    "s3-skills-c3-s2": {
        "content": "编排（Orchestration）是组合的灵魂——Skill 是零件，编排是装配。编排负责三件事：判断什么时候用哪个 Skill（选型）、怎么传参（把当前任务的上下文映射到 Skill 的输入）、怎么汇总（把多个 Skill 的输出整理成最终结果）。编排可以由 Agent 自己完成（模型自主决定调用顺序），也可以由外部流程固定（程序决定流程，模型执行每步）。\n\n两种编排方式的取舍：Agent 自主编排灵活，能应对不可预知的任务流，但不可控、成本高；外部流程编排可控、可预测、省 Token，但僵硬，遇到计划外情况会卡住。成熟方案通常是混合——外部流程定骨架（确定性步骤），Agent 在骨架构内自主选 Skill（灵活性细节）。理解『Skill 是能力、编排是装配』，你就抓住了 Agent 架构设计的核心：把能力切得足够小，把流程设计得足够清晰。",
        "case": "一个项目周报 Agent 的编排：外部流程固定『①调用数据汇总 Skill ②调用分析 Skill ③调用周报生成 Skill』；每步内部，Agent 可以自主决策——比如数据汇总 Skill 里，Agent 判断数据是文本还是表格，决定用哪个子路径。固定骨架保证流程稳定，Agent 自主保证细节灵活。这就是『流程编排 + 能力自主』的混合编排。",
    },
    "s3-skills-c4-s1": {
        "content": "Skill、Tool、RAG 是三个常被混淆的概念，厘清它们对设计 Agent 至关重要。Skill（技能）——能力的封装，是一套『输入→处理→输出』的方法论（通常以提示词模板呈现），回答的是『会做一件事』；Tool（工具）——调用外部系统的接口，是让 Agent 访问真实世界的通道（API、数据库、文件），回答的是『能操作什么』；RAG（知识）——给 Agent 补充资料，是让 Agent 基于真实文档回答的机制，回答的是『知道什么』。\n\n三者的关系是组合而非互斥：一个 Skill 内部可以调用 Tool（如『订单查询 Skill』内部调用订单 API），也可以走 RAG（如『制度问答 Skill』内部先检索再回答）。理解这个分层，你就知道在设计 Agent 时该往哪一层加东西：要加一项『会做的事』→ 加 Skill；要连一个新系统 → 加 Tool；要让 Agent 懂你的资料 → 加 RAG。",
        "case": "『企业制度问答 Skill』的解剖：它内部用 RAG 检索制度文档（知道什么——资料），必要时调用 OA 系统的查询工具（能操作什么——真实系统），整体对外是一个『输入问题、输出带引用的答案』的 Skill（会做什么——一件事）。三层各司其职，共同构成一个完整的 Agent 能力。",
    },
    "s3-skills-c4-s2": {
        "content": "看一个组合示例，体会 Skill 内部如何综合运用工具与 RAG。『企业问答 Skill』对外接收用户问题，内部流程：先用 RAG 检索企业内部知识库（找资料）；如果问题涉及实时数据（如『我本月报销到账了吗』），再调用财务系统查询工具（连真实系统）；最后把检索到的资料 + 工具结果一起组织成回答。对外它是『一个问题→一个答案』的完整能力，对内它协调了『RAG 找资料 + 工具查数据』两条管线。\n\n这个示例揭示了 Skill 设计的精髓：对外接口简单（输入输出清晰），对内实现可以很复杂（自由组合 RAG、工具、推理）。用户和上层 Agent 都不需要关心它内部怎么实现——这正是封装的本质。当你设计的 Skill 具备『外部简单、内部强大』的特质时，它就具备了高复用价值。",
        "case": "用户问企业问答 Skill『项目立项的审批流程是什么，我这个月的差旅什么时候报销』。Skill 内部：RAG 检索到《项目立项制度》关于审批流程的章节；同时调用财务工具查询差旅报销状态；最后把制度流程与报销状态一并回答。用户一个复杂问题，Skill 内部协调了两条能力管线。对外它就是『一个能回答企业问题的 Skill』。",
    },
    "s3-skills-c5-s1": {
        "content": "把 Skill 思想落地的五步封装流程：①定义输入——明确这个 Skill 要接收什么数据、什么格式；②写处理提示词——针对该 Skill 的专属指令（角色、逻辑、约束），这是 Skill 的核心；③定输出格式——明确产出结构（文本、列表、JSON），为下游复用做准备；④写描述与边界——用上一节的方法把『功能、输入、输出、边界』写清楚，这是让 Agent 能选对它的关键；⑤测试——构造典型输入，验证 Agent 能否正确调用并产出合格结果。\n\n封装时的两个提醒：一是『一次只做一个 Skill』——先解决一个高频、单一的任务，不要急着把一堆能力塞进一个 Skill（那样又退回了大 Prompt）；二是『从实际使用中打磨』——Skill 的描述和提示词要在真实调用中迭代，观察 Agent 选错或输出不合格的地方，针对性修改。",
        "case": "把『周报生成』按五步封装：输入=本周记录列表；处理提示词=『你是资深运营，把记录按「数据亮点-问题-计划」组织成周报』；输出=Markdown 周报；描述边界=『适用：汇总周工作；不适用：生成月报/分析趋势』；测试=喂 3 组不同记录验证输出质量。一轮测试发现输出偏口语化，在提示词里补『用书面语、分点列出』后达标。五步流程让 Skill 从想法变成可复用的合格能力。",
    },
    "s3-skills-c5-s2": {
        "content": "质量验证是 Skill 开发中容易被跳过、却最能拉开差距的环节。核心方法是『对比测试』：构造一个测试集（几组典型任务），分别用『描述清晰』和『描述模糊』两个版本的 Skill 跑一遍，统计 Agent 的选用正确率与输出合格率。这一步能让你直观看到：同一个 Skill，只是描述质量不同，Agent 的选用准确率可能有 20-30 个百分点的差距。\n\n验证的扩展：还可以对比不同处理提示词（同描述不同逻辑）的输出质量；对比 Skill 与其他方案（大 Prompt）的效果与成本。把测试结果记录下来，作为 Skill 迭代的依据——这既是质量工程，也是你在面试时能讲出的『我如何用数据验证优化』的素材。Skill 开发的成熟度，体现在『每一次改动都有测试支撑』，而不是凭感觉调。",
        "case": "对一个『邮件生成 Skill』做对比测试：描述模糊版（『生成一封邮件』）的选用准确率 62%，输出里有 20% 需要返工；描述清晰版（明确输入输出格式、触发条件、边界）选用准确率 92%，返工率降到 5%。同一份提示词逻辑，仅靠描述质量就拉开了差距。这个测试结果也成了团队优化其他 Skill 的参照标准。",
    },

    # ================= s3-langchain LangChain 框架 =================
    "s3-lc-c1-s1": {
        "content": "LangChain 的设计思想，是把 LLM 应用拆成『可插拔的组件』，再通过『链』把组件串起来。它的组件化体现在：模型（Model）——LLM 是可替换的组件，换个模型只换配置；检索器（Retriever）——从向量库取回资料的组件，可以替换底层向量库；工具（Tool）——让 Agent 调用外部能力的组件；记忆（Memory）——管理上下文的组件。每个组件都有标准接口，可以独立替换、独立测试。\n\n『链』（Chain）则是把这些组件按业务逻辑串起来：一条 RAG 链把『检索器 + 模型 + 提示词模板』串成『输入问题 → 检索资料 → 生成回答』；一条 Agent 链把『模型 + 工具 + 记忆』串成『输入任务 → 循环调用工具 → 输出结果』。组件化的收益：复用（写好的组件到处用）、替换（换模型/向量库只动配置）、组合（不同组件拼成不同流程）、测试（每个组件可单独验证）。理解『组件 + 链』，你就掌握了 LangChain 的整套心智模型。",
        "case": "同一个 RAG 功能，用 LangChain 组装时：Retriever 组件指向 Chroma（本地）或 FAISS（换库只改一行）；Model 组件指向 Ollama（开发）或 GPT（上线，只改配置）；提示词模板复用同一个。对比手写版——换向量库要改存储代码、换模型要改调用代码，LangChain 的组件化让『替换』变成了『改配置』。这就是框架相对手写的核心价值。",
    },
    "s3-lc-c1-s2": {
        "content": "『手写 vs 框架』不是对立，而是学习与生产的两阶段。手写的价值在于懂原理：你亲手实现过加载、切分、向量化、检索、回答，就知道每一步在做什么、坑在哪里，遇到框架不理解的报错能自己排查。框架的价值在于提效率：把样板代码（协议处理、参数组织、组件组合）封装好，让你专注业务逻辑。\n\n两者正确的衔接方式是『先手写，再框架化』：阶段三先手写 RAG 和 Agent（理解原理），再用 LangChain 重写（体会框架解决了什么——它替你省了哪些重复劳动）。这样你用框架时是『知道它在替你做什么』而不是『黑盒地照模板写』。一个实用判断标准：如果你能讲清楚『这一行框架代码等价于我手写里的哪几步』，说明你真正理解了框架——这也是面试官最想听到的能力。",
        "case": "手写 RAG 时，检索后拼装提示词、组织多轮 messages、处理工具结果回传，这些零散逻辑占了大半代码。用 LangChain 重写后，`create_retrieval_chain` 一行就完成了『检索 + 拼装 + 回答』的串联。这时你会明白：框架省掉的是『协议与组织的样板』，不是『理解原理的必要性』——原理懂没懂，看你能不能解释那行代码在做什么。",
    },
    "s3-lc-c2-s1": {
        "content": "Document Loaders 是 LangChain RAG 线的起点，负责把不同来源的文档统一加载成标准化的 Document 对象。三个最常用的 Loader：TextLoader——读取纯文本文件，最简单；PyPDFLoader——解析 PDF（有页级元数据）；DirectoryLoader——批量加载一个目录下的所有文档（可指定 glob 匹配），一次性把知识库文档都读进来。所有 Loader 的产出都是统一的 Document 对象，包含 page_content（正文）与 metadata（元数据，如来源路径、页码），这为后续『引用溯源』打下了基础。\n\n使用要点：Loader 只是『读进来』，不做清洗——脏数据（页眉页脚、乱码）要靠后续处理或在加载时配置参数处理；metadata 是你在加载时主动维护的资产（打上来源、版本标签），对后续检索和溯源至关重要。先把『批量加载 + 元数据标记』做好，后面的 RAG 管线才有好原料。",
        "case": "用 DirectoryLoader 加载公司制度文件夹：`DirectoryLoader('./docs', glob='**/*.pdf')` 一次性把 20 份 PDF 读进来，每份转成 Document 并自动带上『source: ./docs/考勤制度.pdf』的元数据。后续检索命中某段时，这段元数据就会告诉你答案来自哪份文件——这就是引用溯源的第一步，也是你未来『回答带出处』的基础。",
    },
    "s3-lc-c2-s2": {
        "content": "RecursiveCharacterTextSplitter 是 LangChain 最常用的文本分块器，它的设计思想是『按优先级递归切分』：依次尝试用一组分隔符（如 '\n\n'、'\n'、句号、空格）切分，优先保留完整段落，段落太长再往下拆。这样切出来的块『尽量是完整的语义单位』——不会把一句话拦腰切断，同时又能控制块大小。核心参数：chunk_size（每块的最大字符数）与 chunk_overlap（相邻块重叠的字符数，防止语义在切分边界丢失）。\n\n为什么它是『最常用』？因为真实文档结构复杂（段落、列表、标题混排），固定按字数切会破坏语义，而递归切分在『保留结构』与『控制大小』之间取得了良好平衡。使用建议：chunk_size 从 300-500 起步，chunk_overlap 设为 50-100；切分后抽查几块，确认没有明显的语义断裂。分块质量直接影响检索效果，值得花时间调。",
        "case": "一份 Markdown 制度文档，用 RecursiveCharacterTextSplitter（chunk_size=400, overlap=50）切分：优先按标题、段落切，一段完整的『报销标准』被保留在同一块里（而不是被硬切到两块的边界处）。对比固定 400 字硬切——『报销标准』恰好在边界被切断，检索『报销上限』时两块都不完整，回答缺了关键数字。递归切分保住的，正是这类语义完整性。",
    },
    "s3-lc-c3-s1": {
        "content": "Embeddings 组件是 LangChain 对『文本向量化』的标准封装，最核心的价值是『一行切换供应商』。OllamaEmbeddings——用本地 Ollama 的向量模型（如 nomic-embed-text），免费、数据不出本地，适合开发调试；OpenAIEmbeddings——用云端向量模型，质量更高、随规模扩展，适合生产。两者都实现了同一个接口：`embed_query(文本) -> 向量`，所以业务代码不需要区分底层是谁，换模型只是换一行实例化代码。\n\n在 LangChain 里，Embeddings 组件被 VectorStore 使用：建库时把每块文本向量化写入，检索时把查询向量化再匹配。选本地还是云端，权衡逻辑与阶段一 Ollama 章节一致——开发用本地（免费试错）、生产看数据敏感性与质量要求。记住 Embeddings 是『语义质量』的基础——向量模型的语义理解能力，直接决定了检索能匹配到多相关的资料。",
        "case": "开发阶段用 `OllamaEmbeddings(model='nomic-embed-text')` 跑通 RAG（免费、数据不出本地）；上线时改成 `OpenAIEmbeddings()` 只改一行，其余代码不动。对比手写——手写时换向量模型要改向量化的调用代码、可能还要改向量维度相关的存储；LangChain 的封装让切换变成了配置级操作。这就是 Embeddings 组件化的价值。",
    },
    "s3-lc-c3-s2": {
        "content": "VectorStores 组件管理『向量的存取与检索』，LangChain 把不同向量库封装成统一接口。两个常用选择：Chroma——本地、零配置、适合原型与中小规模；FAISS——同样是本地库，检索性能更优，适合更大规模。核心操作：`Chroma.from_documents(docs, embedding)` 一把梭——把 Document 列表向量化并建库（可指定 persist_directory 持久化）；检索用 `store.as_retriever()` 得到一个标准检索器（Retriever），后续链可以直接用它『取回相关文档』。\n\nas_retriever() 是关键衔接点：它把『向量库』包装成『检索器』——统一接口 `get_relevant_documents(查询) -> 文档列表`，可以让检索器配置 top-k（`as_retriever(k=4)`）、元数据过滤等。理解『VectorStore 管存取、Retriever 管取回』的分工，你就知道在链里用的是 Retriever，而底层向量库随时可换。",
        "case": "用 `Chroma.from_documents(split_docs, embedding, persist_directory='./chroma_db')` 建好知识库并持久化。之后在 RAG 链里 `retriever = vectorstore.as_retriever(k=4)`，链调用检索器就能取回最相关的 4 块文档。换向量库时只需把 `Chroma.from_documents` 换成 `FAISS.from_documents`，检索器接口不变，链的其余代码一行不动——底层替换、上层无感。",
    },
    "s3-lc-c4-s1": {
        "content": "LangChain RAG 链由两条链配合完成：create_retrieval_chain 与 create_stuff_documents_chain。create_stuff_documents_chain（文档链）负责『把检索到的文档 + 问题组织成一次模型调用』——它把文档全部『塞进』提示词（stuff 即塞入），配上一个模板，让模型基于文档回答。create_retrieval_chain（检索链）则负责『先调用检索器取回文档，再交给文档链生成回答』——它把检索与生成串联成一条完整的 RAG 流程。\n\n两条链的分工：检索链是『编排者』（检索→喂给文档链→返回），文档链是『执行者』（拼装文档→调模型→产出回答）。使用上：先配好文档链（含提示词模板与模型），再用检索链把它与检索器包起来。理解这个组合，你就掌握了 LangChain RAG 的标准拼装方式——这也是从『手写检索问答』到『框架化 RAG』的典型升级路径。",
        "case": "拼装一段 RAG 链：`doc_chain = create_stuff_documents_chain(llm, prompt)`；`rag_chain = create_retrieval_chain(retriever, doc_chain)`。调用 `rag_chain.invoke({'input': '年假怎么休'})`，内部自动完成『检索相关文档 → 文档塞进提示词 → 模型基于文档回答』，一步返回答案。对比手写——这行调用等价于手写版的『检索循环 + 上下文拼装 + 模型调用』三块代码。",
    },
    "s3-lc-c4-s2": {
        "content": "引用溯源在 LangChain 里要做到『元数据一路传递』：从加载（Loader 打上 source 标签）→ 切分（Splitter 保留元数据）→ 入库（VectorStore 存元数据）→ 检索（Retriever 返回带元数据的文档）→ 回答（把元数据给模型，要求标注出处）。关键点：一是加载时把来源写进 metadata（前文 DirectoryLoader 的例子）；二是提示词模板里要求模型引用来源（如『请标注依据的文档，例如（来源：考勤制度）』）；三是最终响应里可以把 source 一并返回给前端展示。\n\nLangChain 的文档对象从头到尾携带 metadata，让『来源』贯穿整条链——这是框架相对手写的又一个便利（手写时你要自己维护每块的来源字段）。实现引用溯源后，你的 RAG 回答就『可验证、可追溯』了，这是企业知识库场景的必备能力，也是项目作品里的一个亮点。",
        "case": "RAG 链的提示词模板里加一句『回答时在末尾标注依据，如（依据《考勤制度》）』。调用后模型的回答是『事假按天扣除工资，依据《考勤制度》第三章』。同时 retrieve 返回的文档自带 metadata['source']，接口可以把来源列表一并返回给前端做引用展示。整个链路里，来源信息从加载到回答一路贯穿，零额外成本地实现了引用溯源。",
    },
    "s3-lc-c5-s1": {
        "content": "LangChain Agent 线的起点是 @tool 装饰器：用它把普通 Python 函数声明成 Agent 可调用的工具。核心机制：函数名自动成为工具名，docstring 自动成为工具描述，函数签名里的类型注解自动生成参数 schema。也就是说，你只要写好『一个带清晰 docstring 和类型注解的函数』，@tool 就把它包装成工具——省去了手写 JSON schema 的麻烦。\n\n因为 docstring 就是给模型看的描述，写 @tool 函数时最该用心的是 docstring：写清楚功能、参数含义、使用边界（对应工具描述的黄金法则）。参数的类型注解要准确（str/int/float），必要时用枚举或默认值约束取值。@tool 是对『工具定义』的工程化简化——原理与手写的工具 schema 完全一致，只是把『函数 ↔ schema』的映射自动化了。",
        "case": "对比手写与 @tool：手写要在 schema 里写 `{'name':'get_weather','description':'...','parameters':{'type':'object','properties':{'city':{'type':'string'}}}}`；@tool 只需 `@tool\ndef get_weather(city: str) -> str:\n    \"\"\"获取指定城市实时天气，city 为城市名。\"\"\"\n    return weather_api(city)`。docstring 自动成为描述、类型注解自动成为 schema——一行装饰器完成了手写的整个 schema 定义，且描述质量直接取决于你 docstring 写得够不够好。",
    },
    "s3-lc-c5-s2": {
        "content": "AgentExecutor 是 LangChain Agent 的执行引擎，负责运行『模型决策 → 调用工具 → 结果回传』的循环，直到任务完成。它替你处理了执行循环的所有工程细节：最大迭代次数（防止模型无限循环调工具）、中间步骤管理（记录每一步的 Thought/Action/Observation）、调试输出（可以打印或返回中间过程，方便排查问题）。\n\n理解 AgentExecutor，关键是把它对应到阶段二手写的最小循环：它就是那个循环的框架化封装——你不再手写循环控制，而是配置它。使用要点：设置 max_iterations 控制循环上限（工程护栏）；开启 verbose 或检查返回的 intermediate_steps 查看中间过程（调试利器）；工具与记忆都通过配置注入。掌握 AgentExecutor 的『循环控制 + 中间步骤』，你就理解了 LangChain Agent 的运行机制——这为阶段三下一节 LangGraph 的『更精细的循环控制』做好了铺垫。",
        "case": "用 `create_tool_calling_agent(llm, tools, prompt)` + `AgentExecutor(agent=agent, tools=tools, max_iterations=5, verbose=True)` 跑一个客服 Agent。verbose 模式下，控制台会打印每一步：`Thought: 需要查订单 → Action: get_order(订单号) → Observation: 订单已发货`，让 Agent 的整个决策过程可视化。这就是 AgentExecutor 帮你省下的工程细节——循环、护栏、可观测性。",
    },
    "s3-lc-c6-s1": {
        "content": "LangChain 的记忆组件负责管理多轮对话的上下文，新版 API 的核心是 RunnableWithMessageHistory。它的机制：把『会话历史』与『处理逻辑』绑定——每次调用时传入 session_id，组件从历史存储里取出该会话的历史消息、拼进上下文、处理完再更新历史。这样模型就能记住多轮对话，且不同会话的历史互相隔离。\n\n核心概念：消息历史存储（ChatMessageHistory，可用内存版或持久化版）、会话标识（session_id，区分不同用户/会话）、自动管理（历史截断/摘要可配置，防止上下文爆炸）。与阶段二记忆章节对照——RunnableWithMessageHistory 就是『短期记忆』的框架化实现，只是框架替你处理了『历史存取与拼接』的样板代码。理解原理后你会发现：它不神秘，就是把『把历史消息拼进 messages』这件事封装成了可配置的组件。",
        "case": "多轮客服对话用 RunnableWithMessageHistory：每次 `chain.invoke({'input': '再帮我查下那个订单'}, config={'configurable': {'session_id': 'user-123'}})`，组件自动从存储取出 user-123 的历史（含之前提到的订单号），模型据此理解『那个订单』指什么。切换 session_id 即会话隔离。对比手写——你要自己维护每会话的消息数组并手动拼接，框架把它封装成了配置项。",
    },
    "s3-lc-c6-s2": {
        "content": "Retriever 即 Tool，是 RAG 与 Agent 结合的关键技巧：把『知识库检索器』包装成工具，让 Agent 自主决定『何时需要查知识库』。实现非常直接——把 retriever 的 `get_relevant_documents(query)` 包成一个 @tool 函数（或直接创建一个 RetrieverTool），这样 Agent 的工具列表里就多了一个『查知识库』的工具，Agent 面对需要知识的问题时会主动调用它。\n\n这个组合的价值：让 RAG 从『固定流程』升级为『Agent 自主决策』。固定 RAG 是『每个问题都检索』，而 Retriever-as-Tool 让 Agent 判断『这个问题需不需要查资料、查什么』——不需要查的（闲聊、简单计算）直接回答，需要查的才检索，既省成本又更智能。这是 Agentic RAG 的雏形，也是阶段三 LangGraph 章节要深入的方向。理解『把检索器变成 Agent 手里的一个工具』，你就打通了 RAG 与 Agent 的结合点。",
        "case": "客服 Agent 的工具列表里有：get_order、get_weather、retrieve_knowledge（知识库检索工具）。用户问『订单到哪了』→ Agent 调 get_order；用户问『公司年假政策』→ Agent 判断需要知识，调 retrieve_knowledge 查制度库；用户问『今天天气』→ 调天气工具。Agent 自主决定何时查知识库，而不是所有问题都走 RAG——这就是 Retriever-as-Tool 带来的『按需检索』智能。",
    },
    "s3-lc-c7-s1": {
        "content": "LangChain 综合实践的终点，是把 RAG 线与 Agent 线整合成一个『能自主检索、可多轮对话、带引用』的知识问答 Agent。整合架构：Agent 持有多个工具，其中包括知识库检索工具（Retriever-as-Tool）；记忆用 RunnableWithMessageHistory 实现多轮上下文；回答时检索结果带来源元数据实现引用；整个流程由 AgentExecutor 编排循环。\n\n整合的关键是『各组件如何协同』：模型通过 Agent 循环决定调哪个工具（查知识库还是查业务），检索结果经提示词模板拼装并强制标注引用，历史经记忆组件维护。这个综合 Agent 比纯 RAG 智能（按需检索），比纯工具 Agent 懂知识（有知识库），是 LangChain 阶段的集大成之作。跑通它，你对 LangChain 的掌握就到了『能整合、能讲清架构』的层面。",
        "case": "整合后的知识问答 Agent 实际行为：『公司团建补贴怎么申请』→ Agent 判断需要查制度，调知识库检索工具拿到相关条款→ 基于条款回答『按部门预算申请，需附发票』并标注『依据《福利制度》』→ 用户追问『那能报打车费吗』→ 记忆让 Agent 记得上下文，继续检索细化回答。一条对话里，检索、工具、记忆、引用各司其职。",
    },
    "s3-lc-c7-s2": {
        "content": "LangChain 阶段完成后，要清醒认识它的边界——这直接通向下一节 LangGraph。AgentExecutor 的主要局限：循环控制不精细——它是『内部封装好的循环』，你难以在特定步骤插入人工介入、难以实现复杂的分支与回溯；流程不透明——执行过程是黑盒，虽然能看中间步骤，但难以结构化地设计『什么时候走哪条路』；状态管理弱——Agent 的中间状态没有清晰的结构化建模，复杂多步任务的协调力不足。\n\n这些局限，恰好是 LangGraph 要解决的问题：用『状态图』显式建模流程，把循环、分支、人工介入、持久化都变成可控的图节点与边。所以学习路径的设计是『LangChain 学组件化与标准流程，LangGraph 学精细控制与复杂编排』——不是替代关系，而是从『框架化』到『可控化』的进阶。带着『AgentExecutor 哪里不够灵活』的问题进入 LangGraph，你会更有针对性地学到它的价值。",
        "case": "你希望客服 Agent 在『退款操作』前暂停等人工确认（Human-in-the-Loop），AgentExecutor 里很难优雅实现——循环是封装死的。而在 LangGraph 里，这只是一个节点间的条件边+中断点，天然支持。这个『想加人工审批却发现框架不够灵活』的体验，正是 LangGraph 存在的意义——它把循环与流程的控制权交还给开发者。",
    },

    # ================= s3-langgraph LangGraph 框架 =================
    "s3-lg-c1-s1": {
        "content": "LangGraph 把 Agent 建模成一张『状态图』，核心三件套中第一个是 State（状态）。State 是整张图共享的数据对象——所有节点读写它，节点执行的结果通过更新 State 在图中传递。定义 State 时用 TypedDict 描述字段，其中消息字段通常配 `add_messages` 这个 reducer：reducer 决定『多个来源的更新如何合并』——`add_messages` 表示把新消息追加到已有消息列表（而不是覆盖），这样多轮消息能持续累积。\n\n理解 State，关键是『它是图运转的中枢』：节点的输入来自 State、输出更新 State，一条条边把 State 流转到下一个节点。与函数式编程的『不可变状态 + 纯函数』对照，LangGraph 让每个节点都是『读 State → 处理 → 返回增量』的纯函数，状态变化清晰可追溯。相比 AgentExecutor 把循环藏在黑盒里，State 让 Agent 的每一步中间状态都显式可见——这是 LangGraph 可控性的根基。",
        "case": "定义一个客服 Agent 的 State：`{'messages': Annotated[list, add_messages], 'order_id': str, 'stage': str}`。messages 用 add_messages 自动累积多轮对话；order_id 记录从用户输入中提取的订单号；stage 记录流程走到哪一步。图每执行一个节点，State 就被更新一次，调试时打印 State 就能看清 Agent 每一步想什么、做什么。",
    },
    "s3-lg-c1-s2": {
        "content": "State、Node、Edge 三件套中，Node（节点）是处理单元，Edge（边）决定流转。Node 是一个函数：输入整个 State，执行处理（调 LLM、调工具、做判断），返回一个『State 的增量』（要更新的字段）。Edge 连接两个节点，定义执行顺序；特殊的 START 是图的入口（任务从哪开始），END 是图的出口（任务在哪结束）。一条最基本的图就是『START → 节点A → 节点B → END』。\n\nNode 的设计原则：职责单一——每个节点只做一件事（『调模型决策』是一个节点、『执行工具』是另一个节点），这样方便复用、测试和调试；Node 返回增量而非整个 State——只声明它改动的字段，其余保持不动，降低节点间耦合。理解 Node/Edge/State 的协作，你就理解了 LangGraph 的核心心智：把 Agent 的流程『画』成一张可执行、可观察、可控制的图。",
        "case": "一个简单客服图：`builder = StateGraph(State)`；`builder.add_node('agent', agent_node)` 模型决策；`builder.add_node('tools', tool_node)` 执行工具；`builder.add_edge(START, 'agent')`；`builder.add_edge('tools', 'agent')` 工具执行完回到 agent 再决策；`builder.add_conditional_edges('agent', ...)` 判断是否结束。这张图把『决策→执行→再决策』的循环显式画了出来——对比 AgentExecutor 的黑盒循环，每一步都在图上看得清清楚楚。",
    },
    "s3-lg-c2-s1": {
        "content": "条件路由（add_conditional_edges）是 LangGraph 实现『Agent 自主决策流向』的机制：一条边不是固定的 A→B，而是根据一个判断函数的结果，决定走哪条边。典型场景：Agent 节点执行完，判断『模型是否请求了工具调用』——请求了就走 tools 节点，没请求（模型给出最终答案）就走 END 结束。这个判断就是一条条件边。\n\n条件路由的核心函数是『路由函数』：输入 State，返回一个字符串（走哪条路），LangGraph 根据返回值把执行转到对应节点。它是 ReAct 循环的『方向盘』——决定 Agent 是继续行动还是收尾。相比 AgentExecutor 固定封装好的循环，条件路由把『什么时候继续、什么时候停止』的控制权交给了你：你可以加多条条件（工具可用性、迭代次数、质量判断），让 Agent 的流向完全按你的设计走。",
        "case": "ReAct 图的条件路由：`def should_continue(state): return 'tools' if state['messages'][-1].tool_calls else END`。agent 节点调完模型后，路由函数检查最后一条消息有没有 tool_calls——有就回到 tools 执行，没有就结束输出最终答案。这个几行的路由函数，就是整个循环的『刹车与油门』，全部由你控制。",
    },
    "s3-lg-c2-s2": {
        "content": "用 LangGraph 手搓 ReAct，是理解『框架化循环』的巅峰练习。核心节点两个：agent 节点——调 LLM，输入系统提示词与对话历史，模型输出可能包含工具调用请求；tools 节点——用 ToolNode 包装工具列表，执行 agent 请求的工具并返回结果。加上一条条件边（agent 决策走 tools 还是 END），就构成了完整的 ReAct 循环：agent → tools → agent → … → END。\n\n与 AgentExecutor 的对比是这一节的关键收获：AgentExecutor 把整个循环封装成一个黑盒对象，你只能配置；LangGraph 则让你亲手『画出』每个节点、每条边——循环如何流转、何时停止、哪里能插进人工介入，全部由你设计。这就是学习路径说的『从黑盒流程拆成可控状态图』。手搓一遍 ReAct，你会对『Agent 循环到底在做什么』有本质的理解，也能举一反三地设计更复杂的图。",
        "case": "手搓 ReAct 的最小代码：agent 节点 `llm.bind_tools(tools).invoke(state['messages'])`，ToolNode 执行 `tool_node.invoke(state['messages'])`，条件边 `add_conditional_edges('agent', should_continue, {'tools':'tools', END:END})`。跑起来后 verbose 打印 State，你能看到每一次 Thought/Action/Observation 的流转。相比 AgentExecutor 的『配一下就跑』，这里每一步都来自你的设计。",
    },
    "s3-lg-c3-s1": {
        "content": "Checkpointer（检查点）让 LangGraph 的 Agent 具备『跨轮次记忆』能力。原理：图每执行完一个节点，Checkpointer 就把当前的 State 完整保存一份（内存 MemorySaver 或持久化 SQLite）；当带相同 thread_id 的新请求到来时，从检查点恢复之前的 State，继续往下执行——而不是从零开始。这实现了『多轮对话续上』：第一轮跑到一半，第二轮接着跑。\n\n对比阶段二的短期记忆：那是『把历史消息拼进上下文』的软记忆；Checkpointer 是『保存完整执行状态』的硬记忆——不仅记得聊过什么，还记得流程走到哪一步（State 里的 stage、order_id 等）。工程上 Checkpointer 的价值：多轮任务续跑（跨请求保持状态）、断点恢复（崩溃后从保存点继续）、流程状态持久化。理解 Checkpointer 的『保存 State』机制，你就知道了 LangGraph 如何做到 Agent 的状态跨轮存活。",
        "case": "一个多步下单 Agent：第一轮模型已调用了 get_order 拿到订单信息、State 里存了 order_id，但还差确认那步；用户第二轮说『确认下单』。带 Checkpointer 后，第二轮从上次的 State 恢复，模型记得 order_id 和已完成的步骤，直接执行确认。没有 Checkpointer 的话，第二轮要重新查订单、重走流程——Checkpointer 让多轮复杂任务真正『接得上』。",
    },
    "s3-lg-c3-s2": {
        "content": "thread_id 是 Checkpointer 的『会话钥匙』，它区分不同的对话流。每次调用图时传入 thread_id（如用户 id + 会话 id），Checkpointer 就按 thread_id 存储与恢复对应的 State。这样：同一个 thread_id 的多次调用共享一条连续的状态流（多轮续跑）；不同 thread_id 完全隔离（用户 A 的流程不会串到用户 B）。\n\nthread_id 的设计要点：线程粒度要合理——通常『用户 + 会话』一级（一个会话一条线程），而不是『整个用户』（否则该用户所有会话的状态混在一起）或『每次调用』（那就没有记忆了）；thread_id 要稳定唯一，作为状态存储的主键。理解 thread_id 与 Checkpointer 的配合，你就掌握了 LangGraph 多轮记忆的完整机制——它与阶段二『会话隔离』的思想一脉相承，只是在这里落地为状态级隔离。",
        "case": "客服 Agent 用 `config={'configurable': {'thread_id': f'{user_id}-{session_id}'}}` 区分会话。用户甲的退款流程跑到『待确认』暂停，用户乙新开一个 thread_id 从零开始，互不干扰；甲回到同一会话继续，从检查点恢复继续确认流程。thread_id 就是那个『让每个会话拥有独立状态空间』的钥匙——多用户并发、互不串扰。",
    },
    "s3-lg-c4-s1": {
        "content": "Human-in-the-Loop（人工介入）是生产级 Agent 的关键能力，LangGraph 用 interrupt_before 实现『在指定节点前暂停，等人工确认』。机制：`interrupt_before=['要暂停的节点名']`——图执行到该节点前会停下，把当前 State（含待确认的信息）交给外部；程序拿到暂停状态，向用户展示待审批内容；用户批准后，用 `Command(resume=...)` 或相同 thread_id 续跑，图从断点继续执行。\n\n为什么需要人工介入：Agent 自主执行高风险动作（付款、删除、发消息）风险太大，必须让人把关。interrupt_before 的意义，是把『人工审批』优雅地嵌入 Agent 流程——不是事后检查，而是在执行到关键节点前强制暂停，等人点头才继续。这比『Agent 全自动』安全，比『纯人工』高效。工程上，interrupt 的触发节点要精心选择——只在真正需要人判断的高风险节点暂停，普通步骤让它自动走完。",
        "case": "退款流程的图：查订单 → 校验资格 → interrupt_before('execute_refund') 暂停 → 展示『用户 XXX 申请退款 199 元，是否批准』→ 人工批准 → 从 execute_refund 继续执行退款。整个过程中，Agent 完成了 95% 的自动化处理，只有『真正花钱』这一步停下来等人。这就是 Human-in-the-Loop：把自主权交给 Agent，把决定权留给人。",
    },
    "s3-lg-c4-s2": {
        "content": "设计人工审批场景，核心是回答两个问题：哪些动作必须审批、审批不过怎么办。必须审批的动作通常是『有真实后果的高风险操作』——资金类（付款、退款、转账）、破坏类（删除数据、批量修改）、对外类（发送消息、发布内容）、越权类（访问敏感数据）。判断标准：这个动作执行后是否难以撤回、是否有真实成本、是否影响他人。\n\n审批不过的兜底设计同样重要：拒绝后 Agent 如何继续——告知用户被拒绝及原因、终止该流程、或提供替代方案（如降级为人工客服）。审批记录要可审计（谁审的、什么时候、批不批）。工程实现上，把审批做成一个统一组件（审批请求 + 结果回调），不同高风险节点复用。设计良好的审批流，让 Agent 既高效又安全——它敢放手做事，因为有闸门兜底。",
        "case": "一个运营 Agent 的高风险动作清单：『发送营销短信』『删除用户数据』『调整价格』都配置 interrupt 审批，而『查询数据』『生成报表』自动执行。实测：高频的低风险操作全自动（效率），偶尔的高风险操作人工确认（安全）。审批记录入库，事后可审计谁批准了什么。这就是『该自动的自动、该审批的审批』的分层设计。",
    },
    "s3-lg-c5-s1": {
        "content": "LangGraph 综合实践，是把前面所有能力整合成一个完整的客服 Agent（课程里的『小 G』）。系统设计全景：RAG 工具（知识库检索，回答制度/产品问题）+ 业务工具（查订单、查物流、改单等）+ Human-in-the-Loop（敏感操作审批）+ Checkpointer 多轮记忆 + 条件路由与循环控制。技术栈：FastAPI 提供接口 + LangGraph 编排 + Chroma 存储知识 + 本地/云端模型 + Docker 部署。\n\n整合时抓住三个要点：一是『图先设计后编码』——先画出节点与边（agent/tools/审批/记忆），理清流程再写代码；二是『State 字段设计』——想清楚哪些信息要跨节点传递（order_id、stage、messages）；三是『每个能力落成图的部件』——RAG 是检索工具、审批是中断点、记忆是 Checkpointer，各归其位。这个『小 G』是 LangGraph 阶段的集大成之作，也是阶段六『智能客服项目』的预演。",
        "case": "『小 G』的实际运转：用户问『我昨天买的耳机什么时候到』→ agent 节点决策调 get_order → 工具节点查物流 → 返回『明天到』；用户又问『能退吗』→ agent 检索知识库退换货制度 → 回答符合条件但提示『退款需人工确认』→ 执行退款时 interrupt 暂停等人工。多轮记忆让『昨天买的耳机』的上下文延续到后续问题。一张图，串起了 RAG、工具、审批、记忆的完整能力。",
    },
    "s3-lg-c5-s2": {
        "content": "LangGraph 阶段与阶段五的衔接点，在于『可观测与可评估』。LangGraph 的图结构天然适合观测——每个节点、每条边、每次 State 更新都是显式的，这为阶段五的监控调试（LangSmith 记录每一步执行）提供了完美基础：你可以把 LangGraph 的节点执行、State 变化、Token 消耗全部接入 LangSmith 或自建日志，回放 Agent 的每一步。\n\n同时，图结构也适合评估——每个节点都可以单独测试（单独测 agent 节点的决策质量、单独测检索工具的效果），而不是只能黑盒地测整个 Agent。带着『图怎么监控、节点怎么评估』的意识完成本节，你进入阶段五时会有非常自然的技术衔接。这也是学习路径把 LangGraph 放在阶段三、把『优化和部署』放阶段五的原因——先把能力（图编排）做出来，再谈监控与上线。",
        "case": "把『小 G』接入 LangSmith 后，一次完整对话的可观测数据：agent 节点调了 3 次 LLM（含每次的输入输出 Token）、检索工具命中 2 块文档、审批暂停 1 次、总成本 X 分。回放整条 trace，能精确定位『是哪一次决策导致回答不准』。这种『每一步都看得见』的观测能力，正是阶段五监控评估的基础，也是 LangGraph 相比黑盒框架的优势。",
    },

    # ================= s3-agentic-rag Agentic RAG 进阶 =================
    "s3-agc1-s1": {
        "content": "传统 RAG 是『一条直线』：检索 → 拼装 → 回答，没有任何质量反馈。当这条直线遇到三类情况时，会直接输出错误答案：一是检索无结果——库里没有相关内容，模型只能瞎猜或答非所问；二是检索到不相关内容——命中的是无关文档，模型『照着错的资料答』，答得越有依据错得越彻底；三是文档本身过时或有误——资料是错的，模型基于错误资料给出貌似合理的错误答案。这就是『垃圾进、垃圾出』——RAG 的质量上限被检索质量锁死，而直线流程没有任何机制发现并纠正。\n\nAgentic RAG 的出发点，正是承认『检索可能失败』这个现实：与其祈祷每次检索都准，不如在流程里加入『检查与纠错』的能力——检索完先判断结果是否相关，不相关就改写重查；生成完检查回答质量，不合格就重做。把一条直线改造成一个有反馈的循环，这是 Agentic RAG 的核心思想。",
        "case": "用户问『公司是否有加班补贴』，直线 RAG 检索命中的是『考勤制度』里的『迟到扣款』条款——不相关，模型却基于它回答『没有补贴，迟到还会扣款』，错误且误导。如果是 Agentic RAG：检索后先做相关性评分，发现『迟到扣款』与『加班补贴』无关，触发查询改写重新检索，命中真正的『薪酬福利制度』里的加班补贴条款，给出正确答案。一次失败的检索，被循环机制纠正了。",
    },
    "s3-agc1-s2": {
        "content": "从直线到图，是把 RAG 从『一次检索→回答』升级为『有反馈的循环流程』。核心变化是增加两个判断点：检索后增加『相关性评分』——判断检索到的结果与问题是否相关，不相关就触发重试；生成后增加『质量检查』——判断回答是否合格（是否基于资料、是否答非所问），不合格就重做。加上『查询重写』——发现检索不好时，先改写问题再重查。这几个环节用图（LangGraph）组织起来，就形成了 retrieve → grade → rewrite → generate → check 的闭环。\n\n这个升级的本质，是把『错误的可能性』纳入设计：直线 RAG 假设检索一定准（不设防），图式 RAG 假设检索可能失败（设了检查与纠错）。代价是流程复杂度和成本上升（多了评分、重写、检查等多次 LLM 调用），换来的是正确率的显著提升。工程上的取舍，是用『只在关键处设卡』控制成本——不是每个问题都重查重做，而是不合格才触发。",
        "case": "同样的问题集对比：直线 RAG 在『问题口语化、术语模糊』的 100 个问题上答对 72 个；升级为 Agentic RAG 后答对 89 个——那 17 个提升，都来自『检索失败被检查发现并纠正』的环节。代价是平均每次问答的 LLM 调用从 1 次升到 1.6 次。这是典型的『用成本换正确率』，适合对准确率要求高的企业知识库场景。",
    },
    "s3-agc2-s1": {
        "content": "Agentic RAG 的核心循环是五节点流水线：retrieve（检索）——向量检索取回候选文档；grade（评分）——判断检索到的文档与问题是否相关，相关则进入下一步，不相关则走重写；rewrite（重写）——把原问题改写得更适合检索（补全、消歧、换措辞），重新检索；generate（生成）——基于合格的检索结果生成回答；check（检查）——校验回答质量（是否基于资料、是否完整、是否幻觉），合格则输出，不合格则回到重写或重生成。\n\n这条循环的精髓是『质量门控』：每一步都设了一道质量关卡，不过关就回流重做，而不是放任错误一路到底。五节点用 LangGraph 实现时，每个节点是一个函数，节点间的条件边决定『合格继续、不合格回流』。理解这个循环，你就掌握了 Agentic RAG 的全貌——它把『检索—生成』从一次性的直线，变成有反馈、可纠错、能兜底的闭环。",
        "case": "一次问答的完整循环：用户问『调休怎么算』→ retrieve 命中『请假制度』中的『调休』条款 → grade 判断相关（通过）→ generate 生成『调休需提前申请，按 1:1 补休』→ check 发现回答缺少『适用范围』这一关键信息 → 判定不合格 → rewrite 补充问题再检索 → generate 补全完整回答 → check 通过输出。可以看到，正是 check 的严格，逼出了更完整的最终回答。",
    },
    "s3-agc2-s2": {
        "content": "用 LangGraph 实现 Agentic RAG，是把五节点流水线『画成图』的过程。每个节点（retrieve/grade/rewrite/generate/check）都是一个函数，读写共享的 State（消息、检索结果、评分、重试次数）；节点之间用条件边连接——grade 后按评分走『continue（相关）』或『rewrite（不相关）』；check 后按质量走『output（合格）』或『retry（不合格）』。State 里要记录重试次数（retry_count），供循环刹车使用。\n\n实现要点：复用阶段三 LangGraph 的知识——State 设计（存什么问题、检索结果、评分、重试次数）、add_conditional_edges（条件路由）、interrupt/Checkpointer（如需人工介入或跨轮续跑）。相比手写循环，LangGraph 让五节点的流转、分支、兜底都可视化、可控化——这是 Agentic RAG 的『骨架』，也是它区别于普通 RAG 的实现基础。",
        "case": "LangGraph 图的实现骨架：`add_node(retrieve)`、`add_node(grade)`、`add_node(rewrite)`、`add_node(generate)`、`add_node(check)`；`add_conditional_edges('grade', route_grade, {'continue':'generate','rewrite':'rewrite'})`；`add_conditional_edges('check', route_check, {'output':END,'retry':'rewrite'})`。跑起来后，图会清楚地显示一次问答走了哪条路径——几次检索、几次重写、几次重生成，全部可视化。",
    },
    "s3-agc3-s1": {
        "content": "文档评分（Grading）是 Agentic RAG 的第一道质量关卡：在检索结果进入生成之前，先判断这些结果与问题是否相关。实现方式：让 LLM 扮演评审，输入『问题 + 检索到的文档片段』，输出一个相关性判断（相关/不相关，或一个相关度分数）。评分的粒度有两种：整体判断（这批检索结果是否可用）或逐条判断（对每条结果分别打分，只把合格的喂给生成）。\n\n评分是 Agentic RAG 纠错链的关键起点：评分发现不相关，才触发查询重写重新检索；评分通过，才让检索结果进入生成。评分的质量直接影响整个循环的有效性——评分太松，垃圾结果进入生成（等于白设卡）；评分太紧，相关结果被误杀（浪费重试）。所以评分提示词要写清楚『什么是相关』的标准（如『片段需包含与问题直接相关的信息，仅有模糊关联不算』），并用测试集校准评分的一致性。",
        "case": "一条评分提示词的实践：『判断以下片段是否包含回答用户问题所需的信息，只输出 YES 或 NO。问题：X；片段：Y』。实测发现模型对『部分相关』的片段经常误判为相关，导致垃圾结果进入生成。改进评分标准：『必须包含可直接回答问题的具体信息，泛泛的背景介绍判 NO』后，评分准确率提升，触发重写的判断更可靠。评分标准写得好不好，决定纠错链动不动得起来。",
    },
    "s3-agc3-s2": {
        "content": "查询重写（Rewrite）是检索失败后的第一张纠错牌：当 grade 判定检索结果不相关时，先用 LLM 把原问题改写得更适合检索，再重新检索。改写要解决三类问题：指代不明（『那个』『这个』——补全省略的主语）、口语化（『能退不』——转成规范查询『是否支持退货』）、关键词不准（用户用词与文档用词不一致——换成更可能命中的措辞）。\n\n重写与直线 RAG 的查询改写（s3-rag-c5）不同：直线 RAG 的改写是预防性的（每次都改）；Agentic RAG 的重写是响应性的（评分不合格才改），更精准、更省成本。工程要点：重写次数要有限制（配合 retry_count，见 s3-agc5），否则会陷入『改写—检索—评分不过—再改写』的无限循环；每次重写要基于上次失败的原因（为什么检索不到），让改写有方向而非盲目换词。",
        "case": "用户问『那个能退吗』，初次检索命中无关的『商品介绍』。grade 判定不相关 → 触发重写：模型把问题补全为『该商品是否支持七天无理由退货』→ 重新检索命中『退换货政策』相关条款 → grade 通过 → 正确回答。一次成功的重写，把『指代不明的口语』转成了『规范检索词』，救了原本要失败的检索。",
    },
    "s3-agc4-s1": {
        "content": "幻觉检测（Hallucination Detection）是生成阶段的质量守护：回答生成后，检查它是否『忠实于检索到的资料』，防止模型脱离资料自由发挥。实现方式：让 LLM 扮演核查员，输入『问题 + 检索资料 + 模型回答』，输出判断——回答中的关键论断是否都能在资料里找到依据；找不到依据的部分，就是潜在的幻觉。更工程化的做法是逐句核查：把回答拆成断言（claim），逐个与资料比对，标记无法验证的断言。\n\n幻觉检测的本质，是给回答加一道『事实性闸门』：模型倾向于顺着话头编造，这道检查专门拦截『编出来的部分』。检测发现幻觉时，处置方式通常是回到重生成（基于更严格的约束重做）或标记存疑（告诉用户『这部分未找到资料依据』）。结合学习路线反复强调的『RAG 减少幻觉』，Agentic RAG 的幻觉检测是把『减少』变成『可拦截』——不仅能降低幻觉，还能在幻觉发生时把它抓出来。",
        "case": "模型回答『调休按 1:1.5 倍计算』，幻觉检测把回答拆成断言后逐条比对资料：发现『1:1.5 倍』在检索资料里没有依据（资料写的是 1:1），判定为潜在幻觉 → 触发重生成，这次模型基于资料回答『按 1:1 补休』。一次本会误导用户的幻觉，被检测环节拦截在输出之前。这就是把『可能幻觉』变成『可拦截』的价值。",
    },
    "s3-agc4-s2": {
        "content": "回答质量检查（Answer Checking）是生成后的整体把关，检查的不只是事实，还有回答的完整性与合理性。三类常见问题：一是答非所问——模型理解了错误意图或跑题；二是不完整——回答了部分问题，漏了关键点（如只答了『能退』没答『怎么退』）；三是自相矛盾——回答前后说法不一致。质量检查的输入是『问题 + 检索资料 + 回答』，输出是质量判定（合格/不合格 + 原因）。\n\n与幻觉检测的关系：幻觉检测聚焦『事实有没有依据』，质量检查聚焦『答得对不对、全不全』——两者互补，共同构成生成后的双保险。检查不合格时，通常触发重生成（带着检查发现的问题，要求模型补全或修正），并计入 retry_count 控制次数。工程建议：检查提示词明确列出质量维度（相关性、完整性、一致性），让检查有据可依、结果可解释。",
        "case": "用户问『调休和年假冲突吗』，模型回答『调休需提前申请』——答非所问（没回答冲突关系）。质量检查判定『未回答用户核心问题（冲突关系）』→ 触发重生成 → 模型这次先查两者的适用范围，再回答『不冲突，二者独立使用，但调休需在当季用完』。一次不完整甚至跑题的回答，被质量检查拦下并修正。检查的维度清晰，修正才有方向。",
    },
    "s3-agc5-s1": {
        "content": "Agentic RAG 的循环必须设刹车，retry_count 就是那个刹车：在 State 里记录『已经重试了几次』，重写、重生成、重检都受这个计数约束——超过上限（如 3 次）就停止循环，走兜底路径（如实告诉用户『未找到足够资料』）。没有刹车，『检索不中→重写→再检索→评分不过→再重写』会无限循环，Token 成本和延迟都失控。\n\n设计 retry_count 的要点：上限要合理（太少浪费纠错机会，太多烧 Token——通常 2-3 次）；每个环节单独计数（重写重试、重生成重试分开计）或共享一个总计数（更简单）；到达上限后的兜底要明确——是直接输出『未找到相关资料』，还是降级为直线 RAG 输出一个弱答案，还是转人工。刹车的意义不只是防失控，更是『给循环定义一个可预期的终点』——好的 Agentic RAG 一定能正常结束，哪怕结果是说『不知道』。",
        "case": "一个用户问的知识库里确实没有的问题，Agentic RAG 的流程：首次检索无结果 → 重写再检索（retry 1）→ 仍无 → 重写再检索（retry 2）→ 仍无 → 达到上限 → 输出『抱歉，知识库中未找到相关资料』。整个流程 3 次检索后干净结束，而不是无限尝试。如果没有 retry_count，这个问题可能触发几十次无效检索，成本失控且用户体验极差。",
    },
    "s3-agc5-s2": {
        "content": "Agentic RAG 的成本核算，是决定『要不要开、开多大』的决策依据。每次循环都是多次 LLM 调用：一次正常问答 = 1 次检索 + 1 次评分 + 1 次生成 + 1 次检查 ≈ 4 次调用；一旦触发重写/重生成，每次多加 1-2 次。相比直线 RAG 的 1 次调用，Agentic RAG 的成本是 3-5 倍。因此上线前必须做成本评估：算清单次问答的平均调用次数与 Token 成本，乘以预估问答量，得到月度成本，再与『正确率提升带来的价值』对比。\n\n降成本的策略：一是分级开启——只对『检索置信度低』或『领域知识敏感』的问题开自纠错，简单问题走直线 RAG（用评分或规则分流）；二是控制触发率——提高评分阈值让更多检索一次通过，减少重写触发；三是预算熔断——设置单次问答的调用次数或成本上限，超过就走直线降级。成本核算的本质，是把 Agentic RAG 当作一个『有成本的质量投资』来管理——投资多少取决于正确率提升值不值。",
        "case": "某知识库改造 Agentic RAG 前先算账：预估月问答 10 万次，直线 RAG 平均 1.2 次调用/次，Agentic RAG 平均 3.5 次/次，Token 成本约翻 2.5 倍。他们最终采用『分级开启』：80% 的简单问题走直线 RAG，只有检索评分低于阈值或涉及金额/法规的高风险问题走完整自纠错，整体成本只增加 40%，却获得了大部分的正确率提升。成本控制不是牺牲质量，而是把预算花在刀刃上。",
    },
    "s3-agc6-s1": {
        "content": "把 Agentic RAG 服务化，是把它从『一个能跑的图』变成『一个可用的 API』。推荐接口设计：问答接口 `POST /query`——输入问题，输出答案 + 检索轨迹（走了几次检索、几次重写、引用哪些文档）；单独检索接口 `POST /retrieve`——只做检索可视化，展示命中片段与评分（便于调试与演示）；质量统计接口 `GET /stats`——返回问答量、平均调用次数、重写率、重试分布等指标（对接监控）。用 FastAPI 承载，图逻辑封装成服务层。\n\n服务化的要点：一是把图的『中间状态』暴露出来（检索轨迹、评分结果）——这是 Agentic RAG 区别于直线 RAG 的展示亮点，也是调试依据；二是接入可观测（LangSmith 或自建日志）记录每次调用的节点路径与成本；三是并发与性能——图调用是重操作，用异步处理、加超时与重试。服务化完成后，这个『带检索轨迹的自纠错知识库』就是阶段六项目一的核心升级版。",
        "case": "服务化后的交互效果：调用 `POST /query` 问『年假怎么算』，返回 `{answer: '按工作年限 1-10 年 5 天…', trace: [{step:'retrieve',hits:3},{step:'grade',result:'pass'},{step:'generate'}]}`。前端可以展示回答、引用与检索过程——用户看到『这个答案查了这些资料、经过了质量校验』，信任感大幅提升。检索轨迹既是功能，也是可观测性的体现。",
    },
    "s3-agc6-s2": {
        "content": "Agentic RAG 是阶段六『RAG 知识库问答系统』项目的核心升级——它把项目的技术含量从『能问答』提升到『问答得准、还能自纠错』。在项目里的落地路径：先做直线 RAG 版本（项目基线，能跑、能演示）；再升级 Agentic 版——把检索评分、查询重写、自纠错用 LangGraph 图化，作为项目的『进阶亮点』；最后在简历与面试里，把『从直线 RAG 到 Agentic RAG』的演进讲成一条清晰的技术主线：为什么要升级、遇到什么问题、怎么解决、效果如何。\n\n这个衔接的设计意图：项目实战阶段不是重复造轮子，而是把阶段三打磨的能力（Agentic RAG）做成完整产品。面试时，能讲清『直线 RAG 的局限 → 我如何用图加纠错 → 成本如何控制』的候选人，展现的是『有深度、有方法、有数据』的工程能力，这正是 RAG 工程师岗位最看重的东西。",
        "case": "项目演示的讲述线：『我的知识库项目分两版——第一版直线 RAG，检索偶尔不准导致答错；我分析错误模式后升级第二版：加评分门控和查询重写，检索不中自动重查，回答自动检查，用 LangGraph 编排。升级后准确率从 72% 提到 89%，成本通过分级开启控制在 +40%。』这段讲述同时展示了：发现问题、分析根因、技术方案、量化效果——是求职作品里最有说服力的叙事。",
    },
}
